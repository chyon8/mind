# RUDY-STATUS.md — 지금 상태 (세션 핸드오프)

> **이 파일만 읽는다.** `RUDY-ARCHIVE.md`·`RUDY-BUILD.md`는 열지 마라 — 끝났거나 대체된 계획이
> 가득해서 읽으면 틀린 추론을 한다. 2026-08-02에 새 세션이 정확히 그 사고를 냈다: 아카이브에
> 남아 있던 "다음은 넛지" 블록을 읽고 1순위로 답했는데 **넛지는 8/1에 이미 끝나 있었다.**
> 유저가 "그때 왜 그렇게 정했나"를 물을 때만 연다.
>
> 설계=RUDY.md · **발견/브리핑 기준=RUDY-DISCOVERY.md(작업 전 필독)** · 지금 상태=여기.
>
> ⚠️ **RUDY-DISCOVERY.md는 런타임 입력이다.** `npm run discover`가 `--allowedTools Read`로 돌고
> `scripts/discover-websearch/prompt.md`가 §1·§2·§3·§5·§6·§9를 **절 번호로 집어** 읽으라고
> 지시한다. **절을 지우거나 번호를 바꾸면 발견이 조용히 나빠진다.** 문서 정리 대상 아님.
>
> 마지막 갱신: 2026-08-09 (464줄 → 정리. 완료된 작업의 구현 기록과 만료된 실측 로그는
> 걷어냈다 — git log가 원장이다).

## 다음에 개발할 것

**남은 확인·배포는 없다.** 판 A/B·아침 브리핑 4차·A3+D1 결합까지 전부 끝났다.
"실기 검증부터 하자"고 답하지 마라.

| 순위 | 무엇 | 왜 지금 | 착수 전 볼 것 |
|---|---|---|---|
| **1** | **투두** — 앱 안, 파편과 완전 분리 | 백로그의 절반(구매 결정 지원 · 막연한 할 일 쪼개기)이 투두를 전제로 한다. 지금 Mind가 이슈 트래커로 쓰이며 깨져 있다 | 아래 「투두」 절 |
| **2** | **라이트 모드 · 리스트 엔티티** | Rudy가 아니라 앱 쪽. 오래 밀려 있다 | ⚠️ SPEC §7과 충돌 소지 — 착수 전 유저 확인 |

**잔업 (개발 아님, 몇 분짜리):**
- **RUDY-DISCOVERY.md 축소가 항목 6→8개로 늘린 게 진짜인지 아직 가설(n=2).** 발견 돌릴 때
  항목 개수·`lens`/`resurface` 유무만 몇 번 더 눈여겨보면 된다.
- **`idea` slot이 2회 연속 소재 절단에 성공했다** (8/1 1/2 → 8/2 2/2). RUDY-DISCOVERY-ARCHIVE §7-e가
  "네이티브 web_search 자유탐색이 마지막 시도, 실패하면 slot을 접는다"고 남겨뒀던 게 이 실험이다 —
  몇 번 더 보고 §7-e에 결론(유지 확정)을 추가할 것.
- 채팅 모델 `gpt-4o` 올릴지 — 며칠 써보고 판단하기로 함(2026-07-31).
- `fragments.source` 컬럼이 DB에 남아 있다(nullable, 아무 코드도 안 읽고 안 씀).
  지우려면 `alter table fragments drop column source;` — 안 지워도 무해하다.

### ⚠️ 다음 할 일이 **아닌** 것 (반복해서 오답이 나온 자리)

- **넛지(A3) — 끝났다.** 2026-08-01에 **아침 브리핑 안의 카드**로 들어갔다.
  원래 계획(`recall.ts`에 슬롯 1칸 예약)은 **실측으로 기각**됐다: `touch_count` 분포가 63:4라
  "최저 가중치라 5배 덜 뽑힌다"는 전제가 거짓이었고, 떠오르기 랜덤 풀과 넛지 후보가 **72% 겹쳐서**
  이름만 바꿔 같은 걸 두 번 보여주는 꼴이었다. **다시 recall에 붙이자고 하지 마라.**
- **막혀 있는 결정에 먼저 손 내밀기 (A3+D1) — 끝났다** (2026-08-03, 커밋 `a420dd6`).
  아침 브리핑 넛지 카드에 `link` 타입일 때만 "후보 찾아줄까" 칩. 채팅으로 `fid`+`mode=more_like`를
  들고 진입하되 **자동 전송하지 않는다**(`draft` 프리필). **다시 만들자고 하지 마라.**
- **주간 편지(§4-F5) — 안 만들기로 했다** (2026-08-02). 아침이 매일 나오면 같은 재료를 한 번 더
  훑는 중복이고, "주 1회 희소성"은 실측이 아니라 추정이었다. 다시 꺼낼 조건: 아침을 며칠 돌려보고
  "매일은 과하다"가 실제로 느껴질 때. 그땐 주기만 늘리면 된다.
- **아침 브리핑 만들기 버튼 / 발견 만들기 버튼 — 앱에서 뺐다.** 앱은 읽기만 한다.

---

## 지금 돌아가는 것

| 표면 | 어떻게 | 만드는 곳 |
|---|---|---|
| **아침 브리핑** | 파편 전량을 읽고 관찰 + 패턴 1개 + 질문 1개 + 넛지 | `npm run morning` (맥, 클코) |
| **발견** | 바깥에서 물어온다. 재시도 금지판 | `npm run discover` (맥, 클코) |
| **채팅** | 파편 전량(`·무덤` 표시) + 축 + 바깥. `gpt-4o` | Edge `chat` |
| **떠오르기** | 4시간마다 4개. 흘려보내기 쿨다운 7일, 최근 본 것 24개 제외 | 앱 로컬 |
| **파편 상세 칩** | `이거 관련 뭐 있었지`(안쪽) / `채팅하기`(자유) / `more like this`(바깥) | 앱 + Edge `chat` |
| **무리** (2026-08-12) | 살아있는 파편만 평균연결로 묶어 접힌 목록으로 보여준다. **Rudy 아님 — LLM 무관, 원장 무관, $0.** 사이드바 진입 | Edge `groups` + `rudy.group_edges`(SQL) |

**앱은 이제 발견도 아침도 안 만든다. 읽기만 한다** (유저 지시 2026-08-02).
Edge `discovery` 함수와 `lib/rudy.ts streamBriefing`은 **안 지웠다** — 부르는 사람이 없으면
안 돈다. 되살릴 때 배포부터 다시 하지 않으려고 남겼다.

**꺼둔 것:** 자발적 연결(§4-C1) · 늦은 의도(§4-F1) — 2026-08-01, 로그로 오작동 확인 후.
`findLink`는 삭제, `intent.ts` 로직은 살아 있다(다시 부르면 됨).

**발견 병렬판은 지웠다** (2026-08-09). 8/6 실측에서 2.6배 빨랐는데도(261초 vs 691초) 유저가
품질로 채택판을 골랐다. 되살릴 근거가 필요하면 git log의 `6e6a63c`·`2ef2245`에 코드·설계 이유가
그대로 있다 — 다시 만들 땐 거기서 시작하지 처음부터 새로 만들지 마라.

---

## 절대 건드리지 말 것 (유저가 되돌리며 확정한 값)

- **`LENS_MAX = 2` · `IDEA_MIN = 2`(둘 다 프롬프트 요구).** 원래 "관점 한 개 넘지 않게"였는데
  **2026-07-31에 유저가 2로 올렸다** — 같은 날 `idea`를 "만들 만한 것" → **"누가 이미 만들어
  파는 물건"**으로 못박으면서, 밀려나는 오프라인 갈래(스파·텃밭·클래스·전시)를 받을 자리가
  필요해졌기 때문이다. 둘은 **한 세트다 — 하나만 되돌리지 마라.**
  ⚠️ 지금 코드에 `LENS_MAX` 상수는 없다(앱 경로는 07-27에 `lens`를 통째로 뺐고, 클코 판은
  프롬프트로만 건다). **관점은 0개여도 된다** (유저 2026-07-28) — 상한이지 하한이 아니다.
- **출처 링크 = 항목당 1~3개** (2026-07-31 유저 확정, 1개에서 되돌림). 조건은 "뒤 링크가
  앞과 **다른 걸** 보여줄 때만". 상세는 `RUDY-DISCOVERY.md` §6.
- **`<이미 다룬 주제>` 힌트 = 250(사실상 전체).** 30으로 줄였다가 **되돌렸다** — 잘린 중복 6개가
  전부 힌트 밖이었다. **막는 게 잡는 것보다 6배 싸다**(입력 $5/1M vs 출력 $30/1M).
- **`fragLine`에서 `{id:}` 제거, URL은 유지.** URL은 유저가 승인한 적 없어서 복원했다 — 다시 빼지 마라.
- **`done` 프로젝트는 재료에서 제외** — ⚠️ **발견 재료에 한한 규칙이다.**
  **아침 브리핑은 정반대로 `done`을 반드시 포함한다** (2026-08-02) — 상태를 안 보면 침묵과
  종료가 똑같이 0으로 보여서 "Caselab이 완전히 멈췄다"고 오독했다. 한 규칙을 두 표면에 옮기지 마라.
  `paused`는 양쪽 다 남긴다(다시 시작 가능).
- **재료 구획 3분리**: `진행 중인 일`(active, 캡 2개) / `아이디어·수집함`(💡·글감·paused, 캡 없음) / `미소속`.
  **"프로젝트"라는 단어를 lists 구획에 쓰지 마라** — 라벨이 캡 소속을 정한다.
- **원장 저장은 조립 *전에*.** 되돌리면 07-26 사고(화면엔 나왔는데 기록 0건)가 재발한다.
- **잘 도는 것은 손대지 마라** (유저 명시): 각도 프롬프트의 렌즈·원리·few-shot·프로젝트 캡 2개,
  조립 프롬프트, 검색, 무덤, 회상, 축/orient, 재료 로딩 구조.

## 다시 제안하지 마라 (기각됨 — 이유 포함)

| 기각된 것 | 왜 |
|---|---|
| 검색어에 "구체적 이름·고유명사" 요구 | 유저: "고유명사 이딴 소리하지말고." idea 정의 + few-shot으로 충분했다 |
| 프롬프트에 규칙 계속 쌓기 | 3회 연속 규칙 추가 → 출력 4,077→5,053→6,475로 오르고 검색어가 막연해졌다. **막히면 빼는 방향으로 먼저** |
| 저장 시점 stance enum 인덱싱 | enum은 미리 예상한 속성만 잡고 새 속성마다 백필이 필요하다. 전수 스캔은 어떤 속성이든 잡는다 |
| 재료 고정 쿼터 (전량 읽기 교체) | 원리 C(북마크 × 프로젝트 겹치기)는 커버리지를 요구한다. "줄이 늘면 주의력이 나빠진다"는 추측이었다 |
| 우선순위 위계표 | active가 위인 게 맞다. 캡 2개는 강등이 아니라 신선도 장치고 이미 해결했다 |
| 프롬프트 캐싱 + 재료 정렬 뒤집기 | OpenAI 자동 캐싱은 수 분~1시간. 하루 1회 브리핑엔 안 걸린다 |
| resurface 90일 창 제거 | 코퍼스가 어려서 효과 0인 투기적 변경. 회상 엔진(`recall.ts`) 후보 로직 재사용이 맞다 |
| 프로젝트 승격 기능 | 이미 있다(파편을 프로젝트에 붙이고 active로). 캡 2개라 승격해도 지분이 안 는다 |
| 브리핑 때 "뭐 찾아줄까" 묻기 | 신호 발생 시점과 묻는 시점이 어긋나고, 원탭 의식에 통행세가 붙는다 |
| 큐 소비 상한 / 7일 만료 | 각도 상한이 이미 자연히 막는다 |
| 무덤을 자발적 연결에 열기 | 유저: "무덤은 그냥 둬도 될 것 같아." 유저가 명시적으로 찾을 때만 나온다 |
| 아직 안 일어난 문제에 미리 장치 만들기 | 전부 "요청 안 했다"로 기각. **유저가 명시적으로 겪은 증상만 고쳐라** |
| 각도에 **갈래**(물건/소프트웨어/공간/문화/사람/일하는방식) 강제 배정 | 내가 만든 목록이었다. 유저: "이 갈래는 좀 쓰지마." 반복도 못 막았다(코리빙이 갈래를 바꿔가며 6회 중 5회) |
| **금지어 선언**시켜 소재 끊기 | 단어는 지키고 영역은 그대로 갔다. `사는 곳` 금지 → `"creative residency"`. **단어를 막지 영역을 못 막는다** |
| 동기에 **행동 동사 금지** | 성공 개수는 그대로(6/10)인데 **전시·공간 갈래가 3→0으로 죽었다.** 상세는 RUDY-DISCOVERY-ARCHIVE §7-f |
| idea 재료를 글감·인용구로 제한 | 유저: "프로덕트 아이디어로 나오는 건 좋은 거지. 무조건 소재를 끊는 데 집중해야." 재료를 좁히는 건 문제를 피하는 것 |
| 던진 발견 카드를 재료에서 출처로 걸러내기 | 유저: "관심 있어서 던진 거고 새 아이디어일 수도 있는데." **던지기 자체가 가장 강한 관심 신호**라 빼면 신호를 버리는 것. 반복은 `dedupeAngles` + `<이미 다룬 주제>` 힌트가 담당 |
| idea 게이트를 임베딩으로 (2026-07-26) | 실측에 신호가 없었다 — 검색어는 영어·소재는 한국어라 sim이 0.16~0.34에 깔리고 **품질 나쁜 게 좋은 것보다 낮게** 나왔다(순서 역전). **판정이 필요하면 판정을 시켜라, 부산물로 판정하지 마라** |

## 재발 방지 교훈 (전부 실제로 터진 것)

- **증상을 들으면 설명하기 전에 DB부터 연다.** 2026-07-26에 "5개만 나온 건 dedupe 때문"이라고
  확인 없이 말했다가 틀렸다(실제로는 조립이 잘림). 3초면 확인할 수 있었다.
- **진단 스크립트가 프로덕션 프롬프트를 import하지 않으면 다른 물건을 재는 것이다.**
  `check-idea-angles.mjs`는 `ANGLE_SYS` 대신 자체 프롬프트를 갖고 있었고, 그걸로 얻은 결론이
  문서에 확정처럼 박혔다. **새 진단 스크립트는 "실물과 같은 프롬프트를 쓰는가"부터 확인해라.**
- **실행 결과는 stdout에만 남으면 사라진다.** 진단 스크립트는 출력을 파일로도 써라.
- **재료를 의심해라 — 모델이 자기 출력을 읽고 있을 수 있다.** 브리핑 문장을 유저가 저장하면
  다음 브리핑의 재료가 된다.
- **"막는 장치"가 공급원이 될 수 있다.** `<이미 다룬 주제>`는 반복을 막으려고 넘기는 목록인데
  모델이 그걸 출발 재료로 썼다. 넘기는 모든 블록에 "이게 재료로 읽힐 수 있나"를 물어라.
- **`create or replace function`은 인자 목록이 다르면 교체가 아니라 오버로드 추가다.**
  `rudy.collision_candidates`에 `min_age_days`를 붙였을 때 옛 2인자 판이 DB에 남아,
  앱이 `seed_ids` 하나만 넘기는 순간 PostgREST가 *"Could not choose the best candidate function"*으로
  거절했다 — **충돌 회상이 조용히 죽고 매번 랜덤으로 폴백하고 있었다**(2026-08-09 실측, 지금은
  옛 판 drop 완료). 인자를 바꿀 땐 `drop function if exists`를 먼저 넣어라. 그리고 **폴백이
  있는 경로는 실패해도 티가 안 난다** — 조용한 폴백엔 로그를 남기고 가끔 실제로 확인해라.
- **스트리밍에서 "사라짐"은 대개 셋 중 하나** — 버퍼 flush 누락 / 낙관적 화면 정리 / ScrollView `flex:1`.
- **PostgREST 다중 행 insert는 모든 행에 같은 키를 채워라.** 빠진 키를 default가 아니라 null로 채운다.
- **프롬프트 예시에 구체값(날짜·제목·수치)을 쓰면 모델이 복사한다.** 이 프로젝트에서 두 번 터졌다.
- **유사도 검색은 시간·개수·존재 여부를 못 답한다.** "있냐/없냐", "몇 개", "언제"는 다른 질의다.
- **시간대 버그는 한 곳만 고치면 반쪽이 남는다.** 경계(자르기)와 표시(찍기)는 다른 코드다.
- **계산된 값으로 현상을 판정하기 전에 계산기를 의심해라.** `cost_usd`가 안 떨어지는 걸 보고
  "캐싱이 안 걸린다"고 적었는데 단가표에 캐시 입력가가 없었을 뿐이었다. 원본 관측치가 옆 컬럼에 있었다.
- **외부 API는 이름이 약속하는 것과 실제 동작이 다르다.** 붙이기 전에 실제 데이터로 때려봐라.
- **약한 지시로는 구조적 쏠림을 못 이긴다 — 숫자 캡은 코드로.** 단, 캡을 코드로 옮기라는
  뜻이지 프롬프트에 문장을 쌓으라는 뜻이 **아니다.**
- **프롬프트로 모델의 마크업 습관을 막으려 하지 마라 — 파서가 견뎌야 한다.**
- **실패가 뜨면 코드가 아니라 판정(테스트·스크립트)이 틀렸을 가능성부터 확인할 것.**
- **묶기(클러스터링)를 새로 만들기 전에 `_shared/cluster.ts` 주석부터 읽어라.** 2026-08-12에
  "무리" 기능을 UMAP+DBSCAN으로 만들었다가 유저가 "이게 왜 여기 들어가?"로 잡아냈다 —
  DBSCAN은 밀도로 이어붙이는 체이닝 알고리즘이라 무관한 둘이 다리 하나로 붙는다. 그런데
  `cluster.ts`엔 이미 **같은 이유로 단일연결을 버리고 평균연결을 골랐다는 주석이 있었다**
  (§4-B1). 그걸 안 읽고 새 알고리즘을 들고 왔다. 평균연결로 바꾸자 최저쌍 유사도가
  0.081(거의 무작위) → 0.292로 올랐다. **이 저장소에서 "비슷한 것끼리 묶기"가 다시 나오면
  cluster.ts를 재사용해라, 새로 짜지 마라.**
- **OpenAI 계정 잔액이 마이너스로 떨어질 수 있다(선불인데도) — 사후 정산이라 0을 감지하기
  전에 이미 나간 호출이 넘어간다.** 2026-08-12에 -$8.41까지 내려갔고, 그동안 `embed` 웹훅이
  429로 조용히 죽어서 새 파편 7개가 임베딩 없이 쌓였다(에러 로그를 보는 사람이 없어서
  발견이 늦었다 — 위 "조용한 폴백" 교훈과 같은 병). 빠르게 확인하려면
  `curl https://api.openai.com/v1/embeddings -H "Authorization: Bearer $OPEN_AI_API_KEY" ...`로
  200/429를 직접 본다. 충전 후 밀린 것 복구는 `node scripts/backfill-embeddings.mjs`.

## 진행 상태 (RUDY.md §10)

| § | 내용 | 상태 |
|---|---|---|
| 0 | 임베딩 파이프라인 · 링크 임베딩 품질 | ✅ |
| 1 | 링크 다이제스트 | ⏸ 보류 (표면 없음·검색은 이미 해결) |
| 2 | 하이브리드 의미검색 + 검색 UI | ✅ |
| 3 | 충돌 회상 + "왜 지금" | ✅ (임계 0.42) |
| 4 | 원장 + 게이트 로그 + 쿨다운 | ✅ |
| 5 | 채팅 RAG + 원탭 | ✅ (자발적 연결은 2026-08-01에 **껐다**) |
| 6 | 클러스터 엔진 → 패턴·경향 | ✅ 채팅에서 사용. **아침은 안 쓴다**(모델이 직접 묶는다) |
| 6+ | 늦은 의도 (F1) + `rudy.evidence` | ⚠️ **껐다.** evidence는 아침 질문이 채운다 |
| 7 | 발견 브리핑 | ✅ 클코(`discover-websearch`)로 이관, 앱 버튼 제거 |
| 8 | 아침 브리핑 (F4) + 넛지 (A3) + D1 결합 | ✅ 클코(`scripts/morning/`), D1 결합 2026-08-03 |
| 9 | 주간 편지 (F5) | ❌ **안 만들기로 함** (위 「다음 할 일이 아닌 것」) |

**미착수:** 월말 성적표(§6-6) · 캘린더/지오펜스 어댑터(A2·D2가 이것 없이는 성립 안 함) ·
OpenAI Costs API 대조 스크립트(추정치 검증용, Admin 키 필요).

### ⚠️ 발견의 위상 — 유저 온도 (2026-07-28, 아직 유효)

유저: **"사실 발견에 크게 의미를 두고 있지 않아. 너무 비싸기도 하고. 무슨 의미인가 싶기도 하고."**

RUDY.md는 발견을 1급으로 놓지만 **유저의 실제 온도는 그렇지 않다.** 발견에 큰 작업을 새로
제안하기 전에 이걸 먼저 떠올려라. (클코 이관으로 API 비용은 0이 됐지만 온도는 별개다.)

### 나중으로 (유저 결정 2026-07-29 — 지금 꺼내지 마라)

| 미룬 것 | 이유 |
|---|---|
| **Life Dashboard** | 아침 브리핑이 이미 상태 그래프를 그린다 — **뭘 더 하는 건지부터 유저에게 물어라.** ⚠️ F5가 "대시보드가 아니라 서사. 숫자·통계·달성률 금지"를 명시한다. 걸리는 건 숫자가 아니라 **점수**(연속일수·달성률·전주대비)다 |
| **옵시디언 맥락** | 큰 작업. 방향은 잡혔다 — **RAG를 만들지 마라.** 클코가 로컬에서 도니 `Read`/`grep`으로 볼트를 인덱싱 없이 뒤질 수 있다. 노이즈 우려는 실재(daily note에 회의록·할일이 섞임) |
| **루디에게 남길 노트 / 사전** | 유저 결정: 지금 큰 도움 안 됨. ⚠️ AI가 "이걸 만들면 발견이 나아진다"고 말했는데 **근거가 없었다.** 발견 헛발질 4건은 전부 **프로젝트 description 미독**이 원인이었고 이미 고쳤다. 다시 제안하려면 실측부터 |
| **먼저 거는 말 되살리기** | 유저: "일단 보류하자" |

### 투두 — 앱 안에서, 완전히 분리 (**위 1순위**)

Things 연동 안 한다. **앱 안에서 하되 파편과 완전히 분리한다.** 파편 → 투두 변환은 가능.
근거: 유저 파편(07-29) *"띵스 안쓸바에야 그냥 이 안에서 투두관리하지"*.

**왜 분리가 필수인가:** Mind의 법칙은 **감쇠**(안 만지면 흐려지고 무덤)인데 todo의 법칙은
**정반대**(완료될 때까지 안 흐려져야 하고 오히려 시끄러워져야 한다). 한 통에 넣으면 하나가
반드시 깨지고, **지금은 todo가 깨져 있다** — 유저가 Mind를 이슈 트래커로 써서 07-16 저장분
43개(개발 버그 메모)가 전부 무덤으로 갔다. 그게 채팅이 멍청했던 원인의 절반이었다.

**보류 논의:** 채팅 라우팅을 tool-calling으로 재설계할지. 지금은 싼 모델이 5개 필드로 먼저
분류하고 그 상자만 태우는데, 분류가 틀리면 본답변이 아무리 좋아도 이미 틀린 재료를 받는다.
유저가 "A(소규모 개선) 먼저, 실사용해보고 판단"으로 미뤘다.

### 채팅 확장 아이디어 (백로그 — 착수 아님, 순서는 논의 순서)

- **채팅이 저장소를 고치는 자리로.** "이거 셋 다 케이스랩으로 묶어줘"처럼 지시하면 D1 승인
  게이트를 거쳐 반영. **가장 값이 큰 건 프로젝트 description 채우기** — 발견·채팅 헛발질 4건이
  전부 description 미독/빈약이 원인이었다.
- **대화 로그를 코퍼스에 편입.** 지금 `messages`는 그 턴 맥락으로만 쓰인다. ⚠️ 되먹임(루디 발화가
  재료가 되는 것)을 07-28에 이미 겪었으니 착수 전에 격리 방법부터 설계할 것.
- **채팅에서 즉석 발견 1개.** 배치 8개짜리 의식은 유저 온도가 낮다. 채팅 `<바깥>` 검색에 각도
  생성 한 단계만 얹으면 항목 1개짜리 발견이 된다. 발견의 값이 배치가 아니라 타이밍에 있었다면 이쪽이 본체.
- **구매·여행지 결정 지원.** 비슷한 걸 전에도 저장했나 + 그 뒤 무덤됐나 + 바깥 정보 겹치기 =
  감쇠 이력이 있어야만 가능한 답. 투두 편입 이후 붙이면 자연스럽다. D1의 구체적 인스턴스다.
- **여행 리서치 (A2 확장).** 트리거만 캘린더고 골격은 위와 같다(인터뷰→검색→비교).
  구글 캘린더 어댑터(§3-1, §10-9) 있어야 성립.
- **남에게 줄 선물 (렌즈 뒤집기).** 지금 렌즈(§4-E3)는 전부 내 취향 기준. 그 사람이 걸린 파편만
  모아 그 사람 기준으로 렌즈 재계산 — 로직 재사용, 대상만 교체. ⚠️ 재료가 충분한지부터 실측.
- **막연한 할 일 쪼개기.** 투두 편입 이후. 프로젝트 description + 진행 이력이 이미 있는 재료다.

## 배포 · 스크립트

**서버가 없다** (SPEC §3) — SQL은 Supabase SQL Editor에 사람이 직접 붙여넣는 게 정상 배포 경로다.
AI는 git 명령을 직접 실행하지 않는다 (CLAUDE.md §5).

**매일 도는 것 (유저가 손으로 친다. 자동화 안 한다).** 커맨드 원본은 `Rudy_Command.md`:
```bash
npm run morning              # 아침 브리핑 생성 + 원장 저장
npm run morning -- --material   # 재료만 뽑기 (LLM 안 태움, 공짜)
npm run morning -- --no-save    # 실험용, 저장 안 함
npm run discover             # 발견 (= scripts/discover-websearch/run.mjs)
```
⚠️ npm 스크립트라 **플래그 앞에 `--`가 하나 더 붙는다.** 없으면 npm이 먹는다.

**Edge Function 배포** (남은 건 `chat`뿐 — 발견·아침은 클코로 갔다):
```bash
# CLI가 PATH에 없고 프로젝트가 로컬 링크 안 돼 있다 — 반드시 이 형태여야 한다
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions list --project-ref ibqyqpmwqujcxlnkyizf   # 버전 확인
```

**진단 스크립트** (전부 로컬 node, service role 필요, `.env`에서 키를 읽는다):
`check-angles` · `check-brief` · `check-idea-engines` · `check-chat` · `check-collision` ·
`check-clusters` · `check-ledger` · `check-intent` · `check-embeddings` · `check-search`.
⚠️ **프롬프트가 `scripts/_discovery-lib.mjs`·`check-brief.mjs`에 복제돼 있다** (Deno↔node 경계).
한쪽만 고치면 진단과 실물이 갈라진다.

## 비용

**다시 계산하지 마라 — `rudy.llm_usage`를 `request_id`로 묶으면 나온다.**
응답별 합계는 `messages`·`utterances`의 `cost_usd`에 남는다.
⚠️ **`embed` 웹훅(파편 저장마다 자동 임베딩)은 이 원장에 안 남는다** — `usage`를 기록 안 한다.
2026-08-12 확인: 원장 합계 $7.49인데 실제 계정 소진은 -$8.41. 차액이 임베딩 비용(무시할 수준,
전량 재계산해도 ~$0.01)이라기보단 **정산 시차**로 보인다 — 확정 아님, 다시 물을 일 있으면
OpenAI 대시보드 Usage 페이지가 원천이다.

- 클코로 옮긴 뒤 **발견·아침의 API 실청구는 0**이다(구독). API 환산치가 궁금하면 세션 로그
  (`~/.claude/projects/...jsonl`)를 까면 된다 — 아침 3턴 / 발견 16턴 수준.
- **발견이 아침보다 3배 비싼 뿌리는 재시도가 아니라 바깥에 나가느냐였다.** 검색 결과가 턴마다
  컨텍스트에 쌓여 캐시읽기가 수십만 토큰까지 자란다(아침은 검색이 없어 컨텍스트가 안 자람).
- **진짜 절감은 빈도다.** "한 번에 많이"의 이득은 "한 번만"에서 오지 "많이"에서 오지 않는다.
- ❌ **각도를 gpt-4o로 내리지 마라.** 실측에서 확실히 나빴다(리스티클 미끼만 냈다).
- **네이티브 web_search는 비싸다** (07-27 실측, 항목당 $0.14 vs 기존 $0.02). 상시 경로로는 못 쓴다.
