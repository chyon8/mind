# RUDY-STATUS.md — 지금 상태 (세션 핸드오프)

> **이 파일만 읽는다.** `RUDY-ARCHIVE.md`(지난 세션 기록)는 열지 마라 — 만료된 계획이 가득해서
> 읽으면 오히려 틀린 추론을 한다. 유저가 "그때 왜 그렇게 정했나"를 물을 때만 연다.
>
> 설계=RUDY.md · **발견/브리핑 기준=RUDY-DISCOVERY.md(작업 전 필독)** · 지금 상태=여기.
> 마지막 갱신: 2026-08-02 (아침 브리핑을 클코로 이관, 아래 "다음 세션 시작점"부터 읽는다).

## 다음 세션 시작점 (2026-08-02)

1. **아침 브리핑이 Edge Function에서 클코 스크립트로 옮겨갔다.** `node scripts/morning/run.mjs`.
   `supabase/functions/morning/`은 **지웠다** — 재료 코드를 두 벌 두지 않으려고(그 병은
   `_discovery-lib.mjs`가 이미 앓았다). 자세한 건 아래 "아침 브리핑 4차" 절.
2. **앱은 이제 발견도 아침도 안 만든다. 읽기만 한다** (유저 지시). `discovery.tsx`의
   `새로 발견하기`·`모닝 브리핑 만들기` 두 버튼과 `/morning`의 `만들기` 버튼을 뺐다.
   ⚠️ Edge `discovery` 함수와 `lib/rudy.ts streamBriefing`은 **안 지웠다** — 부르는 사람이
   없으면 안 돈다. 되살릴 때 배포부터 다시 하지 않으려고 남겼다.
3. **붙여넣을 SQL 하나:** `supabase/rudy-project-status.sql` (`projects.status_changed_at`).
   안 붙여도 안 죽는다(표시만 안 뜬다). 백필하지 마라 — 이유는 그 파일 주석에 있다.
4. **실기 검증 안 된 것:** 아침 화면의 새 카드 둘(패턴 / 오늘의 질문). 스크립트는
   `--no-save`로 왕복까지 실측했고(3턴·131초, 아래) 원장 저장 경로만 미검증이다.
5. `observe` Edge Function(폐기된 1차 시도)은 **유저가 직접 지우겠다고 했다 — 건드리지 마라.**
6. 그 외 밀려 있던 것: `discover-websearch` 실사용 관찰(유저 직접), 채팅 모델 gpt-4o 테스트.

---

## 아침 브리핑 4차 — 클코 이관 (2026-08-02)

3차(집계 전용 Edge Function)를 유저가 실사용하고 **틀렸다고 판정했다.** 원장·DB 대조로
원인이 정확히 잡혔고, 그 결과 재료 정책이 뒤집혔다.

### 3차가 왜 틀렸나 — 실측 (2026-08-01 브리핑 4문단 중 3문단이 사실 오류)

| 브리핑이 한 말 | 실제 | 원인 |
|---|---|---|
| "앞선 3주 평균보다 **네 배** 몰렸다" | 최근 7일 118개 / 그 앞 하루 26.8개 → **오히려 줄었다** | 생존편향 + 분모 |
| "최근엔 **link가 새로 섞였다**(이전 0)" | 이전 3주에 링크 44개 | 묻힌 걸 안 봄 |
| "채팅 개선이 **잦아드는 중**" | 7/29 하루에만 채팅 파편 4~5개 | 묻힌 걸 안 봄 |

**뿌리는 하나 — 모델이 파편을 0개 봤다.** 재료가 1,461토큰짜리 집계 블록이었다.
전체 386개 중 `stats.ts`가 본 건 살아있는 58개(15%)뿐이었고, 그마저 모델에는 요약만 갔다.

- **묻기는 "버림"이 아니라 "정리됨"이다.** 유저가 7/31에 직접 썼다 — *"아이디어를 넣어놓고
  한번 쭉 정리하면서 묻기하고있는데 이 워크플로우가 루디랑 맞나?"* 최근 건 정리 전이라
  살아 있고 옛것은 정리돼서 묻힌다. → **살아있는 것만 세면 무엇을 재든 "요즘이 폭발했다"가 나온다.**
- **분모도 틀렸다.** `priorPerWeek = prior / 3` 고정인데 코퍼스가 7/16 시작이라 이전 창 21일 중
  실데이터가 10일뿐이었다. 빈 11일이 평균을 반토막 냈다.
- **`.neq('status','done')` 때문에 Caselab이 이름조차 재료에 없었다.** 그래서 파편 0개를
  "완전히 멈췄다"로 오독했다 — 실제로는 **완료**다. 상태를 안 보면 침묵과 종료가 똑같이 0이다.
- **축 3개는 임계 문제가 아니었다.** 실측: 0.42에서 3축, 살아있는 58개 중 9개 커버(전체의 2.3%).
  셋 다 최소 크기 3에 겨우 걸린 것. 노드가 58개뿐인 게 원인이었다.

### 4차 구조

**한 번 주고 한 번 뽑고 끝난다.** 루프도 검색도 없다. `claude -p --allowedTools Read` 한 방.

| | 3차 (Edge) | 4차 (클코) |
|---|---|---|
| 재료 | 집계 1,461토큰 · 파편 0개 | **전량 386개 43KB** · 묻힌 것 `(묻음)` 표시 |
| 축 | 임베딩 클러스터(`morning_edges` + gpt-4o-mini 라벨) | **모델이 직접 묶는다** — `refs`로 번호를 내면 코드가 시간모양 계산 |
| 억지 방지 | 파편을 안 준다 | **패턴 1개 + 가설 화법 + 근거 `refs` 표시** |
| 만드는 곳 | 앱 버튼 → Edge | `node scripts/morning/run.mjs` (유저가 손으로) |
| 비용 | $0.02/회 | $0 (구독) |

**옮긴 이유는 비용이 아니다** — API로도 전량 25K토큰 = $0.15/회, 월 $4.5였다.
**재료를 다 읽을 수 있어서**다. 그리고 앱에 "만들기 버튼"이 있는 것 자체가 이상했다 —
아침은 열었을 때 이미 있어야 하는 물건이다.

**실측 1회 (`--no-save`, 2026-08-01 재료):** 3턴 · 131초 · API환산 $0.513(구독이라 실청구 0).
결 7개, 패턴 근거 5개, 질문 1개. 3차가 못 본 것을 잡았다 — Steady_focus를 만든 당일 done으로
둔 것, 7월 중순에 묻은 TouchGrass·acti가 오늘 다시 올라온 것, `rejected`에
*"Caselab·글감·포폴용에 최근 파편이 없는 것 — done과 paused라 침묵이 아니다"*.

### 새로 생긴 것

- **패턴 카드** — 하루 하나. 다섯 종류(반복되는 질문 / 지속되는 가치관 / 충돌하는 관심사 /
  사고방식 변화 / 호기심 변화) 중 하나. **근거 파편을 같이 그린다** — 두 개로 본 건지
  여덟 개로 본 건지 안 보이면 억지인지 알 수가 없다. `refs`가 그 장치다.
- **오늘의 질문** — 답하면 `rudy.evidence`에 자기 진술로 쌓인다. 그 테이블이 지금 **1행뿐**이라
  (늦은 의도를 8/1에 껐으니 안 쌓인다) `findAxes`의 `stated` 배관이 물 없이 돌고 있었다.
  넘기면 아무 일도 안 일어난다.
  ⚠️ **넛지와 질문이 같은 날 둘 다 뜬다.** "하루에 질문 하나"로 갈라야 한다고 제안했지만
  유저가 결정한 적 없어 둘 다 뒀다. 아침에 숙제가 둘로 느껴지면 여기부터 본다.
- **`projects.status_changed_at`** — 없어서 "이번 주에 끝냈다"를 원리상 못 봤다.
  `updateProject()`가 status 패치 때 같이 찍는다.

### 죽은 것 (되살리려면 이유부터 읽어라)

- `supabase/functions/morning/` · `supabase/rudy-morning.sql` — 삭제. `findAxes`의 rpc 인자도 되돌렸다.
- **DB의 `rudy.morning_edges` 함수는 남아 있다** (`collision_by_embedding`과 같은 처리).
  아무도 안 부른다. 임베딩 축으로 돌아갈 일이 생기면 SQL부터 다시 붙일 필요는 없다.
- `MorningSkeleton` — 앱이 안 만드니 기다릴 게 없다.

---

## 아침 브리핑 3차 (2026-08-01 밤) — 위 4차로 대체됨. **실패 이유만 살아 있다**

RUDY.md §4-F4(아침 브리핑)·§4-A3(넛지)를 **처음으로 실제 구현**했다. 세 번 시도했고 처음 둘은
유저가 명시적으로 거부했다 — 실패 이유를 남기는 게 다음 세션이 같은 삽질을 피하는 유일한 방법이다.

### 실패 1 — 요약형 (즉흥 기준, RUDY-DISCOVERY.md를 안 읽고 짬)
"세 가지를 각각 한두 문장으로 관찰해라"는 프롬프트. 유저 판정: **"뻔한 얘기, 파편 나열, 가독성 0"**.
원인은 모델이 아니라 **필독 문서 두 개(RUDY-DISCOVERY.md·Design.md)를 안 읽고 즉흥으로 짠 것.**
CLAUDE.md가 "발견/브리핑 작업 전 반드시 읽는다"고 못박아둔 걸 건너뛰었다. **전체 되돌림**(파일 삭제,
`git status` 클린 확인).

### 실패 2 — 주장형 (RUDY-DISCOVERY §6 형식을 그대로 옮김, 파편 인용)
문서를 읽고 "제목=주장, 파편을 겹쳐 근거로 삼아라"(§6 항목 모양)를 그대로 썼다. 유저 판정:
**"제일 최악"** — *"파편을 뽑아서 이건 이래 이렇게 설명하지마. 너는 나를 좆도 모르잖아.
이렇게 줄줄이 억지로 관련짓지마."* **RUDY-DISCOVERY §6은 발견(바깥 정보) 형식이지 아침
관찰(안쪽 요약) 형식이 아니다** — 같은 문서라도 용도가 다르면 형식을 그대로 못 옮긴다는 교훈.

### 실패 2.5 — 형태소 토큰화로 "키워드" 시도 (설계 중 자체 기각)
"반복되는 키워드"를 글자 그대로 받아 한국어 조사 제거 토크나이저를 손으로 짰다. 실측 상위가
`https·com·www·아니라·이렇게·있다` — 불용어를 채워도 나오는 건 **단어 카운트지 의미가 아니다**
("옵시디언 3/0"은 통찰이 아니다). 유저가 코드를 보기도 전에 "형태소 분석을... 괜찮아?"로 제지.
**교훈: 의미가 필요하면 의미 있는 도구(임베딩 클러스터)를 써라. 텍스트 프로세싱으로 흉내내지 마라.**

### 확정된 구조 (3차)

**역할 분담:** 코드가 집계한다(`morning/stats.ts`) → UI가 그림으로 그린다(`MorningViz.tsx`) →
모델은 집계를 해석만 한다(`morning/prompt.ts`). **모델에 파편 개별 내용을 안 준다** — 프롬프트로
"엮지 마라"는 안 지켜지므로 구조로 막았다(RUDY-DISCOVERY §7-f의 2단계 분리와 같은 수법).

**관심사 변화는 임베딩 클러스터로 잰다.** `findAxes`(채팅과 공유)가 이미 의미로 파편을 묶고
이름 붙인다 — 거기에 "이 결의 증거가 최근 7일에 몇 개 / 그 앞 21일에 몇 개"만 얹었다.
`새로 생김 / 굵어지는 중 / 잦아드는 중 / 그대로` 네 상태로 코드가 판정하고, UI가 좌우 막대로
그리고, 모델은 그 이름·분포만 갖고 문장을 쓴다. 실측 예: "음성 변경 플러그인: 최근3/이전0(새로 생김)".

**출력은 제목 없는 문단 2~4개** (JSON `{headline, reading[], rejected[]}`). 마크다운 링크·헤딩 금지 —
링크를 걸 파편 id 자체를 안 줬으니 걸 수가 없다(구조적 방지, 프롬프트 규칙 아님).

**축 재료가 채팅과 다르다.** `cluster_edges`(채팅용)는 프로젝트 소속 파편을 뺀다 — "이미 조직된 건
암묵 클러스터 재료가 아니다"(발견 규칙, `rudy-cluster.sql:12-17`). 아침 관찰은 정반대로 **"내가
요즘 어떤가"라 프로젝트 파편이 핵심**이다. 실측(2026-08-01, 살아있는 74개): 제외 있으면 노드 4개
→ **축 0개**(화면이 통째로 빔). 제외를 뺀 형제 RPC `morning_edges`(`rudy-morning.sql`, 신규,
**SQL Editor 붙여넣기 필요**)로 같은 임계 0.42에서 **축 3개** 확인. `findAxes`에 rpc 이름 인자
추가(`chat/clusters.ts`) — 기본값이 `cluster_edges`라 채팅 동작은 안 바뀜.

**넛지(A3) 설계 변경.** 원래 계획(RUDY-STATUS 이전 기록: 떠오르기에 슬롯 1칸 예약)을 실측으로
기각했다 — touch_count 분포가 63:4라 "최저 가중치라 5배 덜 뽑힌다"는 전제가 이 코퍼스에서 거짓,
떠오르기 랜덤 풀(36개)과 넛지 후보(26개)가 **72% 겹쳐서** 이름만 바꿔 같은 걸 두 번 보여주는 꼴이었다.
대신 **아침 브리핑 안의 카드 하나**로 넣었다 — 빈도가 하루 1회로 구조적으로 제한되고, "3주째"
하드코딩 대신 실제 나이를 코드가 계산(`${days}일째 한 번도 안 건드렸어. 버릴까, 진짜 할까?`).
질문 문장은 **모델이 안 쓴다** — 서버 템플릿. "반드시 질문형"·"죄책감 화법 금지"가 코드로 보장됨.

**흘려보내기 지속 버그(2차 판정 후 유저 신고) — 원인·수정.** 브리핑 본문이 원장에 JSON으로
굳어 있어 넛지 응답 후에도 그 JSON은 안 바뀌었다 → 다시 열면 흘려보낸 넛지가 그대로 돌아옴.
판정 원천을 굳은 JSON이 아니라 **넛지 발화 행의 `user_response`**로 바꿈(`morning.ts` `answered()`).

**비용 — $0.128 → $0.026 (-80%, 2차→3차).** 2차는 파편 인용하느라 출력이 길었다(gpt-5.5 출력
$30/1M이 비용의 70%). 파편 목록을 안 주니 입력 6,440→1,462, 출력 2,488→607로 같이 떨어짐.

**F5 개정 반영됨 (RUDY.md).** "숫자·통계·달성률 금지" → 상태 표시(그래프)는 허용, **점수는 여전히
금지**(달성률·연속일수·전주대비·순위) — "숫자를 보여주지 마라"와 "점수를 매기지 마라"는 다른 규칙이라
후자만 남겼다. 이 경계를 지울지는 유저에게 물어본 적 없다 — 필요하면 다시 확인할 것.

**지난 브리핑 4행(구 형식) 삭제 완료.** 발견(`kind=discovery`) 35행은 안 건드림.

### 살아남은 것 / 죽은 것
화면(`MorningViz`·`MorningCard`·`morning.tsx`)과 원장 형식(`kind='pattern'` JSON 한 행)은
4차가 그대로 물려받았다. 죽은 건 **재료 만드는 쪽**뿐이다 — 위 4차 절 「죽은 것」 참고.

---

## 지금 여기 (2026-07-28)

**07-27 세션의 두 문제는 둘 다 코드에 들어가 main에 있다.** 브랜치 `rudy-discovery-fix`는
병합됐다(`68b36f5`). "미구현"이라고 믿고 다시 만들지 마라 — `discovery/idea.ts`가 2단계
분리 구조 그 자체다.

### 문제 ① 조립이 문장 중간에 잘린다 — 배포됨, 표본 1건으로 해소 신호

원인: gpt-5.5 출력 토큰이 함수 실행 시간 한도를 넘겨 죽었다(각도 out 5,111=완주 / 6,539=사망).
수정: 각도 상한 12→8, `why는 한 줄`, 원장 행을 조립 *전에* 만들어 4초마다 갱신, 중간 저장에
`cost_usd`도 기록.

**실측(07-27 13:47~48, `rudy.llm_usage` 완전 실행 1건):**

| call_site | 입력 | 출력 | $ |
|---|---|---|---|
| `discovery.motive` | 6,251 | 731 | 0.053 |
| `discovery.idea` | 564 | 799 | 0.027 |
| `discovery.angles` | 11,793 | 2,410 | 0.131 |
| `discovery.assemble` | 18,003 | 2,947 | 0.178 |

**각도 출력이 5,111~6,539 → 2,410으로 떨어졌다** = 죽던 구간에서 멀어졌다. 회당 총 **$0.389**
(2단계 분리 $0.08이 얹혀 예전 평균 $0.34보다 높다). 표본은 이 1건뿐이다.

### 문제 ② `[아이디어]`가 사실상 확장이다 — 2단계 분리로 배포됨

**→ 상세·근거는 `RUDY-DISCOVERY.md` §7-f. 여기엔 결론만.**

1단계에서 파편→**동기**만 뽑고, 2단계엔 **파편을 안 주고 동기만 준다.** 소재를 안 보여주니
따라갈 수가 없다 — 프롬프트 규칙이 아니라 구조다. 하네스 실측 6/10 성공·소재 누출 0.
`scripts/check-idea-2step.mjs`가 그 하네스다(지우지 마라).

### 07-28 브리핑(22:48 KST) — 유저: "진짜 쓰레기였다, 가치가 없다고 느꼈다"

원장 + `llm_usage` + `gate_log` 대조로 원인 확인:
- **되먹임 재현.** 07-27 [아이디어] "Mindsight 락박스"를 보고 유저가 07-28에 "Times lock box"를
  직접 저장 → 그날 저녁 재료가 돼 브랜드만 바뀐 같은 물건으로 다시 나왔다(§7-f의 되먹임이
  하루 만에 실측). **이건 되먹임 사고가 아니다** — 유저가 발견을 받아 적은 정상 동작이고,
  다음 날 그걸 그대로 되돌려준 게 고장이다.
- **저장한 링크 자기 사이트 재출고.** Pushary/TouchGrass를 Product Hunt에서 저장 →
  각도가 그 이름으로 검색 → pushary.com 3페이지가 "발견"으로 나왔다. `<이미 저장>`은
  URL로만 비교해서 도메인이 다르면(저장 URL=producthunt.com, 결과 URL=pushary.com) 안 걸렸다.
- **중복 게이트가 한국어 제목만 비교해 영어 반복을 못 잡았다.** 어제 항목의 영어 제품명이
  본문엔 있는데 게이트 비교 대상엔 없었다. 언어 차이가 의미 차이를 덮었다 — idea 게이트
  (임베딩)가 죽은 것과 같은 원인.
- **조립이 각도 하나를 항목 둘로 쪼갰다.** 게이트 로그가 `expansion 4`인데 [확장]이 5개
  나왔다. 링크 1개 규칙(`36949c7`, 07-28 21:15 커밋)이 그날 배포가 안 돼 있었다 — 배포됐으면
  이 압력이 억제했을 것.
- 출처 링크 3개 문제도 같은 미배포 때문에 재발.

### 2026-07-29 수정 — discovery v21 배포, 실측 완료

1. **저장한 제품 자기 사이트를 코드로 컷** (`isOwnSite`, `brief.ts`). 도메인이 아니라
   **이름**으로 맞춘다 — Product Hunt 링크 하나 때문에 Product Hunt 소스 결 전체를
   막으면 안 되므로. 07-28 실데이터(저장 링크 47건)로 실측: pushary.com·touchgrass.land만
   정확히 컷, 나머지 통과.
2. **중복 게이트 비교 대상에 영어 링크 제목 추가** (`matchTopics`, 프롬프트 힌트`topics`는
   그대로 둬 입력 비용 안 늚). 락박스 재구성 각도로 재보니 신호는 올랐다(0.374→0.535)
   **but 임계 0.6을 못 넘었다** — 이것만으론 부족.
3. **`REPEAT_SIM` 0.6 → 0.45.** 감이 아니라 실측 — 07-27 항목 6개 중 진짜 반복이 0.535,
   나머지 5개는 전부 0.32 이하였다(2번 적용 후 기준). 그 사이가 비어 있어 0.45로 놓았다.
   ⚠️ 그 각도는 저장이 안 돼 있어 **재구성**이었다(잡음 바닥만 실측). 되돌릴 조건: 멀쩡한
   각도가 잘리기 시작하면(`gate_log.dropped`로 보임) 0.5로 올린다.
4. `ANGLE_SYS`에 "저장한 링크 그 자체를 목적지로 삼지 마라" 추가 (`angles.ts` +
   진단 복제본 `scripts/_discovery-lib.mjs` 동기화).
5. `gate_log.detail`에 **`kept_angles`**(살아남은 각도의 slot/query/from/why) 추가.
   전엔 컷된 것만 남고 통과한 건 안 남아서 "이 항목이 어느 각도에서 나왔나"가 항상 추측이었다.

**배포 직후 실측(07-29 08:13 KST, `threshold=0.45` 로그로 새 코드 확인됨):**
- 저장한 제품 자기 사이트 0개, 출처 링크 전부 1개 — 두 버그 해소.
- [아이디어]가 실측으로 소재를 갈아탔다: `mpk mini 기능들` → 영상편집 다이얼 콘솔(완전히
  다른 물건). `아름다움은 발견될 때 눈부시다` → 포틀랜드 보도블록 모자이크(§4가 원하던
  "아예 다른 것" 갈래, 프로덕트 아님).
- **그런데 반복은 여전히 나왔다** — 원인이 이번엔 정확히 잡힌다. 게이트가 7개 중 5개를
  반복으로 정확히 잡았는데(sim 0.462~0.637, 임계 0.45 이하로 잘 걸림), `MIN_KEEP=4` 안전망이
  "5개 자르면 2개만 남는다"고 판단해 **컷을 통째로 포기**하고 5개 전부 그대로 출고했다.
  락박스 계열이 3일 연속(Mindsight→Habit Control→Promise) 나온 이유, "마케팅 툴들" 파편
  하나가 항목 2개를 먹은 이유가 이거다. **임계 조정은 먹혔다 — 안전망이 죽였다.**
- 비용 $0.319 (0.390 → 0.351 → 0.319, 계속 하락 추세).

**미해결 — 다음이 이어받을 것:** 안전망이 전부-아니면-전무(all-or-nothing)라, 반복이 많을수록
오히려 덜 거른다. **고치는 방향은 확인됨:** "다 자르거나 다 살리거나"가 아니라 **"안 겹치는
순으로 최대 4개는 살린다"**(부분 컷)로 바꾸면 이번 데이터에서 락박스·마케팅중복이 빠지고
포틀랜드 항목(0.462, 아슬아슬하게 반복 판정됐던 것)은 순위로 살아남는다. 대가는 항목 수
7→4. **유저 판단: "일단 지금 상태로 그냥 두자."** 다음에 손댈 때 여기부터 보면 된다.

### 2026-07-29 밤 — 채팅 전량 개편 + 발견 제외 (배포·커밋 완료 `b900d6f`)

**증상:** "나한테 필요한 게 뭐야" 류에 앱 개발 버그 메모가 나왔다(`Year 표시가 없네`, `반응형`,
`모달끄면 저장안되는 문제`). 유저: "무덤에 있는 것도 가져오네."

**진단 (DB 실측):**
- 파편 331개 중 **무덤 238개 = 71.9%**. 무덤은 "나쁜 것"이 아니라 **"일주일 넘은 것"**이다
  (07-16 저장분 43개 = 43개 전부 무덤). **무덤을 빼면 채팅이 지난 5일치만 아는 앱이 된다.**
  → 무덤 필터는 처방이 아니다. 눈에 띈 건 결과지 원인이 아니었다.
- 진짜 원인: 주제어가 안 뽑히는 질문이 **질문 원문 검색**(코드가 스스로 "오염된 경로"라 경고하는
  길)로 떨어져 331개 중 10개를 틀린 벡터로 골랐다.

**고친 것:**
1. **기본 갈래를 유사도 10개 → 전량으로 뒤집었다.** 331개 = 24,567토큰이라 통째로 들어간다.
   검색은 없앤 게 아니라 **역할이 바뀌었다** — 상위 10개의 상세(URL·덧·프로젝트) + 근거 칩.
2. 파편 줄에 **`·무덤` 표시**. 빼는 게 아니라 표시하는 게 처방이다.
3. 전량 블록을 **`system` 바로 뒤**에 뒀다 (캐싱은 앞에서부터 걸린다).
   ⚠️ 기각 목록의 "캐싱 안 걸린다"는 **하루 1회 브리핑** 얘기다 — 채팅은 조건이 반대라 되살렸다.
4. 프로젝트 `description`을 근거에 실었다.
5. 상한 300 → **600**. 실제 파편이 331개라 **오래된 31개가 이미 조용히 잘리고 있었다.**
6. `prompt.ts`에 `## 저장소` 규율 추가.

**모델은 안 올렸다 (`gpt-4o` 그대로).** 재료가 틀리면 모델은 소용없고, 재료가 맞으면 gpt-4o로도
될 수 있다. 나중에 올리면 **차이가 순수하게 모델 몫**으로 읽힌다.

**배포 직후 실측 (3턴):** $0.0788 / $0.0847 / $0.0767 (전 $0.015). description 실린 것 확인됨
("Nophone 맥락 설명" → 실제 설명대로 답함). 개발 버그 메모 안 나옴.
⚠️ ~~**캐시 효과가 안 보인다** — 2번째 턴부터 떨어져야 하는데 3턴 다 같다. 미확인.~~
→ **07-31 해소. 캐시는 걸리고 있었고 단가표에 캐시 입력가가 없어서 안 보였다** (아래 「다음 할 일」).

**발견 제외 (`discover_skip`)** — 프로젝트·파편 두 층. `discover_next`의 대칭이되 **비대칭이 의도**다:
포함은 1회성 지시(브리핑 돌면 꺼짐), 제외는 상시 경계(안 꺼짐). 제외된 프로젝트의 파편은
**통째로 빠진다**(미소속으로도 안 샘). **발견에만 건다** — 채팅·검색은 그대로 다 본다.
배포 확인: 유저가 이미 프로젝트 3개에 켜뒀다.

### 앱 쪽 두 판 — 2026-08-01 **코딩 끝, 실기 검증 대기**

> 기획은 07-31, 코딩은 08-01에 했다. `tsc`·`jest`(46)·`expo lint` 통과했을 뿐 **아무도 실기에서
> 안 돌려봤다** — 유저가 테스트 가능한 환경이 아니어서 두 판을 연속으로 짰다.
> 아래 설계 표는 그대로 두었다(왜 그렇게 짰는지의 근거). 지금 상태만 여기에 적는다.
>
> **배포 안 된 것 (이것부터):**
> 1. **SQL 3개를 SQL Editor에 붙여넣어야 한다** — `rudy-similar.sql`(신규, "이거 관련 뭐 있었지"가
>    이거 없으면 항상 빈 목록) · `rudy-collision.sql`·`rudy-chat.sql`(쿨다운 60→7 반영).
> 2. **chat 함수 재배포** — `npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf`
>    (`pinnedId`·`mode=more_like` 받는 자리가 서버에 있다).
>
> **판 A에서 바뀐 것:** `Saved.day`(문자열) → `at`(타임스탬프)+TTL 4시간, `seen` FIFO 24개 신설,
> `todayRecall`→`currentRecall` 개명("하루"를 약속하는 이름이라), `readSaved`/`isFresh`로
> "지금 판인가" 판정 단일화(전엔 `recallUtteranceId`·`recallSeed`가 각자 `dayKey`로 판단),
> `RecallSection`에 `AppState` active 복귀 `load()`. 쿨다운 60→7은 4곳 전부.
> **SPEC 세 줄도 고쳤다** — `:126` 자리엔 새로 생긴 seen 규칙 한 줄이 들어갔다.
>
> **판 B에서 바뀐 것:** 칩이 `이거 관련 뭐 있었지`(인라인, 채팅 안 감) / `채팅하기`(`?fid=`,
> 자동 전송 없음, 입력창 위 "물고 있음 — …/놓기") / `more like this`(`?mode=more_like&q=`)로
> 갈렸고, **`이거 다음 뭐 볼까` 칩은 없앴다**(more like this가 그 자리다).
> 서버는 `mode==='more_like'`면 `EXTRACT_SYS` 대신 `MORE_LIKE_SYS`(종류로 뽑기)를 쓰고
> `outward`를 `go`로 고정한다. **종류를 못 뽑으면 바깥에 안 간다** — 질문 원문 폴백을 막았다
> (원문에 제품명이 들어 있어 그대로 Exa로 가면 findSimilar 실패가 뒷문으로 재현된다).
> `pinnedId`는 `<물고있는파편>` 블록 하나만 더한다(갈래 판정 무수정). `prompt.ts`에 그 절 추가.
>
> **미검증·판단 남은 것:**
> - `similar_fragments`에 **유사도 임계를 안 걸었다.** 검색은 상위 N을 주는 게 일이라는 판단인데,
>   무관한 게 섞여 나오면 그때 임계를 넣는다 (충돌의 0.42는 "루디가 먼저 말할 자격"이라 다른 값이다).
> - `more like this`가 실제로 물건 대신 **종류**를 물어오는지는 돌려봐야 안다. 이게 이 판의 전부다.
> - 판 A는 풀(≈50개)에서 하루 6판×4개=24개를 뽑는다. seen 24개면 하루 안에서 안 겹치는 대신
>   **후보가 마르는 날엔 seen을 통째로 무시하고 채운다** — 겹쳐 보이기 시작하면 그게 이 신호다.

#### 판 B 실사용 첫 반응 — 2026-08-01, 채팅 채용 후 바로 두 개 껐다

`채팅하기`(fid 물기)는 붙자마자 잘 됐다("돼" 확인). 그 대화 안에서 기존 기능 둘이 실제로
망가진 게 보였다 — **로그(`rudy.messages`)로 원문을 직접 읽고** 확인, 문서 서사로 추론 안 함.

- **자발적 연결(§4-C1, `<연결>`) 껐다.** 실측: 임계(0.34) 통과한 연결을 모델이 붙이면서
  `[『』](mind://fragment/…)`처럼 **링크 글자를 비웠고**, 본문에 "이게 지금과 무슨 관련이
  있는지 모르겠어"라고 **스스로 썼다.** 임계를 넘겨도 의미로는 무관한 사례였다는 뜻 —
  튜닝으로 고칠 값이 아니라 기능 자체를 끄라는 신호로 판단. 유저: "관련있는 거 물어보지
  않으면 그냥 굳이 안 띄우는 게 좋겠다." `findLink`·`LINK_THRESHOLD`·`LINK_COOLDOWN_DAYS`·
  `<연결>` 프롬프트 절 삭제(`chat/index.ts`). `collision_by_embedding` RPC는 DB에 그대로 둠
  (되살릴 때 SQL부터 다시 안 붙이려고). **그 자리는 판 B의 `이거 관련 뭐 있었지` 칩이 받는다** —
  사람이 누를 때만, 회상 없이 안 뜨는 게 같은 정신이다.
- **늦은 의도(§4-F1, `<물어볼것>`) 껐다.** `pickQuestion`/`logQuestion` 호출을 뺐다(`intent.ts`엔
  로직 그대로 있음, 되살리려면 다시 부르면 됨). `captureAnswer`(이미 물어놓은 질문에 대한 답
  캡처)는 남겼다 — 끄는 시점에 24시간 안에 답을 기다리는 질문이 있었을 수 있어서.
- 위 둘의 공통 프롬프트 처방 하나 더 넣음: `prompt.ts`에 "## 묻지 않은 것은 꺼내지 마라" 절
  (무관한 파편 끌어오기 금지 / 확인 질문 금지 / 이름 없는 링크 금지). 두 기능을 껐다고 이
  버릇이 다른 경로(예: 축·오늘 갈래)에서 재현 안 한다는 보장은 없어서 넣은 일반 규율이다.
- **떠오르기 카드 → 파편 상세로 연결.** 막아뒀던 이유("열면 touch되어 선명해진다")가
  2026-07-19에 이미 깨져 있었다(파편 상세는 그 뒤로 실질 편집에만 touch). 문서 서사가 아니라
  코드를 다시 읽고 확인한 뒤 풀었다. "왜 지금?"은 카드 탭 자리를 잃어서 액션 줄로 이동.

이 셋 다 **아직 실기 검증 안 됨** — chat 함수 재배포 후 확인할 것.

#### 판 A — 떠오르기 4시간 갱신 (자기완결, 먼저)

| 바꾸는 것 | 어디 | 값 |
|---|---|---|
| 갱신 주기 | `recall.ts` `Saved.day` → 타임스탬프 | **4시간** |
| 한 판 개수 | `RECALL_COUNT` | 4 (유지) |
| 흘려보내기 쿨다운 | **4곳 전부** (아래) | 60일 → **7일** |
| 최근 본 것 제외 | `Saved.seen` 신규, FIFO | 24개(≈6판) |

**쿨다운 60→7은 4곳이다. 단일 원천이 없어서 한 곳만 고치면 조용히 갈라진다:**
`src/lib/supabase.ts:334` `LET_GO_COOLDOWN_DAYS` · `src/lib/fixtures.ts:175` ·
`supabase/rudy-collision.sql:36` · `supabase/rudy-chat.sql:48` (뒤 둘은 **SQL 붙여넣기 필요**).

**SPEC.md 세 줄이 어긋난다 — 유저 승인 받음(2026-07-31), 같이 고친다:**
`:117` "60일간" → 7일 · `:126` "큐·개수·진행률 표시 금지 … 생산성 앱이 되고 죽는다" → **삭제**
(유저: "그 규칙은 이제 성립하지 않아") · `:115` "하루 2개" → 4시간마다 4개
(07-30에 2→4 올릴 때 이미 어긋나 있었다).

**깨지기 쉬운 지점 (전부 실제 경로다):**
- **기존 기기에 옛 `{day,...}` 형태가 남아 있다.** `at`이 없으면 `NaN` 비교로 새 판이 뽑히니
  사고는 안 나지만, `!saved.at → 새 판` / `saved.seen ?? []`로 명시 처리할 것.
- **`recallUtteranceId`·`recallSeed`가 `day !== dayKey(...)`로 "오늘 것인가"를 본다.**
  같이 TTL 기준으로 안 바꾸면 판이 4시간마다 갈리는데 이 둘만 하루 단위로 판단한다.
- **`seen` 제외 후 후보가 4개 미만이면 `seen`을 무시하고 채운다.** 안 그러면 풀(≈50개)이
  마르는 날 떠오르기가 통째로 사라진다.
- **앱을 켜둔 채 4시간이 지나면 안 바뀐다** — `RecallSection`은 `visible` 변화에만 `load()`한다.
  `AppState` active 복귀에서 `load()` 한 줄 추가(새 의존성 0). 이게 없으면 "4시간마다"가 거짓말이 된다.
- 충돌 픽은 원장 30일 쿨다운이 이미 막는다 — `seen`은 **랜덤 뽑기에만** 건다.

#### 판 B — 파편 상세 칩 3개 (판 A 다음)

셋의 역할이 겹치지 않게 갈랐다: **안쪽 / 자유 / 바깥.**

| 칩 | 하는 일 | 새로 만드는 것 | 비용 |
|---|---|---|---|
| **이거 관련 뭐 있었지** | 안쪽 유사 파편, 상세 안에 인라인 목록 | RPC `rudy.similar_fragments` 1개 | **$0** |
| **채팅하기** | 파편 물고 채팅 진입, **자동 전송 없음** | `/chat?fid=` 경로 + 물고 있다는 표시 | 대화한 만큼 |
| **more like this** | 바깥(Exa). 구 "이거 다음 뭐 볼까" 자리 | `searchQueries` 프롬프트 분기 | Exa $0.007 |

- **`similar_fragments`는 `collision_candidates`의 쌍둥이**인데 **회상 필터를 전부 뺀다**
  (`archived`·`let_go_at`·`tier<>'pinned'`·`last_touched_at`). 검색은 "찾으러 온 행위"라
  무덤도 뒤진다(`rudy-search.sql:3`). 한 함수에 두 규칙을 섞지 마라.
  ⚠️ `fetchFragmentsByIds`는 **순서를 보존하지 않는다** — 유사도 순 정렬을 클라이언트에서 다시 건다.
  ⚠️ 임베딩은 저장 후 비동기 생성이라 갓 저장한 파편은 0건이 정상. "아직 없다"를 표시할 것.
- **`fid`는 `q`와 별개 파라미터다.** 기존 원탭 진입(`q`=받자마자 자동 전송, `chat.tsx:200`)을
  건드리지 마라 — 파편 상세의 다른 칩이 그걸 쓰고 있다. `fid`는 전송 안 하고 물고만 있는다.
  서버엔 `pinnedId` optional로 넘겨 `<물고있는파편>` 블록 하나만 더한다. **갈래(intent) 판정은
  그대로 둔다** — 여기 손대면 채팅 전체가 흔들린다.
- **`more like this`의 진짜 함정:** Exa에 가는 검색어는 `outwardBlock(topics)`의 `topics`이고
  (`chat/index.ts:384`), `topics`는 `EXTRACT_SYS`가 **저장소 검색용 주제어**로 뽑은 소재어다.
  파편을 그냥 던지면 소재 이름이 그대로 Exa로 가서 **findSimilar가 실패한 그 결과**(같은 물건
  파는 다른 사이트)가 온다. **질문 문구만 바꾸면 안 막힌다 — 검색어 만드는 자리가 다르다.**
  → 해법: `searchQueries`에 모드 하나. `more_like`면 `EXTRACT_SYS` 대신 "이게 **어떤 종류의**
  것인지 규정하고 그 종류를 찾는 검색어를 내라, 소재의 이름·브랜드·URL은 검색어에 넣지 마라,
  `outward`는 `go`"를 쓴다. **호출 수 증가 0**(이미 도는 호출의 프롬프트만 교체).
  이건 `[아이디어]` 슬롯을 고친 2단계 분리(RUDY-DISCOVERY §7-f)와 같은 약이다.
- 자기 사이트 재출고 컷(`isOwnSite`, `brief.ts`)은 발견에만 있고 채팅엔 없다. **미리 옮기지 마라** —
  실제로 나오면 그때. 볼 지점으로만 안다.

**Exa vs GPT 비교는 판 B가 돈 뒤.** 검색어 만드는 프롬프트는 그대로 두고 엔진만 바꿔 잰다
(3파전 때 "재료·기준은 공유한다, 다르면 비교가 안 된다"와 같은 방식).

#### 나중 (이 두 판에 넣지 마라)

- **넛지(A3)** — 판 B의 칩들이 넛지가 필요로 하던 "그 다음 흐름"이라 순서가 이것이다.
  붙일 때는 `recall.ts`에 슬롯 1칸 예약(충돌 1칸과 같은 수법). ⚠️ 넛지 후보는 `touch_count=0`
  이라 `weight = 1 + touch_count*2`에서 **최저 가중치**라 그냥 풀에 섞으면 5배 덜 뽑힌다.
- **넛지 푸시** — 배관은 전부 살아 있고(`push.ts` 등록·`morning-briefing:32` 발송·탭 라우팅)
  **부르는 사람만 없다**(cron 폐기). 갈 때는 **로컬 알림**(`scheduleNotificationAsync`)이 맞다 —
  넛지 재료가 전부 로컬 계산이라 서버가 낄 이유가 없고, 서버 푸시로 가면 폐기한 cron이 되살아난다.
- **채팅 답변 → 파편으로 저장** — 필요하다고 합의됐으나 설계 미착수. 백로그 「채팅이 저장소를
  고치는 자리로」의 일부라 D1 승인 게이트와 같이 봐야 한다.
- ⚠️ **SPEC `:129-130`**(헤매기에서 판단 버튼을 뺀 이유 — "무한히 뽑을 수 있는 곳에 판단을
  붙이면 솎아내는 노동이 된다")은 **유저가 지우라고 한 `:126`과 다른 규칙이다. 살아 있다.**
  떠오르기가 하루 6판이 되면 이 압력이 옮겨붙는다. 흘려보내기가 노동처럼 느껴지면 여기부터 본다.

### 다음 할 일 (2026-07-31 정리 — 다음 세션 시작점)

> 아래 0/1번(클코 3파전·이관)은 이 세션에서 끝났다 — "3파전 결론" 섹션 참고, 1번 채택·운영
> 방식도 확정됐다. 여기는 그 다음으로 남은 것만 정리.

1. **`discover-websearch` 실사용 관찰 (유저가 직접 돌리며 판단, AI 액션 아님).** 운영 커맨드:
   `node scripts/discover-websearch/run.mjs`. 볼 것은 "2026-07-31 수정" 섹션 맨 아래 "다음" 참고.
2. **채팅 모델(`gpt-4o`) 올릴지 테스트 — 하기로 함(2026-07-31).** 며칠 써보고 판단.
✅ **채팅 캐시 — 2026-07-31에 확인 완료. 오진이었다: 캐시는 처음부터 걸리고 있었다.**
`rudy.llm_usage.cached_tokens` 7일치 실측 — `chat.answer` 13건 중 7건이 캐시 히트, 입력의
40.2%가 캐시(97,408 / 242,302). 대화 안에서 보면 정확히 설계대로다:

| 대화 | 턴 | 입력 | 캐시 |
|---|---|---|---|
| `045cd78d` (07-29) | 1 → 2 | 29,849 → 32,291 | 0 → **27,520** |
| `c2a9ce52` (07-30) | 1 → 2 → 3 | 32,922 → 30,543 → 33,470 | 0 → 2,944 → **29,568** |

**비용이 안 떨어진 진짜 이유: `_shared/usage.ts` 단가표에 4o 계열 `cachedIn`이 없었다.**
`estimateCost`가 `p.cachedIn ?? p.in`으로 폴백해서 캐시 토큰을 **정가로** 계산했다 —
캐시가 아무리 걸려도 `cost_usd`는 절대 안 떨어지는 구조. 07-29의 "3턴 다 같았다"는
캐시 실패가 아니라 **계기판 고장**이었다. 고침: `gpt-4o` `cachedIn: 1.25`,
`gpt-4o-mini` `cachedIn: 0.075` (공식 가격표 재확인 — 4o 계열 50% 할인, gpt-5 계열 90%).
효과: 위 `c2a9ce52` 3턴차 기록 $0.0853 → 실제는 **$0.0483** (-43%). 앞으로 캐시 걸린
턴은 이만큼 싸게 찍힌다(실제 청구는 원래 이 값이었다 — 기록만 틀렸다).

⚠️ **중간의 `cached=2944`(= systemPrompt만)는 버그가 아니다.** `<저장소>` 전량 블록은
**기본 갈래에서만** 붙는다(`index.ts:486`). 기간·열거·축·orient 턴이 끼면 프리픽스가
통째로 바뀌어 다음 턴 한 번이 차가워진다. 갈래가 섞이는 대화의 구조적 대가지 고장이 아니다.
`chat.question_judge`가 캐시 0%인 것도 정상 — 입력이 건당 200토큰이라 최소 1,024에 못 미친다.

✅ **`_discovery-lib.mjs`의 `discover_skip` 미반영 — 07-31에 고침.** 재확인해보니 이 버그는
`check-brief`·`check-angles` 등 **진단 스크립트에만** 영향이 있었다 — `discover-websearch`가
매일 쓰는 재료 로더는 `discover-claude/material.mjs`([discover-websearch/run.mjs:32](../scripts/discover-websearch/run.mjs))
인데 그건 처음부터 `discover_skip`을 반영하고 있었다(실사용 무영향, 코드로 확인함).
`loadMaterial()`에 `discover_skip` 필터·`hidden` 셋을 `discover-claude/material.mjs`와 같은
패턴으로 추가해 진단·실물을 다시 맞췄다.

✅ **`chat` 열거 경로 검증 — 유저가 직접 확인 완료(2026-07-31).**

❌ **조립 비용 모델 비교(gpt-4o 전환 등) — 유저가 뺐다(2026-07-31): "테스트 안해도 될것같아."**
다시 꺼내지 마라 (조립을 gpt-4o로 $0.150→$0.063 레버 자체는 §비용 섹션에 기록만 남겨둠).

❌ **안전망 부분 컷 전환 — 유저가 기각했다(2026-07-31): "다 나와도 상관없어. 적은 건 문제지만
많은 건 문제가 아니야."** 즉 중복 몇 개 섞여도 항목 수 7개가 4개로 주는 것보다 낫다는 판단.
`MIN_KEEP=4` all-or-nothing 안전망은 **지금 그대로 둔다.** 다시 제안하지 마라.

✅ **`angles.ts` 배포 완료 (2026-07-31, 유저 확인).** `discovery` 함수 버전 23, 오늘 배포됨
(`npx supabase functions list`로 재확인). 위 "미정"은 07-30 세션 기준으로 낡은 기록이었다.

### 클코 이관 — 합의된 설계 (2026-07-29, 미착수)

발견을 OpenAI 대신 **맥에서 `claude -p`(헤드리스, v2.1.218 확인)로** 돌린다. 유저 지시:
**"지금 있는 건 그대로 둬. 건드리지 말고 새로 만드는 걸로."** → `scripts/discover-claude/` 신규,
기존 `discovery` Edge Function·`angles.ts`·`brief.ts`·`dedupe.ts`·`discovery.tsx`·
`_discovery-lib.mjs` 전부 무수정. 앱은 `rudy.utterances`를 읽으므로 **UI 변경 0**.

**이득이 비용($12/월→0)보다 큰 게 둘 있다:**
- **원리 D(재시도 루프)가 공짜로 들어온다.** RUDY-DISCOVERY §2-D: "검색 실패하면 각도를 바꿔라.
  원샷 금지. **Exa도 이건 못 고친다 — 루프 설계다**." 지금 파이프라인은 각도→검색 1회→조립으로
  **루프가 없다.** 에이전트는 루프가 기본이다.
- **RUDY-DISCOVERY.md를 원본 그대로 읽힌다.** 지금 §1~§6이 `angles.ts`에 압축돼 박히고
  `_discovery-lib.mjs`에 또 복제돼 있는데, `Read` 툴로 문서를 직접 읽으면 **네 번째 벌이 안 생긴다.**
- 보조 논거: §0에 따르면 **골드 스탠다드 7항목은 원래 Claude Opus가 손으로 만든 것**이다.
  검증 안 된 모델 교체가 아니라 기준을 세운 판단자에게 돌려주는 것.

**2026-07-29 밤 1차 실행 (`briefing-…13-29-14.md`):** 19턴·494초·$1.53, 환각 0(전수 검증됨),
사고 0(개발버그 없음, 링크재출고 없음), 재시도 실증(각주에 "각도를 두 번 바꿔봤다").
유저 지적 3개 → 프롬프트 수정: ① 오늘 파편 쏠림 금지 ② `idea` 최소 2 강제
③ 목표 8개(RUDY-DISCOVERY §1 "상한이지 목표 아니다"를 실사용 튜닝이 뒤집은 걸 반영).

**2차 실행 (`briefing-…23-25-29.md`, 수정 후):** **28턴·933초(15.5분)·$2.30 — 더 오래·더 비쌈.**
정상 종료(is_error false). **아직 안 봤다 — 다음 세션에서 유저가 결과 보고 판단.**
재료 37K를 매 턴 재청구하는 구조라(§ 위 "사용량" 계산) 턴 수가 곧 비용이고, 이번엔 목표를
"8개·idea 2+"로 세게 걸어서 턴이 늘었을 가능성. **다음에 볼 것:** ①②③이 실제로 먹었나
②비용 상승이 감수할 만한가 → 아니면 `--max-turns` 상한 또는 sonnet 전환 검토.

**결정된 값:** `trigger='pull'` · 중복 방지는 **힌트만**(임베딩 게이트는 안 옮긴다 —
"아직 안 일어난 문제에 장치 만들지 마라") · 첫 판은 **수동**, 만족하면 자동.

⚠️ **`trigger`는 `push`로 잡았다가 되돌렸다** (2026-07-29, 유저 지적: "왜 아침으로 떠?
이건 브리핑 아니고 그냥 발견 아니야?"). `push`는 앱에서 **"아침" 배지**가 되는데, 이 스크립트가
만드는 건 **발견**이다. 같은 날 유저가 "아침 브리핑은 발견 빼고 관찰만"으로 정했으므로
발견을 아침이라 라벨링하면 그 결정과 어긋난다. `push`를 밀었던 이유(모닝 버튼 잠금 =
중복 지출 방지)는 부수 효과였고, 아침이 관찰 전용이 되면 그 버튼 자체가 없어질 자리다.
→ **아침 브리핑을 만들 때 그때 새 표면을 쓴다. 발견은 발견으로 남는다.**
부작용: 목록에서 클코 발견과 앱 발견이 **구분이 안 된다**. 비교가 필요하면 `.work/`의
타임스탬프와 `created_at`을 맞춰 본다 (앱을 안 건드리기로 한 결과).

**자동 실행 — 조사 완료. 맥이 유일한 길이다:**

| 방법 | 되나 | 이유 |
|---|---|---|
| 클라우드 예약 에이전트 | ❌ | **로컬 파일·환경변수 접근 불가.** `.env`가 gitignore라 Supabase 키가 없고 옵시디언도 못 읽는다 |
| 세션 크론(CronCreate) | ❌ | 세션 메모리에만 산다. 클로드 끄면 사라진다 |
| 모바일 | ❌ | 모바일 런타임 없음 |
| GitHub Actions | ⚠️ | 돌지만 구독이 아니라 **API 키 과금** — 목적과 반대 |
| **맥 launchd** | ✅ | **전원 연결이면 뚜껑 닫아도 됨** (`pmset repeat wakeorpoweron`). 배터리+닫힘은 안 됨 |

RUDY.md §3-1이 Things 브리지에 대해 내린 판단과 같다: "맥이 꺼져 있으면 그 시점에 멈출 뿐."

### 클코 3파전 — 2026-07-30, 코딩 끝. 다음 세션이 돌리고 판단한다

**시작은 "GPT랑 비교하자, 모델만 바꿔봐"였는데 두 번 헛다리를 짚었다.** 기록해두는 이유는
같은 헛다리를 또 안 짚기 위해서다:
- 1차 시도: `discover-claude`(어제)와 `discover-split`(오늘 초반)을 비교했는데, **둘 다
  내가 그날그날 새로 쓴 프롬프트였다.** `ANGLE_SYS`/`ASSEMBLE_SYS`(프로덕션 상수)를
  **0건** 사용 — grep으로 확인됨. 그래서 "오늘 판이 왜 돈 얘기로 쏠렸나"에 내가
  "품질이 낫다"는 근거 없는 판단까지 얹었다가 유저에게 정면으로 지적당했다.
- 원인은 셋: ① 각도를 12개로 강제(§1 "상한이지 목표 아니다"를 내가 또 위반) ②
  RUDY-DISCOVERY.md §1의 "실제로 돈이 된다/얼마 버는가" 문장 자체가 사업 방향을
  유도 ③ Exa `type=auto`가 인터뷰가 아니라 정리글·업체 홈페이지를 물어옴(옛 스크립트
  `check-idea-engines.mjs` 헤더에 이미 기록돼 있던 현상).
- **재료가 원인이 아니었다** — 실측: 어제·오늘 재료를 diff해서 돈 각도의 출발 파편
  (혁신의숲 리포트 4건·'운영까지 각잡고'·Fedica·해외살기)이 **둘 다에 똑같이 있었음**을
  확인. 어제 판은 안 골랐고 오늘 판이 골랐다 — 재료 탓이 아니라 각도 프롬프트 탓.

**돈 얘기 문장을 지웠다** (하지마 형태 아니라 문장 자체를 뺐다 — 유저가 명시적으로
"제일 싫어하는 얘기"라고 했다):
`RUDY-DISCOVERY.md` §1·§2-D·§3-④·§6 · `supabase/functions/discovery/angles.ts`(배포판) ·
`scripts/_discovery-lib.mjs`(노드 미러, 프로덕션과 반드시 동기화) · `discover-claude/prompt.md` ·
`discover-split/prompt-angles.md`. ✅ **`angles.ts` 2026-07-31에 배포 완료** (버전 23) — 앱
발견 버튼에 반영됨.

**최종 합의: 3파전, 코드로 확정.** 유저 확정 문장: "1. 클코 + 클로드websearch (루프 없이)
2. 클코 + exa". 재료·기준(RUDY-DISCOVERY.md)은 셋이 전부 공유한다 — 다르면 비교가 안 된다.

| | 폴더 | 각도 | 검색 | 조립 | 상태 |
|---|---|---|---|---|---|
| 원본 (안전망) | `discover-claude/` | 한 호출 (WebSearch, 루프 허용) | Claude WebSearch | 같은 호출 | 무수정 |
| **1번** | `discover-websearch/` | 한 호출 (WebSearch, **재시도 금지**) | Claude WebSearch | 같은 호출 | 신규 |
| **2번** | `discover-split/` | 별도 호출 (툴 없음, 인라인 재료) | Exa (Node, 병렬) | 별도 호출 (재료 안 봄) | 신규 |

- **1번**은 `discover-claude/`를 `cp`한 것 — `diff` 결과가 전부다: "재시도 정상, 2~3번까지"
  → "재시도 금지, 각도 10~12개 미리 잡아 방어" + "검색은 병렬로 던져라". 그 외 글자 그대로.
  원본(`discover-claude/`)은 **무수정, 언제든 되돌아갈 안전망.**
- **2번**은 각도 상한을 12→**8~10**으로 되돌렸다(위 원인 ①). 조립 프롬프트에 지정 파편
  보호 규칙 추가.
- **셋 다 기본이 `--save` 없이 저장 안 함** — 원장에 넣으면 다음 판 재료의 <이미 다룬 주제>가
  바뀌어서 비교 조건이 흔들리고, 앱에서 셋을 구분할 방법도 없다(전부 `trigger='pull'`).
- **비교 축이 이제 명확하다:** 1번 vs 원본 = "재시도를 끄면 얼마나 싸지고 얼마나 상하나".
  1번 vs 2번 = "어제 좋았던 게 프롬프트냐 WebSearch냐"(유저 원래 질문). 2번 vs 1번의
  Exa 출처(정리글) vs WebSearch 출처(1차 인터뷰) 차이도 같이 보일 것.

**지정 파편(`discover_next`) 버그 — 이번에 같이 고쳤다.** 원인: `brief.ts:377`이 스스로
인정 — "전부 내린다. 어느 지정이 실제로 각도가 됐는지는 알 수 없다." 검색이 망해 항목이
안 나와도 표시는 사라져서, 유저가 지정해놓고 표시만 잃는 사고가 실제로 났다. 조치:
① 두 프롬프트에 "지정에서 나온 각도는 절대 버리지 마라, 못 찾았으면 못 찾았다고 쓰되
항목은 내라" 추가 ② 소비는 **자동으로 그대로 유지**(유저 확정: "일일이 못 끈다") —
다만 2번은 각도 JSON에 `from_picked`가 있어서 **"각도조차 안 됐으면 그때만 안 내린다"**를
코드로 구현 가능했다(1번·앱은 출력이 마크다운이라 이 확인 자체가 불가능 — `brief.ts:377`의
한계 그대로). ③ `discover-claude/material.mjs`가 전부 `data ?? []`라 **쿼리가 죽어도
조용히 0건**이 되던 것도 고쳤다 — 이제 에러를 던진다(세 판이 공유하는 파일이라 셋 다 적용됨).

### 3파전 결론 — **1번(`discover-websearch`) 채택** (2026-07-31, 유저 확정)

1·2번을 돌려서 나란히 놓고 판단했다. 브리핑 2건은 원장에 저장돼 앱 발견 탭에 있다
(맨 위 `※ 1번/2번 …` 라벨로 구분 — 앱은 셋을 구분 못 하므로 라벨을 본문에 넣었다).

| | 턴 | 시간 | API환산 | 세션 사용량 | 현금 |
|---|---|---|---|---|---|
| 1번 | 15 | 546초 | $1.43 | **+24%p** | 0 |
| 2번 | 2+2 | 629초 | $1.39 | +27%p | + Exa 7건(몇 센트) |

- **"턴 수가 곧 비용"은 깨졌다.** 2번은 턴이 1/4인데 더 먹었다 — `claude -p`를 두 번 띄워
  캐시를 두 번 새로 만들고(118K vs 99K), 각도 호출 하나가 출력 29.7K(사고 포함)를 태웠다.
  **턴을 줄이는 방향으로 최적화할 근거가 사라졌다.**
- 1번이 이긴 축: 갈래 다양성(오프라인 4개 vs 1개) · 출처 1차성 · 보이는 거절(3건 vs 1건).
  2번이 이긴 축: `idea` 슬롯이 전부 프로덕트로 착지 · 각도가 JSON이라 **사후 진단이 가능**
  (1번은 마크다운이라 "어느 각도에서 나왔나"를 영원히 못 짚는다 — `brief.ts:377`과 같은 한계).
- **`discover-split`·`discover-claude`는 안 지운다** — 안전망 + Exa/분할 구조의 유일한 기록.

### 2026-07-31 수정 (1번에만 적용 — 나머지 두 판 프롬프트는 무수정)

1. **`idea` = "누가 이미 만들어 파는 물건"으로 못박음** + **`lens` 1 → 2.** 위 「절대 건드리지
   말 것」 참고. 진단: 소재 절단 규율은 지켜졌는데 검색어에 지명·프로그램 이름이 들어가
   idea 3개가 전부 "가볼 곳"으로 착지했다(감각차단탱크·텃밭분양·오픈마이크). 원인은
   프롬프트 문구가 아니라 **아직 없는 물건은 검색이 안 되고 이미 파는 체험은 검색이 되는 것.**
2. **출처 링크 1개 → 1~3개** (유저: "같은 아이디어여도 제품이 다를 수 있는데 그 다른 게
   또 의미있을 수 있으니까"). 조건 = 뒤 링크가 앞과 다른 걸 보여줄 때만.
3. **무덤 링크가 재료에 현재형으로 들어가던 것** (`material.mjs`, 세 판 공유).
   실측: `=== 이미 저장한 링크 ===` 68개 중 **57개(84%)가 무덤**이었고, 유저가 이틀 전 묻은
   Duedance를 1번이 "저장해둔 Duedance"라고 각주에서 불렀다.
   ⚠️ **빼는 게 처방이 아니다** — 묻어도 유저가 가진 링크라 빼면 07-28 사고(Pushary 자기 사이트
   재출고)가 그대로 재발한다(Pushary·TouchGrass가 지금 무덤이다). **`(묻음)` 표시 + "막는
   용도로만 봐라"**로 갔다. 채팅 무덤 문제를 푼 방식과 같다.

**운영 방식 확정 (2026-07-31):** 자동화 안 한다 — 유저가 매일 손으로 커맨드 치고 앱에서
확인한다. 그래서 `discover-websearch/run.mjs`의 **기본을 저장으로 뒤집었다**
(비교용이라 기본이 저장 안 함이었는데, 채택됐으니 의미가 없어졌다).
**돌리는 커맨드: `node scripts/discover-websearch/run.mjs`** (실험은 `--no-save`).

**다음 (유저가 직접 볼 것):** 돌리면서 ① idea가 물건으로 착지하나 ② 관점 2칸이 오프라인을
받나 ③ 뒤 링크가 앞과 같은 종류인 항목이 3개 이상이면 링크를 1개로 되돌린다.
프로젝트 `discover_skip`·재료 상태는 유저가 알아서 관리 — AI가 먼저 건드리지 마라.
`angles.ts`(앱 발견)는 2026-07-31 배포 완료.

### ⚠️ 발견의 위상 — 유저 온도 (2026-07-28)

유저: **"사실 발견에 크게 의미를 두고 있지 않아. 너무 비싸기도 하고. 무슨 의미인가 싶기도 하고."**

RUDY.md는 발견을 1급으로 놓지만 **지금 유저의 실제 온도는 그렇지 않다.** 발견에 큰 작업을
새로 제안하기 전에 이걸 먼저 떠올려라. 회당 $0.39는 매일 돌리면 월 $12다.
**비용을 줄이는 제일 큰 레버는 모델이 아니라 빈도다**(아래 비용 섹션) — 의미가 확실하지
않은 기능이면 모델 교체(-26%)보다 "덜 자주 돌린다"가 먼저다. 단, 빈도는 유저가 정한다.

---

## 절대 건드리지 말 것 (유저가 되돌리며 확정한 값)

- **`LENS_MAX = 2` · `IDEA_MIN = 2`(둘 다 프롬프트 요구).** 원래 "관점 한 개 넘지 않게"였는데
  **2026-07-31에 유저가 2로 올렸다** — 같은 날 `idea`를 "만들 만한 것" → **"누가 이미 만들어
  파는 물건"**으로 못박으면서, 밀려나는 오프라인 갈래(스파·텃밭·클래스·전시)를 받을 자리가
  필요해졌기 때문이다. 둘은 **한 세트다 — 하나만 되돌리지 마라.**
  ⚠️ "코드 컷"이라고 적혀 있었지만 **지금 코드에 `LENS_MAX` 상수가 없다**(전수 grep 0건).
  앱 경로는 07-27에 `lens`를 통째로 뺐고, 클코 판은 프롬프트로만 건다.
  **관점은 0개여도 된다** (유저 2026-07-28: "렌즈는 없어도 된다"). 상한이지 하한이 아니다 —
  lens가 0이라고 각도 상한을 다시 올리지 마라.
- **출처 링크 = 항목당 1~3개** (2026-07-31 유저 확정, 1개에서 되돌림). 조건은 "뒤 링크가
  앞과 **다른 걸** 보여줄 때만". 상세·되돌릴 신호는 `RUDY-DISCOVERY.md` §6.
- **`<이미 다룬 주제>` 힌트 = 250(사실상 전체).** 30으로 줄였다가 **되돌렸다** — 잘린 중복 6개가
  전부 힌트 밖이었다. **막는 게 잡는 것보다 6배 싸다**(입력 $5/1M vs 출력 $30/1M).
  30일 창이 자연 상한이라 무한히 안 자란다.
- **`fragLine`에서 `{id:}` 제거, URL은 유지.** URL은 유저가 승인한 적 없어서 복원했다 — 다시 빼지 마라.
- **`done` 프로젝트는 재료에서 제외.** `paused`는 남긴다(다시 시작 가능).
- **재료 구획 3분리**: `진행 중인 일`(active, 캡 2개) / `아이디어·수집함`(💡·글감·paused, 캡 없음) / `미소속`.
  **"프로젝트"라는 단어를 lists 구획에 쓰지 마라** — 라벨이 캡 소속을 정한다.
- **원장 저장은 조립 *전에*.** 되돌리면 07-26 사고(화면엔 나왔는데 기록 0건)가 재발한다.
- **잘 도는 것은 손대지 마라** (유저 명시): 각도 프롬프트의 렌즈·원리·few-shot·프로젝트 캡 2개,
  조립 프롬프트, 검색, 무덤, 회상, 축/orient, 재료 로딩 구조.

## 다시 제안하지 마라 (기각됨 — 이유 포함)

| 기각된 것 | 왜 |
|---|---|
| 검색어에 "구체적 이름·고유명사" 요구 | 유저: "고유명사 이딴 소리하지말고." idea 정의 + few-shot으로 충분했다 |
| 프롬프트에 규칙 계속 쌓기 | 3회 연속 규칙 추가 → 출력 4,077→5,053→6,475로 오르고 검색어가 막연해졌다. **막히면 빼는 방향으로 먼저** |
| 저장 시점 stance enum 인덱싱 | enum은 미리 예상한 속성만 잡고 새 속성마다 백필이 필요하다. 전수 스캔은 어떤 속성이든 잡는다 |
| 재료 고정 쿼터 (전량 읽기 교체) | 원리 C(북마크 × 프로젝트 겹치기)는 커버리지를 요구한다. "줄이 늘면 주의력이 나빠진다"는 추측이었지 실측이 아니었다 |
| 우선순위 위계표 | active가 위인 게 맞다. 캡 2개는 강등이 아니라 신선도 장치고 이미 해결했다 |
| 프롬프트 캐싱 + 재료 정렬 뒤집기 | OpenAI 자동 캐싱은 수 분~1시간. 하루 1회 브리핑엔 안 걸린다 |
| resurface 90일 창 제거 | 코퍼스 2주라 효과 0인 투기적 변경. 나중에도 회상 엔진(`recall.ts`) 후보 로직을 재사용하는 게 맞다 |
| 프로젝트 승격 기능 | 이미 있다(파편을 프로젝트에 붙이고 active로). 캡 2개라 승격해도 지분이 안 는다 |
| 브리핑 때 "뭐 찾아줄까" 묻기 | 신호 발생 시점과 묻는 시점이 어긋나고, 원탭 의식에 통행세가 붙는다 |
| 큐 소비 상한 / 7일 만료 | 각도 상한이 이미 자연히 막는다 |
| 무덤을 자발적 연결에 열기 | 유저: "무덤은 그냥 둬도 될 것 같아." 유저가 명시적으로 찾을 때만 나온다 |
| 아직 안 일어난 문제에 미리 장치 만들기 | 전부 "요청 안 했다"로 기각. **유저가 명시적으로 겪은 증상만 고쳐라** |
| 각도에 **갈래**(물건/소프트웨어/공간/문화/사람/일하는방식) 강제 배정 | 내가 만든 목록이었다. 유저: "이 갈래는 좀 쓰지마. 너맘대로 이거 그냥 다 삭제해버리고." 반복도 못 막았다(코리빙이 갈래를 바꿔가며 6회 중 5회) |
| **금지어 선언**시켜 소재 끊기 | 단어는 지키고 영역은 그대로 갔다. `사는 곳` 금지 선언 → `"creative residency" "community house"`. **단어를 막지 영역을 못 막는다** |
| 동기에 **행동 동사 금지** | 성공 개수는 그대로(6/10)인데 **전시·공간 갈래가 3→0으로 죽었다.** 상세 §7-f |
| idea 재료를 글감·인용구로 제한 | 유저: "프로덕트 아이디어로 나오는 건 좋은 거지. 무조건 소재를 끊는 데 집중해야." 재료를 좁히는 건 문제를 피하는 것 |
| 던진 발견 카드를 재료(`loadMaterial`)에서 출처 컬럼으로 걸러내기 (2026-07-28 시도·되돌림) | §7-f가 "루디 문장이 재료의 17%" 문제로 진단했지만, 처방이 틀렸다. 유저: "루디에 있는 걸 꼭 빼야 할 이유가 있나? 어쨌든 관심 있어서 던진 거고 새 아이디어일 수도 있는데." **던지기 자체가 가장 강한 관심 신호라 재료에서 빼면 신호를 버리는 것.** 진짜 문제(같은 걸 또 낸다)는 이미 `dedupeAngles` + `<이미 다룬 주제>` 힌트가 담당하는 영역 — 반복이 실제로 눈에 띌 때 그쪽을 보되, 출처로 통째로 배제하지 마라. 시도 흔적: `fragments.source` 컬럼이 실제 DB에 남아 있다(nullable, 아무 코드도 안 읽고 안 씀) — 지우고 싶으면 `alter table fragments drop column source;`, 안 지워도 무해하다 |

### 실패한 시도 — idea 게이트(임베딩) (2026-07-26)

`idea`의 `from`(「파편 → 동기」)에서 소재와 `query`를 임베딩 비교해 강등하려 했다. **실측 결과
신호가 없다:** 검색어는 영어·소재는 한국어라 sim이 전부 0.16~0.34에 깔리고, **품질이 나쁜 것이
좋은 것보다 낮게** 나왔다(순서 역전 → 어떤 임계로도 못 가른다). 언어 차이가 의미 차이를 덮었다.
→ **교훈 재확인: 판정이 필요하면 판정을 시켜라. 부산물(임베딩 거리)로 판정하지 마라.**

## 재발 방지 교훈 (전부 실제로 터진 것)

- **증상을 들으면 설명하기 전에 DB부터 연다.** 2026-07-26에 "5개만 나온 건 dedupe 때문"이라고
  확인 없이 말했다가 틀렸다(실제로는 조립이 잘림). 3초면 확인할 수 있었다.
- **진단 스크립트가 프로덕션 프롬프트를 import하지 않으면 다른 물건을 재는 것이다.**
  `check-idea-angles.mjs`는 `ANGLE_SYS` 대신 자체 프롬프트를 갖고 있었고, 그걸로 얻은 결론
  ("모델이 옆동네를 못 간다")이 문서에 확정처럼 박혔다. 실물엔 그 구조 자체가 없다.
  **새 진단 스크립트를 만들면 제일 먼저 "실물과 같은 프롬프트를 쓰는가"를 확인해라.**
- **실행 결과는 stdout에만 남으면 사라진다.** 07-26~27 실험 4건의 결과가 파일로 안 남아
  다음 세션이 "기억도 안 난다"가 됐다(대화 기록에서 겨우 복원). **진단 스크립트는 출력을
  파일로도 써라.**
- **재료를 의심해라 — 모델이 자기 출력을 읽고 있을 수 있다.** 브리핑 문장을 유저가 저장하면
  다음 브리핑의 재료가 된다. `fragments`에 출처 컬럼이 없어서 구별이 안 된다(§7-f).
- **"막는 장치"가 공급원이 될 수 있다.** `<이미 다룬 주제>`는 반복을 막으려고 넘기는 목록인데
  모델이 그걸 출발 재료로 썼다. 넘기는 모든 블록에 대해 "이게 재료로 읽힐 수 있나"를 물어라.
- **스트리밍에서 "사라짐"은 대개 셋 중 하나** — 버퍼 flush 누락 / 낙관적 화면 정리 / ScrollView `flex:1`.
- **PostgREST 다중 행 insert는 모든 행에 같은 키를 채워라.** 빠진 키를 default가 아니라 null로 채운다.
- **프롬프트 예시에 구체값(날짜·제목·수치)을 쓰면 모델이 복사한다.** 이 프로젝트에서 두 번 터졌다.
  예시엔 형식만 쓰고 값은 "블록에서 가져와라"로 지시할 것.
- **유사도 검색은 시간·개수·존재 여부를 못 답한다.** "있냐/없냐", "몇 개", "언제"는 다른 종류의 질의다.
- **시간대 버그는 한 곳만 고치면 반쪽이 남는다.** 경계(자르기)와 표시(찍기)는 다른 코드다.
- **"이게 무슨 종류인가"를 부산물로 판정하지 마라** (주제어 빈 배열 ≠ 메타 질문).
- **계산된 값으로 현상을 판정하기 전에 계산기를 의심해라.** 07-29에 `cost_usd`가 안 떨어지는 걸
  보고 "캐싱이 안 걸린다"고 적었는데, 실제로는 단가표에 캐시 입력가가 없어서 계산이 안 떨어질
  뿐이었다. **원본 관측치(`cached_tokens`)가 바로 옆 컬럼에 있었다** — 3초면 확인할 수 있었다.
- **외부 API는 이름이 약속하는 것과 실제 동작이 다르다.** 붙이기 전에 실제 데이터로 때려봐라
  (`findSimilar`는 미러·쇼핑몰만 물어왔다).
- **약한 지시로는 구조적 쏠림을 못 이긴다 — 숫자 캡은 코드로.** 단, 이건 캡을 코드로 옮기라는
  뜻이지 프롬프트에 문장을 쌓으라는 뜻이 **아니다.**
- **프롬프트로 모델의 마크업 습관을 막으려 하지 마라 — 파서가 견뎌야 한다.**
- **실패가 뜨면 코드가 아니라 판정(테스트·스크립트)이 틀렸을 가능성부터 확인할 것.**

## 진행 상태 (RUDY.md §10)

| § | 내용 | 상태 |
|---|---|---|
| 0 | 임베딩 파이프라인 · 링크 임베딩 품질 | ✅ 실사용 중 |
| 1 | 링크 다이제스트 | ⏸ 보류 (표면 없음·검색은 이미 해결) |
| 2 | 하이브리드 의미검색 + 검색 UI | ✅ |
| 3 | 충돌 회상 + "왜 지금" | ✅ (임계 0.42) |
| 4 | 원장 + 게이트 로그 + 쿨다운 | ✅ |
| 5 | 채팅 RAG + 자발적 연결 + 원탭 | ✅ v4 100/100 검증 |
| 6 | 클러스터 엔진 → 패턴·경향 | ✅ (임계 0.42, 코퍼스 자라면 지속 분기 자동 켜짐) |
| 6+ | 늦은 의도 (F1) + `rudy.evidence` | ✅ |
| 7 | 발견 브리핑 엔진 + 전용 화면 + 던지기 | ✅ (2단계 분리까지 배포. 결과물 눈 검증만 남음) |
| 8 | 아침 푸시 → 수동 버튼으로 대체 (cron 폐기) | ✅ |

**미착수/미정:** `more like this` 칩(자리만 있음 — `findSimilar` 실측이 나빠 방향 미정) ·
넛지 · 주간 편지 · 월말 성적표 · OpenAI Costs API 대조 스크립트(추정치 검증용, Admin 키 필요).

### 아침/주간 브리핑 — 2026-07-29 재점검 (지금 만들지 않기로)

**모닝 브리핑은 발견과 거의 같은 물건이다.** `morning-briefing/index.ts` 주석이 자인한다:
"발견 화면의 '새로 발견하기'와 **같은 엔진**(streamBrief). `trigger='push'` 태그만 다르다."
차이는 `prelude` 한 줄뿐이고, 그마저 **어제 프로젝트별 저장 개수**이며 모델에 안 가고
`brief.ts:309`에서 문자열로 앞에 붙기만 한다.

**F4가 약속한 재료 4개 중 0개가 있다:** 캘린더·투두 맥락(어댑터 없음) / 맥락 리서페이스 A2
(캘린더·지오펜스 필요) / 넛지 A3(**재료는 다 Mind 안에 있다 — 유일하게 지금 만들 수 있는 것**) /
선제 관찰 D2(캘린더 필요). 지금 들어 있는 발견(E)은 F4 재료 목록에 **없고**, RUDY.md §2-5는
오히려 "발견은 아침 브리핑에 **1개까지**"라고 상한을 건다.

**유저가 원한 것 (2026-07-29 구술):** "어제 혹은 오늘 너는 이랬다. 그리고 패턴, 성향,
최근 관심사, 내가 요즘 어떤지." → 이건 이미 유저가 07-22에 파편으로 적어뒀다:
*"내가 오늘 생각해야할게있나 하면 메모리를 뒤져서 내 경향성을 판단해서 딱 추천해줘야되는데
흐림정도나 이런것도 생각을 하든지."* **그리고 그건 `chat/orient.ts`(축×흐림)가 이미 하는 일인데
채팅에 물어야만 도는 자리에 붙어 있다.**

**결론: 지금 안 만든다.** 코퍼스가 2주치라 축이 잘 안 서고(§6 "코퍼스 자라면 자동으로 켜짐"),
A2·D2는 어댑터가 없다. **투두가 들어오거나 코퍼스가 자란 뒤에 다시 본다.**

**✅ 만들 때의 모양은 정해졌다 (유저 확정 2026-07-29):**
> "아침 브리핑은 **발견은 하지 말고 그냥 관찰 정도만**. 근데 **관찰을 좀 더 디테일하게** 가면 좋겠어."

- **발견을 아예 뺀다.** RUDY.md §2-5의 "1개까지"보다 더 센 결정이다 — 0개.
  아침과 발견은 **다른 표면**이고, 발견은 발견 탭에서 당겨서 본다.
- **관찰이 본체다.** 지금 `observationLine()`은 **개수만** 낸다("어제는 No phone에 3개").
  개수는 관찰이 아니다 — 그게 "뭉뚱그려진" 부분이다. 디테일하게 간다는 건 최소한:
  무엇을 던졌나(제목) · 어느 결이 새로 생겼나 · 뭐가 흐려지고 있나 · 안 건드린 게 뭔가(넛지 A3).
  유저가 원한 것: *"어제 혹은 오늘 너는 이랬다. 그리고 패턴, 성향, 최근 관심사, 내가 요즘 어떤지."*
- ⚠️ `observationLine()`이 낸 줄은 **모델에 안 간다**(`brief.ts:309`에서 문자열 접합).
  관찰이 본체가 되면 이 구조부터 바꿔야 한다 — 관찰을 모델이 **쓰게** 해야 한다.

⚠️ **아침 브리핑은 유저가 사실상 본 적이 없다** — `trigger='push'` 원장이 28건 중 **1건**
(2026-07-22, cron 시절). "별로다"는 출력물 평가가 아니라 **설계 의도와 다르다**는 지적이었다.

**조언("이런 거 해봐라")의 리스크 — 유저가 먼저 우려했다.** prompt.ts §오늘이 이미
"처방하지 마라"를 건다. 정체는 이것: **조언은 틀리면 무시를 학습시킨다**(§2-8). 다만 셋은 다르다 —
처방("오늘은 X 해라") ❌ / 열어둔 제안("케이스랩 3주 조용한데, 접은 거야?") ⭕ /
재료 제시("찾아볼까?") ⭕. **질문형으로만** 내보내면 리스크가 준다. 중간 형태는 이미 있다
(채팅의 `outward='ask'`). 무시가 쌓이는지 `utterances.user_response`로 보고 결정한다.

### 나중으로 (유저 결정 2026-07-29 — 지금 꺼내지 마라)

| 미룬 것 | 이유 |
|---|---|
| **Life Dashboard** | 아침/주간 브리핑의 확장이라 그게 먼저다. ⚠️ **F5가 "대시보드가 아니라 서사. 숫자·통계·달성률 금지"를 명시한다** — 원칙 충돌이니 만들 때 유저에게 다시 확인할 것. 걸리는 건 숫자가 아니라 **점수**(연속일수·달성률·전주대비)다. "지금 흐려지는 것", "3주째 안 건드린 것"은 상태 표시라 안 걸린다. 대시보드(당기는 표면·상시)와 편지(받는 것·주1회·희소성)는 **대체재가 아니다** |
| **옵시디언 맥락** | 큰 작업. 단 방향은 잡혔다 — **RAG를 만들지 마라.** 클코 발견이 로컬에서 도니 `Read`/`grep`으로 볼트를 인덱싱 없이 뒤질 수 있다. 채팅에 붙이면 전량이 안 들어가 임베딩 문제가 되돌아온다. 노이즈 우려는 실재(daily note에 회의록·할일이 섞임 — Mind 코퍼스가 개발 버그 메모로 망가진 것의 10배 규모) |
| **루디에게 남길 노트 / 사전** | 유저 결정: 지금 큰 도움 안 됨. ⚠️ **AI가 "이걸 만들면 발견이 나아진다"고 말했는데 근거가 없었다 — 유저가 정확히 지적했다.** 문서가 기록한 발견 헛발질 4건은 전부 **프로젝트 description 미독**이 원인이었고(원리 A) 그건 이미 고쳤다. **고유명사를 몰라서 발견이 나빠졌다는 실측은 0건이다.** 다시 제안하려면 실측부터. (유저 파편에 아이디어는 있다: 07-23 "product for two", 07-26 "AI용 주석 레이어") |
| **먼저 거는 말 되살리기** | 유저: "일단 보류하자" |

### 투두 — 앱 안에서, 완전히 분리 (유저 결정 2026-07-29, 착수는 나중)

Things 연동 안 한다. **앱 안에서 하되 파편과 완전히 분리한다.** 파편 → 투두 변환은 가능.
근거: 유저 파편(07-29) *"띵스 안쓸바에야 그냥 이 안에서 투두관리하지"*.

**왜 분리가 필수인가:** Mind의 법칙은 **감쇠**(안 만지면 흐려지고 무덤)인데 todo의 법칙은
**정반대**(완료될 때까지 안 흐려져야 하고 오히려 시끄러워져야 한다). 한 통에 넣으면 하나가
반드시 깨지고, **지금은 todo가 깨져 있다** — 유저가 Mind를 이슈 트래커로 써서 07-16 저장분
43개(개발 버그 메모)가 전부 무덤으로 갔다. 그게 채팅이 멍청했던 원인의 절반이었다.

**보류 논의:** 채팅 라우팅을 tool-calling으로 재설계할지. 지금은 싼 모델이 5개 필드로 먼저
분류하고 그 상자만 태우는데, 분류가 틀리면 본답변이 아무리 좋아도 이미 틀린 재료를 받는다.
유저가 "A(소규모 개선) 먼저, 실사용해보고 판단"으로 미뤘다.

### 채팅 확장 아이디어 — 2026-07-31 논의 (백로그, 지금 착수 아님)

기획 대화에서 나온 것들. 순서는 우선순위가 아니라 논의 순서.

- **채팅이 저장소를 고치는 자리로 (지금은 읽기 전용).** "이거 셋 다 케이스랩으로 묶어줘" /
  "이건 파편 아니고 버그 메모야, 빼줘"처럼 지시하면 D1 승인 게이트를 거쳐 반영.
  **가장 값이 큰 건 프로젝트 description 채우기** — 발견·채팅 헛발질 4건이 전부 description
  미독/빈약이 원인이었다(§나중으로 표). 루디가 질문만 던지고 유저 답을 §4-B2 자기 진술로 반영.
- **대화 로그를 코퍼스에 편입.** 지금 `messages`는 그 턴 맥락으로만 쓰이고 검색·충돌 대상이
  아니다 — "전에 얘기했던 그 구조 뭐였지"에 지금은 답 못 한다. ⚠️ 되먹임(루디 발화가 재료가
  되는 것)을 07-28에 이미 겪었으니(위 "되먹임 재현" 사고) 착수 전에 격리 방법부터 설계할 것.
- **채팅에서 즉석 발견 1개.** 지금 발견은 배치 8개짜리 의식(회당 $0.3~0.4)이라 유저 온도가
  낮다(§위 "발견의 위상"). 채팅 `<바깥>` 검색에 각도 생성 한 단계만 얹으면 항목 1개짜리
  발견이 된다(항목당 $0.02~0.14 수준). 발견의 값이 배치가 아니라 타이밍에 있었다면 이쪽이 본체.
- **구매·여행지 결정 지원.** 유저가 사고싶은 것/가보고 싶은 곳을 실제로 저장한다(prompt.ts
  예시로 쓸 만큼). 지금은 열거만 된다. 비슷한 걸 전에도 저장했나(반복 욕구 vs 스침) + 그 뒤
  무덤됐나 + 바깥 정보 겹치기 = 감쇠 이력이 있어야만 가능한 답. 투두 편입 이후 붙이면 자연스
  럽다: "스피커 사기" 투두 → 조건 인터뷰 → 검색·비교 → 옵션 제시. D1(액션 프레임워크)의
  구체적 인스턴스이지 새 구조는 아니다.
- **여행 리서치 (A2 확장).** A2는 지금 **저장해둔 파편**을 여행 전에 되살리는 것뿐 — 새로
  찾아주진 않는다. 캘린더에 여행 일정이 잡히면 목적지·예산·동행 조건을 인터뷰하고 코스·맛집·
  숙소를 **새로** 찾아 비교. 트리거만 캘린더고 골격은 위 "구매·여행지 결정 지원"과 같다
  (인터뷰→검색→비교). 구글 캘린더 어댑터(§3-1, §10-9) 있어야 성립.
- **남에게 줄 선물 (렌즈 뒤집기).** "A 생일선물 뭐줄까" — 지금 렌즈(§4-E3)는 전부 **내 취향**
  기준. 유저 파편 중 다른 사람에 대한 관찰("A는 미니멀 좋아함")이 있다면 그 이름이 걸린
  파편만 모아 그 사람 기준으로 렌즈를 다시 계산 — 로직 재사용, 대상만 교체. ⚠️ 재료(남에 대한
  메모)가 실제로 코퍼스에 충분한지부터 확인 필요, 착수 전 실측.
- **막혀 있는 결정에 먼저 손 내밀기 (A3 + D1 결합, 유저 픽 — "제일 괜찮다").** A3는 지금
  "3주째 안 건드렸는데 버릴까 할까"까지만 묻는다. 안 건드린 이유가 "리서치를 안 해서"일 수
  있으니 한 단계 더: **"내가 대신 후보 찾아서 추려줄까?"** — 넛지가 질문에서 그치지 않고
  액션 제안(D1)으로 넘어가는 자리. 새 엔진이 아니라 **A3와 D1을 잇기만 하면 된다** — 이미
  있는 두 조각의 조합이라 착수 비용이 제일 낮다.
- **막연한 할 일 쪼개기.** 투두 편입 이후. "블로그 만들기"처럼 뭉툭한 항목을 넣으면 과거
  비슷한 프로젝트(케이스랩 등)의 진행 이력·description을 참고해 하위 단계로 쪼개 제안.
  새 재료 불필요 — 프로젝트 description + 진행 이력이 이미 있는 재료다.

## 배포 · 스크립트

**서버가 없다** (SPEC §3) — SQL은 Supabase SQL Editor에 사람이 직접 붙여넣는 게 정상 배포 경로다.
AI는 git 명령을 직접 실행하지 않는다 (CLAUDE.md §5).

```bash
# CLI가 PATH에 없고 프로젝트가 로컬 링크 안 돼 있다 — 반드시 이 형태여야 한다
npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions list --project-ref ibqyqpmwqujcxlnkyizf   # 버전 확인
```

**진단 스크립트** (전부 로컬 node, service role 필요, `.env`에서 키를 읽는다):
`check-angles`(각도만 — 브리핑보다 싸다) · `check-brief`(검색+조립) · `check-idea-engines`(엔진 비교) ·
`check-chat`(chat E2E) · `check-collision`·`check-clusters`(임계 튜닝) · `check-ledger`·`check-intent` ·
`check-embeddings`·`check-search`.
⚠️ **프롬프트가 `scripts/_discovery-lib.mjs`·`check-brief.mjs`에 복제돼 있다** (Deno↔node 경계).
한쪽만 고치면 진단과 실물이 갈라진다.

## 비용 — 확정된 사실 (다시 계산하지 말 것)

**`rudy.llm_usage`를 request_id로 묶으면 나온다. 다시 돌리지 말고 쿼리해라.**
07-22~26 완전 실행 11건(각도+조립 둘 다 있는 것) 실측:

| | 평균 입력 | 평균 출력 | 평균 합계 |
|---|---|---|---|
| `discovery.angles` | 14,739tok → $0.074 (36%) | 4,368tok → $0.131 (**64%**) | **$0.205** |
| `discovery.assemble` | 15,993tok → $0.080 (**53%**) | 2,345tok → $0.070 (47%) | **$0.150** |

**브리핑 1회 평균 $0.34** (범위 $0.29~0.43). ⚠️ 이 11건은 **각도 상한이 12였을 때** 데이터다.
상한 8 + 2단계 분리 이후 실측은 **07-27 1건뿐($0.389)** — 내역은 위 "지금 여기" 표.

- **두 호출의 비용 구조가 다르다.** 각도는 **출력**이 먹고(각도 개수·`why` 길이가 레버),
  조립은 **입력**이 먹는다(각도 개수 × 검색결과 5건 × 900자).
- **진짜 절감은 빈도다.** "한 번에 많이"의 이득은 "한 번만"에서 오지 "많이"에서 오지 않는다.
- 단가(2026-07-22 확인): gpt-5.5 $5/$30 (캐시입력 $0.50) · gpt-4o $2.50/$10 · gpt-4o-mini $0.15/$0.60.
- 실제 비용은 `rudy.llm_usage`에 호출별로, 응답별 합계는 `messages`·`utterances`의 `cost_usd`에 남는다.

**레버 순서 (측정된 것부터):**
1. **조립을 gpt-4o로 → $0.150 → $0.063 (-26%).** 제일 큼. **미실측**(§8-5).
2. **2단계 분리의 2단계 호출(§7-f)을 작은 모델로 → $0.035 → ~$0.002.** 입력 569토큰짜리
   좁은 변환이라 판단력이 필요 없는 자리다. 오픈소스·로컬 후보 1순위.
3. 각도 상한 10→8 — 이미 배포됨, 다음 브리핑에서 실측된다.
4. ❌ **각도를 gpt-4o로 내리지 마라.** §8-1 실측에서 확실히 나빴다(리스티클 미끼만 냈다).

**네이티브 web_search는 비싸다 (07-27 실측).** 항목 5개에 **$0.70** + web_search 15건 별도 =
항목당 $0.14. 기존 파이프라인은 항목당 $0.02 — **7배.** 검색 결과가 컨텍스트에 계속 쌓여
입력이 103,910토큰까지 갔다. 상시 경로로는 못 쓴다.
