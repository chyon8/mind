# RUDY-STATUS.md — 지금 상태 (세션 핸드오프)

> **이 파일만 읽는다.** `RUDY-ARCHIVE.md`는 열지 마라 — 끝났거나 대체된 계획이 가득해서
> 읽으면 틀린 추론을 한다. 2026-08-02에 새 세션이 정확히 그 사고를 냈다: 아카이브에 남아 있던
> "다음은 넛지" 블록을 읽고 1순위로 답했는데 **넛지는 8/1에 이미 끝나 있었다.**
> 유저가 "그때 왜 그렇게 정했나"를 물을 때만 연다.
>
> 설계=RUDY.md · **발견/브리핑 기준=RUDY-DISCOVERY.md(작업 전 필독)** · 지금 상태=여기.
> 마지막 갱신: 2026-08-03.
>
> **RUDY-DISCOVERY.md도 501→201줄로 잘랐다 (2026-08-02).** 개발 기록(실사용 튜닝 §7-b~§7-f ·
> 만료된 §0·§8 · Edge 전용 §10)은 `RUDY-DISCOVERY-ARCHIVE.md`로 뺐다 — 절 번호는 원본 유지,
> 코드 주석의 `§7-f` 같은 참조는 거기서 찾으면 된다. **발견 작업 전엔 여전히 RUDY-DISCOVERY.md만
> 읽으면 된다** — 아카이브는 안 열어도 됨.

## 다음에 개발할 것

**남은 확인·배포는 없다.** 판 A/B(떠오르기 4시간 갱신 · 파편 상세 칩 3개)와 아침 브리핑 4차는
코딩·배포·실기 검증이 **전부 끝났다**(유저 확인 2026-08-02). SQL도 다 붙여넣었다.
"실기 검증부터 하자"고 답하지 마라.

| 순위 | 무엇 | 왜 지금 | 착수 전 볼 것 |
|---|---|---|---|
| ~~1~~ | ~~막혀 있는 결정에 먼저 손 내밀기 (A3+D1)~~ | **완료 (2026-08-03, 커밋 `a420dd6`).** 아래 「1순위 상세」 참고 | — |
| **2** | **투두** — 앱 안, 파편과 완전 분리 | 백로그의 절반(구매 결정 지원 · 막연한 할 일 쪼개기)이 투두를 전제로 한다. 지금 Mind가 이슈 트래커로 쓰이며 깨져 있다 | 아래 「투두」 절 |
| **3** | **라이트 모드 · 리스트 엔티티** | Rudy가 아니라 앱 쪽. 오래 밀려 있다 | ⚠️ SPEC §7과 충돌 소지 — 착수 전 유저 확인 |

**지금 1순위는 2번(투두)이다.**

**잔업 (개발 아님, 몇 분짜리):**
- ~~`discover-websearch` 채택판의 턴 수 미측정~~ → **끝났다 (2026-08-02).** 아래 「사용량」 표 참고.
- **RUDY-DISCOVERY.md 축소가 항목 6→8개로 늘린 게 진짜인지 n=2로는 아직 가설.** 발견 돌릴 때
  항목 개수·`lens`/`resurface` 유무만 몇 번 더 눈여겨보면 된다. 개발 아님.
- **`idea` slot이 2회 연속 소재 절단에 성공했다** (8/1 1/2 → 8/2 2/2). RUDY-DISCOVERY-ARCHIVE §7-e가
  "네이티브 web_search 자유탐색이 마지막 시도, 실패하면 slot을 접는다"고 남겨뒀던 게 지금 그
  실험이다 — 몇 번 더 보고 §7-e에 결론(유지 확정)을 추가할 것.
- 채팅 모델 `gpt-4o` 올릴지 — 며칠 써보고 판단하기로 함(2026-07-31).

### ⚠️ 다음 할 일이 **아닌** 것 (반복해서 오답이 나온 자리)

- **넛지(A3) — 끝났다.** 2026-08-01에 **아침 브리핑 안의 카드**로 들어갔다.
  원래 계획(`recall.ts`에 슬롯 1칸 예약)은 **실측으로 기각**됐다: `touch_count` 분포가 63:4라
  "최저 가중치라 5배 덜 뽑힌다"는 전제가 이 코퍼스에서 거짓이었고, 떠오르기 랜덤 풀(36개)과
  넛지 후보(26개)가 **72% 겹쳐서** 이름만 바꿔 같은 걸 두 번 보여주는 꼴이었다.
  아침은 하루 1회라 빈도가 구조적으로 제한된다. **다시 recall에 붙이자고 하지 마라.**
- **주간 편지(§4-F5) — 안 만들기로 했다** (2026-08-02). 아침이 매일 제대로 나오면 같은 재료를
  한 번 더 훑는 중복이고, "주 1회 희소성이 기다림을 만든다"는 실측이 아니라 추정이었다.
  다시 꺼낼 조건: 아침을 며칠 돌려보고 "매일은 과하다"가 실제로 느껴질 때. 그땐 주기만 늘리면 된다.
- **아침 브리핑 만들기 버튼 / 발견 만들기 버튼 — 앱에서 뺐다.** 앱은 읽기만 한다.
- **막혀 있는 결정에 먼저 손 내밀기 (A3+D1) — 끝났다 (2026-08-03, 커밋 `a420dd6 "rudy almost done"`).**
  아래 「1순위 상세」에 실제 구현 기록. **다시 만들자고 하지 마라.**

### 1순위 상세 — 막혀 있는 결정에 먼저 손 내밀기 (완료, 2026-08-03)

A3(넛지)는 *"15일째 한 번도 안 건드렸어. 버릴까, 진짜 할까?"* 까지만 물었다.
안 건드린 이유가 **"리서치를 안 해서"** 일 수 있으니 한 단계 더 갔다 —
**"내가 대신 후보 찾아서 추려줄까?"** 넛지가 질문에서 그치지 않고 액션 제안(D1)으로 넘어가는 자리.

**붙인 자리:** `recall.ts`가 아니라 **아침 브리핑 카드**다(`src/app/morning.tsx`).
넛지 카드에 `link` 타입일 때만 "후보 찾아줄까" 칩을 붙였다 — text 타입(넛지 후보의 88%,
2026-08-03 실측)은 "바깥에서 후보 찾아줄까"가 말이 안 돼서 제외.

**실제 구현은 새 엔진이 아니라 기존 두 조각을 이은 것 + 그 과정에서 발견한 버그 하나를 고침:**

- 칩을 누르면 `fid`(파편 id)와 `mode=more_like`를 들고 채팅으로 진입. 문장은 **입력창에
  채워만 두고 자동 전송하지 않는다**(`draft` 파라미터). 바깥 검색은 되돌릴 수 없고 문장이
  기계 생성이라 고칠 자리가 필요했다 — 파편 상세의 `more like this` 칩도 같은 이유로 이번에
  `q`(자동전송) → `draft`(프리필)로 같이 바꿨다(`src/app/chat.tsx`, `src/app/fragment/[id].tsx`).
- **버그를 하나 잡았다 (`supabase/functions/chat/index.ts`):** `more_like`가 검색어를 뽑을
  재료가 원래 앱이 만든 질문 문장(제목 80자)뿐이었다. 실측: 앱 차단 앱 링크를 "퍼즐 게임"으로
  읽고 애니팡을 물어왔다(제목만 보고 설명·덧붙임을 안 봐서). 지금은 `fid`로 물린 파편 원본
  (제목/설명/내가 덧붙인 말)을 재료로 준다(`moreLikeMaterial`) — 설명이 종류를 정하고
  덧붙인 말이 각도가 된다는 걸 `MORE_LIKE_SYS` 프롬프트에 명시.
- 원장엔 안 적는다 — 이건 넛지에 대한 답이 아니라 답하기 위한 우회라, 갔다 와도 카드는
  그대로 남고 판단(흘려보내기/기억하기)은 여전히 따로 한다.

---

## 지금 돌아가는 것 (한 줄씩)

| 표면 | 어떻게 | 만드는 곳 |
|---|---|---|
| **아침 브리핑** | 파편 전량을 읽고 관찰 + 패턴 1개 + 질문 1개 + 넛지 | `node scripts/morning/run.mjs` (맥, 클코) |
| **발견** | 바깥에서 물어온다. 재시도 금지판 채택 | `node scripts/discover-websearch/run.mjs` (맥, 클코) |
| **채팅** | 파편 전량(≈386개, `·무덤` 표시) + 축 + 바깥. `gpt-4o` | Edge `chat` |
| **떠오르기** | 4시간마다 4개. 흘려보내기 쿨다운 7일, 최근 본 것 24개 제외 | 앱 로컬 |
| **파편 상세 칩** | `이거 관련 뭐 있었지`(안쪽) / `채팅하기`(자유) / `more like this`(바깥) | 앱 + Edge `chat` |

**앱은 이제 발견도 아침도 안 만든다. 읽기만 한다** (유저 지시 2026-08-02).
Edge `discovery` 함수와 `lib/rudy.ts streamBriefing`은 **안 지웠다** — 부르는 사람이 없으면
안 돈다. 되살릴 때 배포부터 다시 하지 않으려고 남겼다.

**꺼둔 것:** 자발적 연결(§4-C1) · 늦은 의도(§4-F1) — 2026-08-01, 로그로 오작동 확인 후.
`findLink`는 삭제, `intent.ts` 로직은 살아 있다(다시 부르면 됨).

---

## ✅ 결론 — 발견은 채택판(discover-websearch) 유지, 병렬판 실험 종료 (2026-08-06)

`npm run discover:compare`를 돌려 실측했다 (재료 29,319자 동일, `<이미 다룬 주제>` 250개):

| | discover (채택판) | discover:p (병렬판) |
|---|---|---|
| 벽시계 | 691초 | 261초 |
| API환산 | $1.62 | $2.18 |
| 항목 | 8개 (확장5·아이디어2·되꺼냄1) | 7개 (확장2·아이디어3·관점1·되꺼냄1) |

**병렬판이 2.6배 빠른데도 유저가 채택판을 골랐다** — "채택판이 훨씬 낫다"(품질 비교, 세부
근거는 안 남겼다). **속도보다 품질 판단이 이겼다.** `npm run discover`가 계속 운영판이다.

**병렬판(`scripts/discover-parallel/`)은 지운다:**

```bash
git rm -r scripts/discover-parallel scripts/compare-discover.mjs
```

패키지 스크립트(`discover:p`, `discover:compare`)도 `package.json`에서 같이 뺀다.
**되살릴 근거가 필요하면 git log의 `6e6a63c`·`2ef2245`에 코드·설계 이유가 그대로 있다** —
호출 13개로 쪼개 병렬 실행 + 중복 게이트를 넣은 실험이었고, 시간은 691→261초로 줄였지만
품질에서 졌다. 다시 만들 땐 이 커밋에서 시작하지 처음부터 새로 만들지 마라.

⚠️ **왜 코드로 안 지우고 여기 적어만 두나** — CLAUDE.md 「Git 워크플로우」: AI는 git 명령을
직접 실행하지 않는다. 위 명령을 복붙해서 지우고, 확인되면 `discover-parallel/` 관련 이
전체 절도 RUDY-STATUS에서 지워라(끝난 실험 기록은 여기 남길 이유가 없다 — git log가 원장이다).

### 처음 요청과 갈렸던 지점 (교훈)

유저 지시는 "gpt-5.5+Exa를 클로드코드로, 루프 없이 단발로"였는데, 실제로 만든 건 **에이전트
세션 하나(호출 1개, 순차)가 아니라 프로세스 13개짜리 병렬 구조**였다. 시간 단축에 꽂혀서
방향이 새 나갔다 — "단발"을 "호출 수 최소화"가 아니라 "재시도 루프 금지" 정도로 넓게
해석했다. 다음에 파이프라인을 다시 손댈 땐 **호출 개수 자체가 제약인지 먼저 확인할 것.**

---

## 아침 브리핑 (4차, 클코) — 2026-08-02

**한 번 주고 한 번 뽑고 끝난다.** `claude -p --allowedTools Read` 한 방. 루프도 검색도 없다.
`supabase/functions/morning/`은 **지웠다** — 재료 코드를 두 벌 두지 않으려고
(그 병은 `_discovery-lib.mjs`가 이미 앓았다).

### 3차(집계 전용 Edge)가 왜 틀렸나 — 실측

2026-08-01 브리핑 **네 문단 중 셋이 사실 오류**였다.

| 브리핑이 한 말 | 실제 | 원인 |
|---|---|---|
| "앞선 3주 평균보다 **네 배** 몰렸다" | 최근 7일 118개 / 그 앞 하루 26.8개 → **오히려 줄었다** | 생존편향 + 분모 |
| "최근엔 **link가 새로 섞였다**(이전 0)" | 이전 3주에 링크 44개 | 묻힌 걸 안 봄 |
| "채팅 개선이 **잦아드는 중**" | 7/29 하루에만 채팅 파편 4~5개 | 묻힌 걸 안 봄 |

**뿌리는 하나 — 모델이 파편을 0개 봤다.** 재료가 1,461토큰짜리 집계 블록이었다.

- **묻기는 "버림"이 아니라 "정리됨"이다.** 유저가 7/31에 직접 썼다 — *"아이디어를 넣어놓고
  한번 쭉 정리하면서 묻기하고있는데 이 워크플로우가 루디랑 맞나?"* 최근 건 정리 전이라
  살아 있고 옛것은 정리돼서 묻힌다. → **살아있는 것만 세면 무엇을 재든 "요즘이 폭발했다"가 나온다.**
  실측: 전체 386개 중 살아있는 게 58개(15%).
- **분모도 틀렸다.** `prior / 3` 고정인데 코퍼스가 7/16 시작이라 이전 창 21일 중 실데이터가
  10일뿐이었다. → 지금은 **데이터가 실제로 있는 일수**로 나눈다.
- **`.neq('status','done')` 때문에 Caselab이 이름조차 재료에 없었다.** 파편 0개를 "완전히
  멈췄다"로 오독했는데 실제로는 **완료**다. 상태를 안 보면 침묵과 종료가 똑같이 0이다.
- **축 3개는 임계 문제가 아니었다.** 0.42에서 3축, 살아있는 58개 중 9개 커버(전체의 2.3%).
  셋 다 최소 크기 3에 겨우 걸린 것. **노드가 58개뿐인 게 원인**이었다.

### 지금 구조

| | 3차 (Edge) | 4차 (클코) |
|---|---|---|
| 재료 | 집계 1,461토큰 · 파편 0개 | **전량 386개 43KB** · 묻힌 것 `(묻음)` 표시 |
| 축 | 임베딩 클러스터 + gpt-4o-mini 라벨 | **모델이 직접 묶는다** — `refs`(#번호)를 내면 코드가 시간모양 계산 |
| 억지 방지 | 파편을 안 준다 | **패턴 1개 + 가설 화법 + 근거 `refs` 표시** |
| 비용 | $0.02/회 | $0 (구독) |

**옮긴 이유는 비용이 아니다** — API로도 전량 25K토큰 = $0.15/회, 월 $4.5였다.
**재료를 다 읽을 수 있어서**다. 그리고 아침에 "만들기 버튼"이 있는 것 자체가 이상했다 —
아침은 열었을 때 이미 있어야 하는 물건이다.

**출력 3종:**
- **패턴 카드** — 하루 하나. 다섯 종류(반복되는 질문 / 지속되는 가치관 / 충돌하는 관심사 /
  사고방식 변화 / 호기심 변화) 중 하나. **근거 파편을 같이 그린다** — 두 개로 본 건지
  여덟 개로 본 건지 안 보이면 억지인지 알 수가 없다.
- **오늘의 질문** — 답하면 `rudy.evidence`에 자기 진술로 쌓인다. 그 테이블이 **1행뿐**이라
  (늦은 의도를 껐으니 안 쌓인다) `findAxes`의 `stated` 배관이 물 없이 돌고 있었다.
  넘기면 아무 일도 안 일어난다.
- **넛지** — 질문 문장은 모델이 안 쓴다. 서버 템플릿이라 "반드시 질문형·죄책감 금지"가 코드로 보장된다.

### 실측 (2026-08-01, 2회) — 유저 판정 "됐다"

1회차 `--no-save`: **3턴 · 131초 · API환산 $0.513**(구독이라 실청구 0).
2회차(저장): 결 8개 · 패턴 근거 5개 · 질문 1개.

3차가 원리상 못 하던 걸 했다:
- *"Caselab이 최근 7일 0개인 건 조용한 게 아니라 done이고, 글감은 paused다. 닫아둔 거지
  잦아든 게 아니다."*
- *"이번 주에 던진 것의 3분의 2를 묻었는데, Mind에서 남긴 건 발견을 어떻게 다르게 굴릴까
  쪽이 거의 전부고 묻은 쪽은 버그·비용·QA였다."* (실측 79/118 = 67%)
- 패턴: 7/28에 묻은 TouchGrass가 오늘 Steady_focus 프로젝트의 기능 후보로 다시 올라온 것 +
  7/18에 링크만 저장하고 묻은 acti를 오늘은 직접 써보고 UX를 뜯은 것.
- 축 8개(3차는 3개). `손으로 만들기`(PCB·아두이노·STM32·매크로패드·녹음기)처럼 임베딩
  클러스터가 절대 못 묶던 게 나왔다.

**결함 하나 — 고쳤다.** 본문에 `#N` 참조가 그대로 샜다(`페달과 기타(#5, #6)`).
그 번호는 재료 파일 안에서만 뜻이 있다. 프롬프트 금지 문단 + `run.mjs plain()`의 제거 정규식.

### 미결정 (유저가 정할 것)

- **넛지와 질문이 같은 날 둘 다 뜬다.** "하루에 질문 하나"로 갈라야 한다고 제안했지만
  유저가 결정한 적 없다. 아침에 숙제가 둘로 느껴지면 여기부터 본다.
- **`rudy.morning_edges` 함수가 DB에 남아 있다** (`collision_by_embedding`과 같은 처리).
  아무도 안 부른다. 임베딩 축으로 돌아갈 일이 생기면 SQL부터 다시 붙일 필요는 없다.

### 사용량 — 발견과 왜 다른가

| | 턴 | 시간 | API환산 |
|---|---|---|---|
| 발견 `discover-claude` 1차 (루프판) | 19 | 494초 | $1.53 |
| 발견 `discover-claude` 2차 (루프판) | 28 | 933초 | $2.30 |
| 발견 `discover-websearch` (**채택판**) 8/1 | 18 | 671초 | $1.678 |
| 발견 `discover-websearch` (**채택판**) 8/2, RUDY-DISCOVERY 분리 후 | **16** | **569초** | **$1.376** |
| **아침** | **3** | **131초** | **$0.513** |

**채택판 실측 완료 (2026-08-02).** 위 "미측정" 상태는 끝났다 — 세션 로그(`~/.claude/projects/...jsonl`)를
직접 까서 쟀다. **재시도를 꺼도 루프판보다는 훨씬 싸다**(28턴 $2.30 → 18턴 $1.68) 하지만
아침(3턴 $0.51)보다는 3배 이상 비싸다. **뿌리는 재시도가 아니라 바깥에 나가느냐였다** — 검색 결과가
턴마다 컨텍스트에 쌓여서 캐시읽기가 8/1 기준 528K토큰까지 자랐다(아침은 검색이 없어 컨텍스트가 안 자람).
이 줄로 §"추론(미검증)" 딱지를 뗀다.

**8/1→8/2 비교 (RUDY-DISCOVERY.md를 501→201줄로 자른 효과, 아래 참고):**
턴 18→16, $1.678→$1.376(**-18%**), 캐시쓰기 -18,582토큰. 재료 크기는 거의 동일(22,150→22,597자)
했으니 그 절감은 문서 축소로 설명된다. **같은 개편에서 항목 6→8개로 늘었다**(재료는 오히려
약간 더 빡빡해졌는데도) — §1의 "6~8은 상한이지 목표 아니다, 4개뿐인 날은 4개다" 브레이크를
지운 게 원인일 가능성이 높지만 n=2라 아직 가설이다. 며칠 더 보고 확정할 것.

**남는 레버 (미실측):** RUDY-DISCOVERY.md를 더 줄여도 이득은 이제 작다(재료 22K가 이미 문서
8K보다 크다). 다음 레버는 §"비용 — 확정된 사실"의 순서와 같다 — thinking(effort) 조정이 제일 크다.

---

## 절대 건드리지 말 것 (유저가 되돌리며 확정한 값)

- **`LENS_MAX = 2` · `IDEA_MIN = 2`(둘 다 프롬프트 요구).** 원래 "관점 한 개 넘지 않게"였는데
  **2026-07-31에 유저가 2로 올렸다** — 같은 날 `idea`를 "만들 만한 것" → **"누가 이미 만들어
  파는 물건"**으로 못박으면서, 밀려나는 오프라인 갈래(스파·텃밭·클래스·전시)를 받을 자리가
  필요해졌기 때문이다. 둘은 **한 세트다 — 하나만 되돌리지 마라.**
  ⚠️ "코드 컷"이라고 적혀 있었지만 **지금 코드에 `LENS_MAX` 상수가 없다**(전수 grep 0건).
  앱 경로는 07-27에 `lens`를 통째로 뺐고, 클코 판은 프롬프트로만 건다.
  **관점은 0개여도 된다** (유저 2026-07-28: "렌즈는 없어도 된다"). 상한이지 하한이 아니다 —
  lens가 0이라고 각도 상한을 다시 올리지 마라.
- **출처 링크 = 항목당 1~3개** (2026-07-31 유저 확정, 1개에서 되돌림). 조건은 "뒤 링크가
  앞과 **다른 걸** 보여줄 때만". 상세·되돌릴 신호는 `RUDY-DISCOVERY.md` §6.
- **`<이미 다룬 주제>` 힌트 = 250(사실상 전체).** 30으로 줄였다가 **되돌렸다** — 잘린 중복 6개가
  전부 힌트 밖이었다. **막는 게 잡는 것보다 6배 싸다**(입력 $5/1M vs 출력 $30/1M).
  30일 창이 자연 상한이라 무한히 안 자란다.
- **`fragLine`에서 `{id:}` 제거, URL은 유지.** URL은 유저가 승인한 적 없어서 복원했다 — 다시 빼지 마라.
- **`done` 프로젝트는 재료에서 제외** — ⚠️ **발견 재료에 한한 규칙이다.**
  **아침 브리핑은 정반대로 `done`을 반드시 포함한다** (2026-08-02) — 상태를 안 보면 침묵과
  종료가 똑같이 0으로 보여서 "Caselab이 완전히 멈췄다"고 오독했다. 한 규칙을 두 표면에 옮기지 마라.
  `paused`는 양쪽 다 남긴다(다시 시작 가능).
- **재료 구획 3분리**: `진행 중인 일`(active, 캡 2개) / `아이디어·수집함`(💡·글감·paused, 캡 없음) / `미소속`.
  **"프로젝트"라는 단어를 lists 구획에 쓰지 마라** — 라벨이 캡 소속을 정한다.
- **원장 저장은 조립 *전에*.** 되돌리면 07-26 사고(화면엔 나왔는데 기록 0건)가 재발한다.
- **잘 도는 것은 손대지 마라** (유저 명시): 각도 프롬프트의 렌즈·원리·few-shot·프로젝트 캡 2개,
  조립 프롬프트, 검색, 무덤, 회상, 축/orient, 재료 로딩 구조.

## 다시 제안하지 마라 (기각됨 — 이유 포함)

| 기각된 것 | 왜 |
|---|---|
| 검색어에 "구체적 이름·고유명사" 요구 | 유저: "고유명사 이딴 소리하지말고." idea 정의 + few-shot으로 충분했다 |
| 프롬프트에 규칙 계속 쌓기 | 3회 연속 규칙 추가 → 출력 4,077→5,053→6,475로 오르고 검색어가 막연해졌다. **막히면 빼는 방향으로 먼저** |
| 저장 시점 stance enum 인덱싱 | enum은 미리 예상한 속성만 잡고 새 속성마다 백필이 필요하다. 전수 스캔은 어떤 속성이든 잡는다 |
| 재료 고정 쿼터 (전량 읽기 교체) | 원리 C(북마크 × 프로젝트 겹치기)는 커버리지를 요구한다. "줄이 늘면 주의력이 나빠진다"는 추측이었지 실측이 아니었다 |
| 우선순위 위계표 | active가 위인 게 맞다. 캡 2개는 강등이 아니라 신선도 장치고 이미 해결했다 |
| 프롬프트 캐싱 + 재료 정렬 뒤집기 | OpenAI 자동 캐싱은 수 분~1시간. 하루 1회 브리핑엔 안 걸린다 |
| resurface 90일 창 제거 | 코퍼스 2주라 효과 0인 투기적 변경. 나중에도 회상 엔진(`recall.ts`) 후보 로직을 재사용하는 게 맞다 |
| 프로젝트 승격 기능 | 이미 있다(파편을 프로젝트에 붙이고 active로). 캡 2개라 승격해도 지분이 안 는다 |
| 브리핑 때 "뭐 찾아줄까" 묻기 | 신호 발생 시점과 묻는 시점이 어긋나고, 원탭 의식에 통행세가 붙는다 |
| 큐 소비 상한 / 7일 만료 | 각도 상한이 이미 자연히 막는다 |
| 무덤을 자발적 연결에 열기 | 유저: "무덤은 그냥 둬도 될 것 같아." 유저가 명시적으로 찾을 때만 나온다 |
| 아직 안 일어난 문제에 미리 장치 만들기 | 전부 "요청 안 했다"로 기각. **유저가 명시적으로 겪은 증상만 고쳐라** |
| 각도에 **갈래**(물건/소프트웨어/공간/문화/사람/일하는방식) 강제 배정 | 내가 만든 목록이었다. 유저: "이 갈래는 좀 쓰지마. 너맘대로 이거 그냥 다 삭제해버리고." 반복도 못 막았다(코리빙이 갈래를 바꿔가며 6회 중 5회) |
| **금지어 선언**시켜 소재 끊기 | 단어는 지키고 영역은 그대로 갔다. `사는 곳` 금지 선언 → `"creative residency" "community house"`. **단어를 막지 영역을 못 막는다** |
| 동기에 **행동 동사 금지** | 성공 개수는 그대로(6/10)인데 **전시·공간 갈래가 3→0으로 죽었다.** 상세 §7-f |
| idea 재료를 글감·인용구로 제한 | 유저: "프로덕트 아이디어로 나오는 건 좋은 거지. 무조건 소재를 끊는 데 집중해야." 재료를 좁히는 건 문제를 피하는 것 |
| 던진 발견 카드를 재료(`loadMaterial`)에서 출처 컬럼으로 걸러내기 (2026-07-28 시도·되돌림) | §7-f가 "루디 문장이 재료의 17%" 문제로 진단했지만, 처방이 틀렸다. 유저: "루디에 있는 걸 꼭 빼야 할 이유가 있나? 어쨌든 관심 있어서 던진 거고 새 아이디어일 수도 있는데." **던지기 자체가 가장 강한 관심 신호라 재료에서 빼면 신호를 버리는 것.** 진짜 문제(같은 걸 또 낸다)는 이미 `dedupeAngles` + `<이미 다룬 주제>` 힌트가 담당하는 영역 — 반복이 실제로 눈에 띌 때 그쪽을 보되, 출처로 통째로 배제하지 마라. 시도 흔적: `fragments.source` 컬럼이 실제 DB에 남아 있다(nullable, 아무 코드도 안 읽고 안 씀) — 지우고 싶으면 `alter table fragments drop column source;`, 안 지워도 무해하다 |

### 실패한 시도 — idea 게이트(임베딩) (2026-07-26)

`idea`의 `from`(「파편 → 동기」)에서 소재와 `query`를 임베딩 비교해 강등하려 했다. **실측 결과
신호가 없다:** 검색어는 영어·소재는 한국어라 sim이 전부 0.16~0.34에 깔리고, **품질이 나쁜 것이
좋은 것보다 낮게** 나왔다(순서 역전 → 어떤 임계로도 못 가른다). 언어 차이가 의미 차이를 덮었다.
→ **교훈 재확인: 판정이 필요하면 판정을 시켜라. 부산물(임베딩 거리)로 판정하지 마라.**

## 재발 방지 교훈 (전부 실제로 터진 것)

- **증상을 들으면 설명하기 전에 DB부터 연다.** 2026-07-26에 "5개만 나온 건 dedupe 때문"이라고
  확인 없이 말했다가 틀렸다(실제로는 조립이 잘림). 3초면 확인할 수 있었다.
- **진단 스크립트가 프로덕션 프롬프트를 import하지 않으면 다른 물건을 재는 것이다.**
  `check-idea-angles.mjs`는 `ANGLE_SYS` 대신 자체 프롬프트를 갖고 있었고, 그걸로 얻은 결론
  ("모델이 옆동네를 못 간다")이 문서에 확정처럼 박혔다. 실물엔 그 구조 자체가 없다.
  **새 진단 스크립트를 만들면 제일 먼저 "실물과 같은 프롬프트를 쓰는가"를 확인해라.**
- **실행 결과는 stdout에만 남으면 사라진다.** 07-26~27 실험 4건의 결과가 파일로 안 남아
  다음 세션이 "기억도 안 난다"가 됐다(대화 기록에서 겨우 복원). **진단 스크립트는 출력을
  파일로도 써라.**
- **재료를 의심해라 — 모델이 자기 출력을 읽고 있을 수 있다.** 브리핑 문장을 유저가 저장하면
  다음 브리핑의 재료가 된다. `fragments`에 출처 컬럼이 없어서 구별이 안 된다(§7-f).
- **"막는 장치"가 공급원이 될 수 있다.** `<이미 다룬 주제>`는 반복을 막으려고 넘기는 목록인데
  모델이 그걸 출발 재료로 썼다. 넘기는 모든 블록에 대해 "이게 재료로 읽힐 수 있나"를 물어라.
- **스트리밍에서 "사라짐"은 대개 셋 중 하나** — 버퍼 flush 누락 / 낙관적 화면 정리 / ScrollView `flex:1`.
- **PostgREST 다중 행 insert는 모든 행에 같은 키를 채워라.** 빠진 키를 default가 아니라 null로 채운다.
- **프롬프트 예시에 구체값(날짜·제목·수치)을 쓰면 모델이 복사한다.** 이 프로젝트에서 두 번 터졌다.
  예시엔 형식만 쓰고 값은 "블록에서 가져와라"로 지시할 것.
- **유사도 검색은 시간·개수·존재 여부를 못 답한다.** "있냐/없냐", "몇 개", "언제"는 다른 종류의 질의다.
- **시간대 버그는 한 곳만 고치면 반쪽이 남는다.** 경계(자르기)와 표시(찍기)는 다른 코드다.
- **"이게 무슨 종류인가"를 부산물로 판정하지 마라** (주제어 빈 배열 ≠ 메타 질문).
- **계산된 값으로 현상을 판정하기 전에 계산기를 의심해라.** 07-29에 `cost_usd`가 안 떨어지는 걸
  보고 "캐싱이 안 걸린다"고 적었는데, 실제로는 단가표에 캐시 입력가가 없어서 계산이 안 떨어질
  뿐이었다. **원본 관측치(`cached_tokens`)가 바로 옆 컬럼에 있었다** — 3초면 확인할 수 있었다.
- **외부 API는 이름이 약속하는 것과 실제 동작이 다르다.** 붙이기 전에 실제 데이터로 때려봐라
  (`findSimilar`는 미러·쇼핑몰만 물어왔다).
- **약한 지시로는 구조적 쏠림을 못 이긴다 — 숫자 캡은 코드로.** 단, 이건 캡을 코드로 옮기라는
  뜻이지 프롬프트에 문장을 쌓으라는 뜻이 **아니다.**
- **프롬프트로 모델의 마크업 습관을 막으려 하지 마라 — 파서가 견뎌야 한다.**
- **실패가 뜨면 코드가 아니라 판정(테스트·스크립트)이 틀렸을 가능성부터 확인할 것.**

## 진행 상태 (RUDY.md §10)

| § | 내용 | 상태 |
|---|---|---|
| 0 | 임베딩 파이프라인 · 링크 임베딩 품질 | ✅ |
| 1 | 링크 다이제스트 | ⏸ 보류 (표면 없음·검색은 이미 해결) |
| 2 | 하이브리드 의미검색 + 검색 UI | ✅ |
| 3 | 충돌 회상 + "왜 지금" | ✅ (임계 0.42) |
| 4 | 원장 + 게이트 로그 + 쿨다운 | ✅ |
| 5 | 채팅 RAG + 원탭 | ✅ (자발적 연결은 2026-08-01에 **껐다**) |
| 6 | 클러스터 엔진 → 패턴·경향 | ✅ 채팅에서 사용. **아침은 안 쓴다**(모델이 직접 묶는다) |
| 6+ | 늦은 의도 (F1) + `rudy.evidence` | ⚠️ **껐다.** evidence는 아침 질문이 채운다 |
| 7 | 발견 브리핑 | ✅ 클코(`discover-websearch`)로 이관, 앱 버튼 제거 |
| 8 | 아침 브리핑 (F4) + 넛지 (A3) + D1 결합 | ✅ 클코(`scripts/morning/`), 2026-08-02. D1(후보 찾아줄까) 결합 2026-08-03 |
| 9 | 주간 편지 (F5) | ❌ **안 만들기로 함** (2026-08-02, 위 「다음 할 일이 아닌 것」) |

**미착수:** 월말 성적표(§6-6) · 캘린더/지오펜스 어댑터(A2·D2가 이것 없이는 성립 안 함) ·
OpenAI Costs API 대조 스크립트(추정치 검증용, Admin 키 필요).

### ⚠️ 발견의 위상 — 유저 온도 (2026-07-28, 아직 유효)

유저: **"사실 발견에 크게 의미를 두고 있지 않아. 너무 비싸기도 하고. 무슨 의미인가 싶기도 하고."**

RUDY.md는 발견을 1급으로 놓지만 **유저의 실제 온도는 그렇지 않다.** 발견에 큰 작업을 새로
제안하기 전에 이걸 먼저 떠올려라. (클코 이관으로 API 비용은 0이 됐지만 온도는 별개다.)

### 나중으로 (유저 결정 2026-07-29 — 지금 꺼내지 마라)


| 미룬 것 | 이유 |
|---|---|
| **Life Dashboard** | ⚠️ 2026-08-02 갱신: 아침 브리핑이 이미 상태 그래프(선명도 지형·저장 리듬·관심의 결)를 그리고 있다 — **Life Dashboard가 뭘 더 하는 건지부터 유저에게 물어라.** ⚠️ **F5가 "대시보드가 아니라 서사. 숫자·통계·달성률 금지"를 명시한다** — 원칙 충돌이니 만들 때 유저에게 다시 확인할 것. 걸리는 건 숫자가 아니라 **점수**(연속일수·달성률·전주대비)다. "지금 흐려지는 것", "3주째 안 건드린 것"은 상태 표시라 안 걸린다. 대시보드(당기는 표면·상시)와 편지(받는 것·주1회·희소성)는 **대체재가 아니다** |
| **옵시디언 맥락** | 큰 작업. 단 방향은 잡혔다 — **RAG를 만들지 마라.** 클코 발견·아침이 로컬에서 도니 `Read`/`grep`으로 볼트를 인덱싱 없이 뒤질 수 있다. 채팅에 붙이면 전량이 안 들어가 임베딩 문제가 되돌아온다. 노이즈 우려는 실재(daily note에 회의록·할일이 섞임 — Mind 코퍼스가 개발 버그 메모로 망가진 것의 10배 규모) |
| **루디에게 남길 노트 / 사전** | 유저 결정: 지금 큰 도움 안 됨. ⚠️ **AI가 "이걸 만들면 발견이 나아진다"고 말했는데 근거가 없었다 — 유저가 정확히 지적했다.** 문서가 기록한 발견 헛발질 4건은 전부 **프로젝트 description 미독**이 원인이었고(원리 A) 그건 이미 고쳤다. **고유명사를 몰라서 발견이 나빠졌다는 실측은 0건이다.** 다시 제안하려면 실측부터. (유저 파편에 아이디어는 있다: 07-23 "product for two", 07-26 "AI용 주석 레이어") |
| **먼저 거는 말 되살리기** | 유저: "일단 보류하자" |

### 투두 — 앱 안에서, 완전히 분리 (**위 2순위**)

Things 연동 안 한다. **앱 안에서 하되 파편과 완전히 분리한다.** 파편 → 투두 변환은 가능.
근거: 유저 파편(07-29) *"띵스 안쓸바에야 그냥 이 안에서 투두관리하지"*.

**왜 분리가 필수인가:** Mind의 법칙은 **감쇠**(안 만지면 흐려지고 무덤)인데 todo의 법칙은
**정반대**(완료될 때까지 안 흐려져야 하고 오히려 시끄러워져야 한다). 한 통에 넣으면 하나가
반드시 깨지고, **지금은 todo가 깨져 있다** — 유저가 Mind를 이슈 트래커로 써서 07-16 저장분
43개(개발 버그 메모)가 전부 무덤으로 갔다. 그게 채팅이 멍청했던 원인의 절반이었다.

**보류 논의:** 채팅 라우팅을 tool-calling으로 재설계할지. 지금은 싼 모델이 5개 필드로 먼저
분류하고 그 상자만 태우는데, 분류가 틀리면 본답변이 아무리 좋아도 이미 틀린 재료를 받는다.
유저가 "A(소규모 개선) 먼저, 실사용해보고 판단"으로 미뤘다.

### 채팅 확장 아이디어 — 2026-07-31 논의 (백로그)

순서는 우선순위가 아니라 논의 순서.
**「막혀 있는 결정에 먼저 손 내밀기」는 완료됐다** (위 「1순위 상세」) — 나머지는 착수 아님.

- **채팅이 저장소를 고치는 자리로 (지금은 읽기 전용).** "이거 셋 다 케이스랩으로 묶어줘" /
  "이건 파편 아니고 버그 메모야, 빼줘"처럼 지시하면 D1 승인 게이트를 거쳐 반영.
  **가장 값이 큰 건 프로젝트 description 채우기** — 발견·채팅 헛발질 4건이 전부 description
  미독/빈약이 원인이었다(§나중으로 표). 루디가 질문만 던지고 유저 답을 §4-B2 자기 진술로 반영.
- **대화 로그를 코퍼스에 편입.** 지금 `messages`는 그 턴 맥락으로만 쓰이고 검색·충돌 대상이
  아니다 — "전에 얘기했던 그 구조 뭐였지"에 지금은 답 못 한다. ⚠️ 되먹임(루디 발화가 재료가
  되는 것)을 07-28에 이미 겪었으니(위 "되먹임 재현" 사고) 착수 전에 격리 방법부터 설계할 것.
- **채팅에서 즉석 발견 1개.** 지금 발견은 배치 8개짜리 의식(회당 $0.3~0.4)이라 유저 온도가
  낮다(§위 "발견의 위상"). 채팅 `<바깥>` 검색에 각도 생성 한 단계만 얹으면 항목 1개짜리
  발견이 된다(항목당 $0.02~0.14 수준). 발견의 값이 배치가 아니라 타이밍에 있었다면 이쪽이 본체.
- **구매·여행지 결정 지원.** 유저가 사고싶은 것/가보고 싶은 곳을 실제로 저장한다(prompt.ts
  예시로 쓸 만큼). 지금은 열거만 된다. 비슷한 걸 전에도 저장했나(반복 욕구 vs 스침) + 그 뒤
  무덤됐나 + 바깥 정보 겹치기 = 감쇠 이력이 있어야만 가능한 답. 투두 편입 이후 붙이면 자연스
  럽다: "스피커 사기" 투두 → 조건 인터뷰 → 검색·비교 → 옵션 제시. D1(액션 프레임워크)의
  구체적 인스턴스이지 새 구조는 아니다.
- **여행 리서치 (A2 확장).** A2는 지금 **저장해둔 파편**을 여행 전에 되살리는 것뿐 — 새로
  찾아주진 않는다. 캘린더에 여행 일정이 잡히면 목적지·예산·동행 조건을 인터뷰하고 코스·맛집·
  숙소를 **새로** 찾아 비교. 트리거만 캘린더고 골격은 위 "구매·여행지 결정 지원"과 같다
  (인터뷰→검색→비교). 구글 캘린더 어댑터(§3-1, §10-9) 있어야 성립.
- **남에게 줄 선물 (렌즈 뒤집기).** "A 생일선물 뭐줄까" — 지금 렌즈(§4-E3)는 전부 **내 취향**
  기준. 유저 파편 중 다른 사람에 대한 관찰("A는 미니멀 좋아함")이 있다면 그 이름이 걸린
  파편만 모아 그 사람 기준으로 렌즈를 다시 계산 — 로직 재사용, 대상만 교체. ⚠️ 재료(남에 대한
  메모)가 실제로 코퍼스에 충분한지부터 확인 필요, 착수 전 실측.
- ~~막혀 있는 결정에 먼저 손 내밀기 (A3 + D1 결합)~~ → **완료 (2026-08-03), 위 「1순위 상세」 참고.**
- **막연한 할 일 쪼개기.** 투두 편입 이후. "블로그 만들기"처럼 뭉툭한 항목을 넣으면 과거
  비슷한 프로젝트(케이스랩 등)의 진행 이력·description을 참고해 하위 단계로 쪼개 제안.
  새 재료 불필요 — 프로젝트 description + 진행 이력이 이미 있는 재료다.

## 배포 · 스크립트

**서버가 없다** (SPEC §3) — SQL은 Supabase SQL Editor에 사람이 직접 붙여넣는 게 정상 배포 경로다.
AI는 git 명령을 직접 실행하지 않는다 (CLAUDE.md §5).

**매일 도는 것 (유저가 손으로 친다. 자동화 안 한다).** 커맨드 원본은 레포 루트 `Rudy_Command.md`:
```bash
npm run morning              # 아침 브리핑 생성 + 원장 저장
npm run morning -- --material   # 재료만 뽑기 (LLM 안 태움, 공짜)
npm run morning -- --no-save    # 실험용, 저장 안 함
npm run discover             # 발견 (= scripts/discover-websearch/run.mjs)
```
⚠️ npm 스크립트라 **플래그 앞에 `--`가 하나 더 붙는다.** 없으면 npm이 먹는다.

**Edge Function 배포** (남은 건 `chat`뿐 — 발견·아침은 클코로 갔다):
```bash
# CLI가 PATH에 없고 프로젝트가 로컬 링크 안 돼 있다 — 반드시 이 형태여야 한다
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions list --project-ref ibqyqpmwqujcxlnkyizf   # 버전 확인
```

**진단 스크립트** (전부 로컬 node, service role 필요, `.env`에서 키를 읽는다):
`check-angles`(각도만 — 브리핑보다 싸다) · `check-brief`(검색+조립) · `check-idea-engines`(엔진 비교) ·
`check-chat`(chat E2E) · `check-collision`·`check-clusters`(임계 튜닝) · `check-ledger`·`check-intent` ·
`check-embeddings`·`check-search`.
⚠️ **프롬프트가 `scripts/_discovery-lib.mjs`·`check-brief.mjs`에 복제돼 있다** (Deno↔node 경계).
한쪽만 고치면 진단과 실물이 갈라진다.

## 비용 — 확정된 사실 (다시 계산하지 말 것)

**`rudy.llm_usage`를 request_id로 묶으면 나온다. 다시 돌리지 말고 쿼리해라.**
07-22~26 완전 실행 11건(각도+조립 둘 다 있는 것) 실측:

| | 평균 입력 | 평균 출력 | 평균 합계 |
|---|---|---|---|
| `discovery.angles` | 14,739tok → $0.074 (36%) | 4,368tok → $0.131 (**64%**) | **$0.205** |
| `discovery.assemble` | 15,993tok → $0.080 (**53%**) | 2,345tok → $0.070 (47%) | **$0.150** |

**브리핑 1회 평균 $0.34** (범위 $0.29~0.43). ⚠️ 이 11건은 **각도 상한이 12였을 때** 데이터다.
상한 8 + 2단계 분리 이후 실측은 **07-27 1건뿐($0.389)** — 내역은 위 "지금 여기" 표.

- **두 호출의 비용 구조가 다르다.** 각도는 **출력**이 먹고(각도 개수·`why` 길이가 레버),
  조립은 **입력**이 먹는다(각도 개수 × 검색결과 5건 × 900자).
- **진짜 절감은 빈도다.** "한 번에 많이"의 이득은 "한 번만"에서 오지 "많이"에서 오지 않는다.
- 단가(2026-07-22 확인): gpt-5.5 $5/$30 (캐시입력 $0.50) · gpt-4o $2.50/$10 · gpt-4o-mini $0.15/$0.60.
- 실제 비용은 `rudy.llm_usage`에 호출별로, 응답별 합계는 `messages`·`utterances`의 `cost_usd`에 남는다.

**레버 순서 (측정된 것부터):**
1. **조립을 gpt-4o로 → $0.150 → $0.063 (-26%).** 제일 큼. **미실측**(§8-5).
2. **2단계 분리의 2단계 호출(§7-f)을 작은 모델로 → $0.035 → ~$0.002.** 입력 569토큰짜리
   좁은 변환이라 판단력이 필요 없는 자리다. 오픈소스·로컬 후보 1순위.
3. 각도 상한 10→8 — 이미 배포됨, 다음 브리핑에서 실측된다.
4. ❌ **각도를 gpt-4o로 내리지 마라.** §8-1 실측에서 확실히 나빴다(리스티클 미끼만 냈다).

**네이티브 web_search는 비싸다 (07-27 실측).** 항목 5개에 **$0.70** + web_search 15건 별도 =
항목당 $0.14. 기존 파이프라인은 항목당 $0.02 — **7배.** 검색 결과가 컨텍스트에 계속 쌓여
입력이 103,910토큰까지 갔다. 상시 경로로는 못 쓴다.
