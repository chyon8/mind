npm run morning              # 아침 브리핑 생성 + 원장 저장
npm run morning -- --material   # 재료만 뽑기 (LLM 안 태움, 공짜)
npm run morning -- --no-save    # 실험용, 저장 안 함

npm run discover              # 발견 생성 + 원장 저장
npm run discover -- --material  # 재료만 뽑기 (공짜)
npm run discover -- --no-save   # 실험용, 저장 안 함

npm run discover:p