# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is on `expo ~54.0.0` / `react-native 0.81.5` / `react 19.1.0` (package.json).
**Keep this URL in sync with the installed major** — it pointed at v57 while the project was on
v54 (found 2026-08-09), which sends agents to docs for APIs this project does not have.
