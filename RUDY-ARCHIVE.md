# RUDY-ARCHIVE.md — 지난 세션 기록 (2026-07-19 ~ 07-26)

> # ⛔ 읽지 마라.
>
> **이 파일은 지시가 아니다.** 2026-07-26에 RUDY-STATUS.md가 1,163줄까지 자라서
> 세션마다 컨텍스트를 먹고, 그 결과 실제 DB·코드를 확인하는 대신 여기 적힌 서사로
> 추론하는 일이 반복됐다(유저 지적). 그래서 통째로 여기 보존하고 잘라냈다.
>
> **현재 상태·다음 할 일·확정값·기각 목록·교훈은 전부 RUDY-STATUS.md에 압축돼 있다.**
> 거기만 읽어라. 이 파일은 "그때 왜 그렇게 정했나"를 **유저가 물었을 때만** 열어본다.
> 여기 적힌 "다음 할 일 / 지금 당장 / 배포 필요"는 전부 **만료된 것**이다 — 따르지 마라.

---

# (원문) RUDY-STATUS.md — 빌드 진행 상황 (세션 핸드오프)

## 🔖 새 세션 시작점 (2026-07-26) — 브랜치 `rudy-discovery-fix`, 커밋 전

**지금 여기 있다:** `git checkout rudy-discovery-fix` (main엔 이 세션 변경이 하나도 없다 —
유저 지시: "main엔 안 올린다, 여기서만 남는다"). 이 세션 작업은 **아직 커밋 안 됨.**
커밋할 때 CLAUDE.md 절차대로 유저에게 git 명령어를 제공했고 유저가 직접 실행한다.

### 지금 당장 할 일 (순서대로)
1. **`git status`로 미커밋 diff 확인** — `_discovery-lib.mjs`·`check-*.mjs`·`discovery/{angles,brief,dedupe,material}.ts`
2. **`node scripts/check-angles.mjs` 한 번 더 돌려서 재현 확인** (~$0.26, 유저가 직접 실행 — 진단 스크립트지만 실비용 발생).
   기대값: **확장 4 · 아이디어 4 · 관점 1**, 중복 게이트 1개 안팎 제거. 이게 재현되면 그대로 커밋.
   **재현 안 되면 멈추고 유저에게 물어라 — 프롬프트를 또 고치러 들어가지 마라** (아래 "하지 말 것" 참고).
3. **커밋** (메시지 초안 아래 §"커밋 메시지"에)
4. **`discovery` 함수 배포는 아직 안 함.** `check-angles`(로컬 진단, DB 읽기 + OpenAI만)로만 검증했다 —
   실제 앱에 반영되려면 `npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf`가 필요하다.
5. **`chat` 함수도 아직 미배포·미검증.** 열거 경로("사고싶어한 것들") 코드는 커밋됐지만 한 번도 안 돌려봤다.

### 이번 세션에서 확정된 것 (건드리지 말 것 — 유저가 여러 번 되돌리며 확정한 값)

- **`idea` / `lens` slot.** `new`는 폐기(정의 없이 관점 슬롯으로 굳어 있었다). `idea` = "소재를 끊고
  동기를 잇는다"(파편 → 동기 형식 강제). **이 정의를 실측이 검증했다** — 키캡 각도가 정확히
  이렇게 작동했다("키캡 자체를 더 파면 반복이다").
- **`LENS_MAX = 1` (코드 컷)** · **`IDEA_MIN = 2` (프롬프트 요구 + 로그, 코드로는 못 만든다)**.
  유저 명시 지시: "아이디어 2개 이상최소. 관점 한개 넘지 안게해." 프롬프트 문구만으론 3개가
  나왔던 전례가 있어 코드로 잘랐다(`from_picked` 컷과 같은 패턴, 안전망 `MIN_ANGLES_AFTER_CUT=6` 포함).
- **`<이미 다룬 주제>` 힌트 = 250(사실상 전체, 30일 창이 상한).** 30개로 줄였다가 **되돌렸다** —
  잘린 중복 6개가 전부 힌트 밖(순번 31~134)이었다. **막는 게 잡는 것보다 6배 싸다**(입력 $5/1M
  vs 출력 $30/1M) — 파편 수와 무관하게 30일 창이 자연 상한이라 걱정한 "계속 자란다"는 틀렸다.
- **`{id:...}` 제거, URL은 유지.** id는 각도·조립 어디서도 안 읽어서 뺐다(재료의 28%, 파편 수
  비례 성장). **URL은 유저가 승인한 적 없어서 되돌렸다** — 뺐다가 지적받고 복원함. 다시 빼지 마라.
- **`done` 프로젝트는 재료에서 제외.** `paused`는 남긴다(다시 시작 가능).
- **재료 구획 3분리**: `진행 중인 일(active만, 캡 대상)` / `아이디어·수집함(💡·글감·paused, 캡 없음)`
  / `미소속`. **"프로젝트"라는 단어를 lists 구획에 안 쓴다** — 라벨 자체가 캡 소속을 정하므로.
- **임베딩 중복 게이트(`dedupe.ts`)**: 검색 전에 각도를 임베딩해 최근 브리핑 제목과 비교,
  `≥0.6`이면 컷. 설계(§6-4)엔 있었고 구현이 없던 것. 안전망: 4개 미만으로 깎이면 컷 포기.
- **스크립트가 실제 usage를 찍는다** (`callOpenAI`가 전엔 버렸다). `check-angles` 3회 실측:
  입력 12~13k, **출력이 비용의 60~70%를 차지** — 각도 개수·`why` 길이가 비용의 진짜 레버다.

### 하지 말 것 (이 세션에서 시도했다가 유저가 되돌린 것 — 다시 제안하지 마라)

- ❌ **검색어에 "구체적 이름·고유명사" 요구 문구.** 넣었다가 **즉시 되돌렸다.**
  유저: "고유명사 이딴 소리하지말고." 대신 idea 정의("동기로 뻗어라") + few-shot 하나로 충분했다.
- ❌ **프롬프트에 규칙을 계속 쌓기.** 3회 연속 규칙을 추가했더니(구성 비율 명시→코드 컷 추가→
  고유명사 요구) 출력 토큰이 4,077→5,053→6,475로 계속 오르고 각도 검색어가 점점 막연해졌다.
  **§7-b 교훈 "숫자 캡이라야 이긴다"는 캡을 코드로 옮기라는 뜻이지, 프롬프트 문장을 쌓으라는
  뜻이 아니다.** 프롬프트가 좋았던 시점(각도 4개, 확장2·새로움2, 고유명사 박힌 검색어)이 있었다 —
  규칙을 더 넣을 때마다 그때보다 나빠졌다. **막히면 빼는 방향으로 먼저 시도해라.**
- ❌ **아직 안 일어난 문제에 미리 장치를 만드는 것.** 큐 소비 상한·만료, 우선순위 위계표,
  프로젝트 승격 기능, resurface 창 제거, 재료 정렬 뒤집기(캐싱 과대평가) — 전부 제안했다가
  "요청 안 했다"는 지적으로 기각. **유저가 명시적으로 겪은 증상만 고쳐라.**

### 알려진 채로 남겨둔 것 (건드리지 말고, 재발하면 그때 보고)

- **게이트가 한 번 뚫린 사례** — "AI 녹음기" 각도가 0.61(임계 0.6 바로 위)로 통과한 적이 있다.
  `why` 문구를 다르게 쓰면 유사도가 임계 밑으로 내려갈 수 있다는 뜻. 재발하면 임계를 올리거나
  각도 텍스트 구성(query만 비교 등)을 조정한다 — 지금은 한 번뿐이라 손대지 않았다.

### 이번 세션에서 완료 (커밋 대기 중)

①②③(이전 세션 계획, 세이브포인트 `e711077`에 이미 커밋됨):
발견 표시 버튼(`discover_next`) · 채팅 열거 경로(`intent: 'enumerate'`) · 브리핑 항목 상한 8→10.

이번 세션 추가분(미커밋):
- 재료 구획 분리(`projects`/`lists`/`loose`) + `done` 제외 (`023ac41`에 커밋됨)
- `idea`/`lens` slot 재설계 + 코드 컷(`LENS_MAX`/`IDEA_MIN`) + 임베딩 중복 게이트(`dedupe.ts` 신규)
  + 힌트 250 + id 제거 + usage 실측 출력 (**미커밋** — 위 "지금 당장 할 일" 참고)

### 다음 순서 (이번 세션 미착수)

1. 위 커밋·배포·검증 마무리
2. **Step 3 — 채팅에 프로젝트 `description` 싣기.** 지금 채팅은 파편만 검색하고 프로젝트
   설명을 안 읽는다 — 발견이 이미 겪은 헛발질(원리 A: No phone을 미니멀폰으로 오독)을
   채팅에서 반복할 수 있다. `projects(id, name)` → `projects(id, name, description)`로 컬럼 추가.
3. **조립 모델 gpt-4o 실측** (`check-idea-engines.mjs`, opus 모드도 있음). 각도는 실측으로
   gpt-5.5 확정이지만 **조립은 검증된 적이 없다.** 이번 세션 실측으로 출력이 비용의 60~70%인 게
   확인됐으니 우선순위가 올라갔다 — 조립 출력이 회당 비용의 큰 부분을 차지한다.
4. main 병합 여부 판단 (위 전부 검증 끝난 뒤)

---

## ⛔ 아래는 낡은 계획 — 위 §"새 세션 시작점"으로 대체됨 (지시로 읽지 마라)

## 🎯 확정 계획 (2026-07-25) — 이것부터 한다. 유저 컨펌 완료

> **먼저 아래 §"기각된 설계"를 읽어라.** 이 계획은 유저와 5회 왕복 검토해서 깎아낸 결과다.
> 기각 목록을 안 읽으면 이미 버린 설계를 또 제안하게 된다(실제로 이 세션에서 4번 그랬다).
>
> **유저의 목표는 셋뿐이다:** ① 내가 지정한 파편이 발견에 나오게 ② 채팅이 질문 의도를 제대로
> 파악 ③ 브리핑 항목 2개 더. **브리핑 퀄리티는 "나쁘지 않다"가 유저 판정이다** — 잘 도는 것
> (각도 프롬프트의 렌즈·원리·few-shot·프로젝트 캡 2개, 조립 프롬프트, 검색, 무덤, 회상, 축/orient,
> 재료 로딩 구조)은 **절대 건드리지 않는다.** 유저 명시: "원래 잘 되는건 절대로 건들지마."

### ① 발견 표시 버튼 — "다음 발견에 포함"

유저가 첫 메시지에서 직접 설계한 것: "체크만 해놓고 브리핑 돌릴 때 최신 상태로 읽기".

- **SQL (일회성 배포)** — `alter table fragments add column discover_next boolean not null default false;`
- **UI** — 파편 상세의 루디 칩 줄(`src/app/fragment/[id].tsx` 267행 부근, `rudyQuestions` 렌더 자리)에
  토글 버튼 1개. **최대 5개** — 누를 때 현재 표시 수를 세고 5개면 "5개까지"만 알린다.
- **재료** — `loadMaterial`이 `discover_next=true`를 따로 싣고, `materialBlock` **맨 위**에
  `<내가 지정한 것>` 구획으로 넣는다.
- **각도 프롬프트 1줄** — `ANGLE_SYS`에 "`<내가 지정한 것>`이 주어지면 유저가 직접 지정한 것이다 —
  반드시 각도로 만들어라." ⚠️ `scripts/check-angles.mjs`의 ANGLE_SYS 복제본도 같이 고친다.
- **소비 = 브리핑 1회 후 전부 해제.** `streamBrief`가 원장 insert를 마친 뒤
  `update fragments set discover_next=false where discover_next=true`.
  ⚠️ **어느 것이 실제로 각도가 됐는지는 알 수 없다** — 각도 출력의 `from`은 자유 문장이고 id가 없다.
  그래서 전부 해제한다. 또 원하고 싶으면 또 누른다. **유저가 승인한 동작이다**("한 번만 원하는
  걸 수도 있잖아" → 한 번 나오고 자동 해제가 기본값).
- **상한·만료 장치를 새로 만들지 않는다.** 각도가 이미 `angles.ts`에서 최대 개수로 잘리므로
  20개를 표시해도 그 안에서 정리된다. 기존 장치가 이미 막고 있다.

**§2-3 검증 완료 (새 세션이 다시 의심할 자리):** 이 플래그는 `public.fragments`에 쓰기를 하지만
`last_touched_at`·`touch_count`를 **안 건드린다** → "그냥 봤다고 선명해지면 안 된다"는 안 깨진다.
근거: `updateFragment`(`src/lib/supabase.ts:480`)는 자동 touch가 없는 순수 patch고,
embed 웹훅은 UPDATE로 깨어나도 `source_hash`가 같아 재임베딩을 스킵한다(`embed/index.ts:40`).

### ② 채팅 — 속성 열거 질의를 검색 대신 전수 스캔으로

**증상:** "내가 사고싶어한 것들이 뭐였지" → 조명만 나오고 스피커·데스크셋업은 못 나온다.
**원인:** `EXTRACT_SYS`가 topics를 못 뽑아(구체적 소재가 없다) → `queries=[question]`으로 떨어짐
(`chat/index.ts:382`) → 질문 임베딩에 남는 신호가 "사고싶다"라는 **서술어뿐**이라, 그 말이 문자로
적힌 파편만 걸린다. 스피커 링크 임베딩은 "스피커 제품"이라 말하고 "사고 싶었다"는 어디에도 없다.
**검색 튜닝으로 못 고친다 — 인덱스에 그 정보가 없다.**

- `EXTRACT_SYS`의 `intent`에 **`enumerate`** 추가. 정의 = 저장물 중 **어떤 속성을 가진 것을
  열거**해달라는 질문. 예: "사고싶어한 것들", "가보고 싶다던 데", "내가 비판했던 것들".
  ⚠️ **period와 반드시 구분** — period는 **시간**으로 자르고("오늘 뭐 저장했지"), enumerate는
  **속성**으로 자른다. 기존 trend/orient/period 구분 예시와 같은 패턴으로 문구를 박을 것.
- **라우팅**: period 분기와 같은 자리(`chat/index.ts:389` 부근). `intent==='enumerate'`면
  유사도 검색을 **안 하고** 전 파편을 한 줄씩(날짜 | 제목 | 덧) 근거로 넘긴다.
  **archived 포함** — "찾으러 온 행위는 무덤도 뒤진다"(`rudy-search.sql:3`) 규칙 계승.
- **프롬프트**: "전수 목록이다. 조건에 맞는 걸 골라라. **확실한 것과 추측을 구분해 말해라.**"
  (유저가 원한 건 확신이 아니라 의미 파악이다.)
- **이게 period를 고친 것과 똑같은 수술이다** — `chat/index.ts:390`의 "시간 질의는 검색으로 답할
  수 없다"를 속성 축으로 확장한 것. 그 교훈을 시간에만 적용하고 끝냈던 게 이 버그의 뿌리다.
- **비용**: 그 종류 질문만 $0.005 → **$0.02**(파편 127개 ≈ 6k 토큰). 나머지 질문 불변.
  ⚠️ **파편 수에 정비례한다** — 500개면 질문당 $0.08. 그때 싼 모델로 1차 필터를 걸고 걸러진
  것만 본답변에 넘기면 다시 $0.01로 내려간다. **지금은 만들지 않는다**(유저: 이 질문은 "간혹").

### ③ 브리핑 항목 상한 6~8 → 8~10

유저: "콘텐츠 2개 정도 더 늘리는 건 괜찮을 것 같아. 부족해서." 비용 +$0.04/회.

고칠 곳 (복제본 포함 — 한쪽만 고치면 진단과 실물이 갈라진다):
- `discovery/angles.ts:57` ANGLE_SYS 문구 "6~8개는 상한이지 목표가 아니다"
- `discovery/angles.ts:101` `.slice(0, 8)` → 10
- `discovery/brief.ts:29` ASSEMBLE_SYS 문구 "6~8개가 상한"
- `scripts/check-brief.mjs:30` (ASSEMBLE_SYS 복제본)
- `scripts/check-angles.mjs` (ANGLE_SYS 복제본 — grep에 안 걸렸으니 실제 문구를 확인해서 맞춰라)

⚠️ **상한이 개수를 보장하지 않는다.** 프롬프트가 "상한이지 목표가 아니다 / 억지로 채우면 쓰레기가
섞인다"(§2-8)라서, 지금 6개 나오고 있으면 상한을 올려도 6개다. **먼저 최근 브리핑들의 `###` 개수를
세라.** 8에 붙어 있으면 상한이 병목 → 인상이 듣는다. 5~6이면 병목은 재료·각도 쪽이고 상한 인상은
효과가 없다 — 그 경우 유저에게 알리고 ④를 검토한다.

### ④·⑤ 미결정 — 새 세션이 유저에게 먼저 물을 것

유저가 답을 안 하고 세션이 끝났다. **넣었다고 가정하지 마라.**

- **④ 💡 한 줄 (아이디어가 안 긁히는 문제).** 유저 원래 질문이 "아이디어 쪽은 잘 안 긁나?"였다.
  **표시 버튼(①)은 이걸 수동으로만 푼다 — 지정 안 하면 오늘과 똑같이 안 긁힌다.** 기계적 원인은
  찾았다(→ RUDY-DISCOVERY.md §7-c). 최소 수정은 ANGLE_SYS **한 줄**:
  "💡·미착수 아이디어는 미소속과 같은 취급 — 여기서 뽑아도 '절반 이상'에 포함된다."
  재료 구조·few-shot·각도 성격 규정은 **건드리지 않는다**(전부 기각됨, 아래 참조).
- **⑤ `fragLine`의 `{id:...}` 제거.** 파편마다 UUID 36자를 찍는데 각도·조립 어디서도 안 쓴다
  (각도 출력의 출처는 자유 문장). **재료의 약 17%**, 위험 0, ③으로 늘어난 비용을 상쇄한다.
  유저가 "잘 되는 건 건들지 마"에 걸린다고 보면 빼기로 열어둔 상태.

### 배포 (①②③ 전부)

```bash
# SQL: ①의 alter table — Supabase SQL Editor에 붙여넣기 (일회성 배포)
npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf   # ①③
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf        # ②
```

### 기각된 설계 — 다시 제안하지 마라 (2026-07-25, 이유 포함)

이 세션에서 내가 제안했다가 유저 지적으로 **철회한 것들**이다. 전부 "안 물어본 걸 넣었거나"
"잘 되는 걸 교체하려 한" 경우다. 유저 명시: "무조건 복잡하게 하는 건 답이 아니야."

| 기각된 것 | 왜 |
|---|---|
| **저장 시점 stance enum 인덱싱** (파편에 사고싶다/하고싶다 태그 + 새 테이블 + 백필) | 유저 지적이 결정타: "사고싶다·하고싶다 외에 다른 게 나오면 어차피 못 잡는 거 아닌가". 맞다 — enum은 **미리 예상한 속성만** 잡고 새 속성마다 백필이 필요하다. ②의 전수 스캔은 판정자가 질문을 들고 전체를 읽으므로 **어떤 속성이든** 잡힌다 |
| **재료 고정 쿼터** (90줄 선별로 전량 읽기 교체) | 원리 C(북마크 × 프로젝트 겹치기)는 **커버리지**를 요구한다. 미소속을 자르면 Cluely류 히트를 구조적으로 못 만든다. RUDY-DISCOVERY §5의 "'추가'를 '교체'로 읽지 마라"를 그대로 재현한 실수. "줄이 늘면 주의력이 나빠진다"는 **내 추측이고 실측이 아니었다** |
| **재료 4구획 분리 / tier·touch_count 싣기 / 💡 확장 성격 규정 / few-shot 추가** | 유저가 요청하지 않았다. 특히 "💡는 경쟁 제품 조사가 아니라 최소 버전을 물어라"는 유저가 명시 거부: "경쟁 제품 조사도 괜찮은데. 알아서 그때그때 판단하게 하면 안 되나" — **모델이 그때그때 판단하게 둔다** |
| **우선순위 위계표** (큐 > pinned > touch_count > active 프로젝트…) | 유저: "진행중 프로젝트를 상위로 올리는 것도 맞나, 뭐 그게 우선순위기는 하니까" → **active가 위인 게 맞다.** §7-b가 잡은 건 *반복* 문제였고 캡 2개로 이미 해결됐다. 캡은 강등이 아니라 신선도 장치다. 우선순위 문제가 아니었다 |
| **프롬프트 캐싱 + 재료 정렬 뒤집기** | OpenAI 자동 캐싱 유지 시간이 **수 분~1시간**이다. 하루 1회 브리핑엔 안 걸린다(12번 연타할 때나 걸림 = 막으려는 상황). 내가 효과를 과장했고, 그걸 근거로 "최근 우선"이라는 잘 도는 지시와 싸우는 변경까지 제안했었다 |
| **resurface 90일 창 제거** | 지금 코퍼스 2주라 **효과 0**인 투기적 변경(CLAUDE.md §2 위반). 그리고 나중에도 이게 맞는 수술이 아니다 — 회상 엔진(`recall.ts`)이 이미 제대로 하니 그때는 그 후보 로직을 재사용한다 |
| **`<이미 저장>` 임베딩 dedup** | O(n) 성장은 실재하지만(파편 500개면 조립 입력 +12k) 지금 고치면 복잡도를 미리 사는 것. 90일 창 덕에 당분간 완만하다. **파편 수백 개 시점의 레버로만 적어둔다** |
| **프로젝트 승격 기능** | 이미 있다 — 파편을 프로젝트에 붙이고 active로 바꾸는 게 승격이다. 게다가 캡 2개라 승격해도 브리핑 지분이 안 늘어난다 |
| **브리핑 돌릴 때 루디가 "뭐 찾아줄까" 묻기** | 유저 직감이 맞았다("이건 좀 그런가"): 신호가 생기는 순간(파편 볼 때)과 묻는 순간(브리핑 누를 때)이 어긋나고, 원탭 의식에 통행세가 붙고, 타이핑으로 답하는 건 범용 챗봇 동작이다 |
| **큐 소비 상한 2개 / 7일 만료** | 각도 상한이 이미 자연히 막는다. 새 장치 불필요 |
| **무덤을 자발적 연결에 열기** | 유저: "무덤은 뭐 그냥 둬도 될 것 같아." 현행 유지 — 무덤은 유저가 명시적으로 찾을 때(검색·채팅 검색)만 나오고 루디가 스스로 꺼내는 경로에선 전부 제외 |

### 비용 — 확정된 사실 (다시 계산하지 말 것)

브리핑 1회 ~$0.30. 구조는 **실행당 고정비 ~$0.07**(재료 읽기 + 시스템 프롬프트) +
**항목당 ~$0.02**(Exa 검색 + 검색결과 읽기 + 출력). 즉 **비용의 대부분이 항목당이다.**

- 유저 질문 "여러 번 돌리는 것보다 한 번에 많이가 비용 효율적인가?" → **맞지만 16%뿐이다**
  (7항목×2회 $0.41 vs 14항목×1회 $0.35). 고정비가 작아서 묶어도 아끼는 게 적다.
- **진짜 절감은 빈도다** — 7항목 1회 = $0.21 (2회 대비 50%). "한 번에 많이"의 이득은 "한 번만"에서
  오는 거고 "많이"에서 오는 게 아니다. 개수 인상은 절감이 아니라 항목당 $0.02씩 **지출 증가**다.
- **비용의 본체는 gpt-5.5 두 호출(~$0.17/회)이고, 그건 아래 Step 2의 몫이다.**
  ⚠️ 각도는 실측으로 5.5 확정이지만 **조립은 실측된 적이 없다**(§8.1의 "조립도 같은 모델이어야"는
  추론이지 측정이 아니다). 조립의 일은 좁다 — 읽고 거르고 세 문장 쓰기. 여기가 최대 미검증 레버.

---

## 📌 Step 2 — 모델·서치 실측 테스트 (2026-07-23 작성 · 여전히 유효, 위 확정 계획 다음)

> **2026-07-25 유저 확인:** "우선은 우선순위를 먼저 하고, 그 다음에 내가 계획해둔 테스트가 있거든."
> → 이 섹션은 살아 있다. **위 ①②③ 다음에 한다.**
> 순서 근거: 이 하네스는 **같은 각도로** 엔진만 바꿔 비교하는데, ①이 재료에 `<내가 지정한 것>`을
> 추가하므로 그걸 먼저 넣어야 테스트 입력이 최종 형태가 된다. 그리고 ①②③은 재료·프롬프트 층이라
> 모델·검색 제공자를 뭘로 갈아타도 재작업이 없다.
> ⚠️ 아래 **C(오늘 만든 것 마무리)는 2026-07-22 기준**이고 이후 커밋(`c93b543` 등)이 있어
> 대부분 처리됐을 것 — 착수 전 실제 상태를 확인할 것.

**주제: "바깥에서 아이디어 찾기" 품질을 실측으로 개선.** 유저가 제일 자주 쓸 케이스(2026-07-22
실사용: 채팅 outward가 osmu·tistory 개념설명 블로그만 물어옴, 발견 브리핑이 Cluely·Revu 실물을
문 것과 대비). **결정 단계 아님 — 옵션 다 벌려놓고 데이터로 고르는 단계.** 편견 갖지 말 것.

### 확장의 세 가지 결 (유저 머릿속 정리 — 섞지 말 것)
1. **파편 × 프로젝트 겹치기** — 개별 저장물에서 각도. **현재 발견 엔진.**
2. **관심사 프로파일 확장** — "이 사람은 개발 프로덕트에 관심" 같은 추상 성향에서 확장.
   **2026-07-22 유저 신규 아이디어.** 파편에 안 묶여 더 신선할 수 있음. 위험: 너무 추상적이면
   "AI 개발자용 툴 10선" 리스티클로 빠짐. 각도 프롬프트를 "재료 읽기"가 아니라 "관심사 추출→확장"
   으로 새로 짜야 함. **테스트 대상.**
3. **맥락 없는 순수 검색** — 렌즈 없이. 루디 정체성과 상충하지만 유저가 "궁금, 실험해보고 싶다".

### A. 실측 테스트 — `scripts/check-idea-engines.mjs` (오늘 만듦, 아직 실행 안 함)
파편 기반 브리핑을 **같은 각도로** 엔진만 바꿔 나란히 뽑는다(각도는 gpt-5.5로 한 번만, 전 모드 공유).
- A `auto`: Exa type=auto + gpt-4o 조립 (현재 프로덕션)
- B `domain`: Exa, HN·IndieHackers·Reddit 도메인 타겟 (렌즈에 이미 적힌 소스 결)
- C `company`: Exa category=company
- D `opus`: A와 동일 검색 결과 + **Claude Opus 조립** (병목이 검색이냐 조립이냐를 가른다)
- ⬜ E: Claude 네이티브 web_search / ⬜ F: GPT 네이티브 web_search — **코드 없음.** 각도+검색+거름이
  한 호출에 통합돼 A~D와 구조가 다르다 → API 형태부터 확인하고 짤 것.
- ⬜ 모드 ②(관심사 확장), ③(맥락 없는 검색)도 하네스에 모드로 추가 검토.
- **유저가 먼저 할 것: `.env`에 `ANTHROPIC_API_KEY` 추가**(D 필수, console.anthropic.com). 크레딧 채움 완료.
  실행: `node scripts/check-idea-engines.mjs` (전부) 또는 `... domain`(하나).

### B. 논의·결정 (실측 데이터 보고)
- **"아이디어만" 브리핑 섹션** — 별도 표면 vs 브리핑 안 구분선(아이디어 4~5개 진하게).
  내 의견: 섹션. **단 "최근 저장·미소속 우선" 규칙은 유지**해야 RUDY-DISCOVERY가 이미 잡은
  "같은 프로젝트 재탕" 버그가 재발 안 함(그 캡 "최대 2개"의 이유가 이거지 "아이디어 과다"가 아니다).
- **"바깥 아이디어" 채팅 칩** — 테스트 결과 보고 뭘로 채울지 정한 뒤 구현. (칩 자체는 아직 안 만듦.)
- **think 버튼**(고급 모델 수동 토글) — 필요한지 판단. ⚠️ 모델 교체와 파이프라인(각도+거름) 개선은
  다른 축 — think로 gpt-5.5 켜도 재료가 블로그면 소용없다는 것을 유저와 확인 완료.

### C. 오늘 만든 것 마무리 (커밋·배포·검증)
- **§10-9 orient — 미커밋·미배포.** chat 함수 재배포 필요(chat은 아직 v20=07-22, orient 안 올라감).
- 오늘 미커밋 파일: `chat/{index,prompt,orient}.ts`·`_shared/cluster.ts`(감쇠 복제본 버그 수정)·
  `discovery.tsx`(던지기 프로젝트 칩)·`supabase.ts`·`check-costs.mjs`·`check-idea-engines.mjs`·이 문서.
- 크레딧 채웠으니 실사용 검증: 비용 캡션·OpenAI 로그(call_site 필터)·orient 라우팅·던지기 칩.

---

## ⏸ 보류 논의 — "후반부에 다시" (유저 지시, 2026-07-22)

**채팅의 질문 라우팅 구조 자체를 바꿀지.** 지금은 질의 재작성(mini)이 질문을 먼저 5개 필드
(topics/type/period/intent/outward)로 분류하고, 그 상자(시간조회/축/검색)만 태운다. 문제:
① 분류가 틀리면 본답변(gpt-4o)이 아무리 좋아도 이미 틀린 재료를 받는다, ② 그 분류를 제일 싼
모델이 한다. 실측: 유저가 "내가 오늘 봐야할게 있나? 생각할거아" → 바로 다음 메시지 "오늘 저장한거
아니어도 그냥 전체적으로"로 스스로 교정 — orient성 질문이 period(시간조회)로 잘못 라우팅된
실제 증거.

**대안 B(재설계) — tool-calling.** 질문을 통째로 모델에 주고 검색/축/기간조회/바깥을 도구로
쥐여줘서 모델이 스스로 고르게. 범용 AI의 유연함 + 루디의 grounding을 같이 얻지만, 큰 리팩터고
지금 잘 도는 것(시간조회 버그 수리 등)을 다시 흔들 위험.

**지금은 A(소규모 개선)만 하기로 함** — §10-9 orient 경로 추가(아래) 하나만 이번에 하고,
B는 A 이후 실사용해보고 라우팅이 실제로 얼마나 자주 틀리는지 본 뒤 판단하기로 유저가 미룸.

## §10-9 orient — 구현 완료 (2026-07-22), 배포·기기 검증 대기

**설계대로 구현.** `chat/orient.ts`(신규) = 축×흐림 교집합. 라우팅은 `intent==='orient'`일 때
(topics 없음·answered 아님·period 아님 셋 다) `buildOrient` 호출, `<오늘>` 블록으로 넣는다.
프롬프트에 `<오늘>` 지시문(한두 개만·처방 금지·없으면 없다고). gate_log에 `kind='orient'` 판정.

- **buildOrient 결정:** NEAR_FLOOR=0.7(recall.ts와 동일)로 흐려지는 것만, `let_go_at` 제외
  (클러스터 엔진은 안 거르지만 여긴 "다시 보여줄 것 고르기"라 회상 엔진과 목적이 같다). 축에
  이미 잡힌 파편은 프로젝트 후보에서 제외. 볼 것 없으면 `null` → 모델이 "딱히 없다"고 말한다.
- **검색 폴백 안 한다** — "오늘 뭐 봐야 할까"를 원문으로 검색하면 3차 오염(메타 표현이 검색 지배)
  재현. orient가 null이면 근거 없이 진행.
- ⚠️ **버그 수정 하나 딸려옴:** `_shared/cluster.ts`의 `vividness`가 2026-07-22 감쇠 재설계
  ([3,14]/[14,45]·.25 → [1,7]/[7,21]·.15)를 **놓친 복제본이었다.** 축·orient 순위가 며칠간
  옛 곡선으로 계산되고 있었음. 고쳤다. vividness.ts와 이 복제본은 손으로 나란히 유지할 것.

**⚠️ 배포:** `npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf`. SQL 없음.
검증: "오늘 뭐 봐야 할까" 물으면 축×흐림에서 한두 개 짚는지, "오늘 뭐 저장했지"는 여전히 period로
정확히 가는지(둘이 안 섞이는지), gate_log에 `orient` 판정이 남는지.

## §10-9 orient — 착수 전 설계 (아래는 원래 설계 메모, 위에서 구현 완료)

"오늘 뭐 봐야 할까/생각하지/놓친 거 있나"에 답하는 새 경로. 지금은 이런 질문이 `period`(오늘
저장한 것만) 아니면 `trend`(요즘 뭐에 꽂혔어, 축)로 잘못 흡수된다 — 위 실측 증거 참고.

**핵심 = 축(경향) × 흐림(감쇠)의 교집합.** 축만 보면 "요즘 뭐에 꽂혔어"랑 똑같고, 흐림만 보면
그냥 랜덤 회상이다. "네가 꽂혔던 결인데 그중 지금 흐려지는 것"이 이 경로의 정의.

- **재료**: `findAxes()` 재사용(이미 파편별 `last_touched_at`·`tier`·`touch_count`를 들고
  있어 `vividness()`를 바로 계산할 수 있다 — 새 쿼리 불필요) → 살아있는 축에서 흐려지는 항목만
  거른다 + active 프로젝트에 걸린 흐려지는 파편 하나 더.
- **Exa(바깥) 자동 금지** — 유저 명시. `<바깥가능>`처럼 물어보고 갈 순 있어도 자동으로는 안 켠다.
- **라우팅**: `intent`에 `'orient'` 신설(EXTRACT_SYS). `period`와 확실히 갈라야 한다 —
  "오늘 뭐 저장했지"(사실 조회, period) vs "오늘 뭐 봐야 할까"(판단, orient). trend/period를
  가르는 기존 문구("요즘 뭐에 꽂혔어?는 기간이 아니다")와 같은 패턴으로 예시를 명시할 것.
- **답의 모양**: 발견 브리핑과 정반대로 **한두 개만 짧게**. 처방 금지, 관찰+질문형(§4-F5).
  볼 게 없으면 침묵(§2-8) — 억지로 채우면 그게 유저가 싫어하는 "상담앱 느낌".
- 새 프롬프트 블록 `<오늘>` 필요 (`<축>`과 톤 비슷, 분량 훨씬 짧게).

## 🔖 새 세션 시작점 (2026-07-22 밤 — 모닝 브리핑 버튼 + OpenAI 로그 가시성, 배포·검증 대기)

**usage 추적(SQL·chat·discovery 배포)은 유저가 이미 완료.** 이어서 두 가지 추가:

1. **§10-8 아침 푸시를 수동 버튼으로 대체.** 발견 홈에 `모닝 브리핑 만들기` 버튼 신설(`새로 발견하기`
   와 별개). 어제 관찰 한 줄 계산 + 하루 1회 상한 로직을 `discovery/observation.ts`(신규)로 뽑아
   `morning-briefing`(cron, 폐기했지만 함수는 유저 지시로 남김)과 `discovery/index.ts`가 공유한다
   — 상한이 갈라지면 나중에 크론을 다시 켜도 하루 2번 나가는 구멍이 생기기 때문. 버튼은 `list`에서
   오늘자 `trigger='push'`가 있으면 미리 비활성화(왕복 없이), 서버도 같은 검사를 다시 한다(이중 방어
   — 어제 12번 눌러서 $4.93 나온 게 이 버튼 자리였다).
2. **OpenAI Platform 로그(platform.openai.com/logs)에서 호출별로 보이게.** `complete()`·`chatStream()`에
   `store:true` + `metadata`(call_site·request_id·conversation_id, 문자열만·파편 원문 없음)를
   추가(`_shared/usage.ts`의 `costTracker.meta()`). 이미 만든 5개 호출부 태그를 그대로 재사용.

⚠️ **배포 필요 (이번 것만, 위 usage 배포는 이미 끝남):**
```bash
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions deploy morning-briefing --project-ref ibqyqpmwqujcxlnkyizf
```
SQL 변경 없음(observation.ts는 순수 코드 리팩터). 배포 후 확인: 발견 홈에서 `모닝 브리핑 만들기`
누르면 정상 스트리밍되고 기록에 "아침" 배지로 뜨는지, 같은 날 두 번째로 누르면 비활성화/에러
메시지가 뜨는지, `platform.openai.com/logs`에서 `call_site` 메타데이터로 필터링되는지.

---

**직전 배경 (§10-8 원래 문제):** pg_cron이 `morning-briefing`을 못 부르고 있던 원인
(유력: `rudy-cron.sql`의 `<SERVICE_ROLE_KEY>` 미채움 → 401, pg_net은 "요청 보냄"만 성공으로
쳐서 에러가 안 남음)을 진단하던 중, 유저가 **"아침 푸시도 cron 없애자 그냥 내가 수동으로 할래"**로
결정. `select cron.unschedule('rudy-morning-briefing');`를 유저가 SQL Editor에서 실행 완료.
`morning-briefing` Edge Function 자체는 남겨뒀다(유저 지시) — 수동 호출용으로 쓸 수 있음, 발견
화면의 "새로 발견하기"가 이미 같은 엔진(streamBrief)이라 굳이 이 함수를 쓸 이유는 딱히 없다.

**오늘 다룬 두 가지 큰 작업, 전부 코드 완료·배포 전:**

### 1. 선명도 감쇠 재설계 (SPEC §5 개정) — "애초에 흐려지질 않는다"

실측(파편 127개): 바닥 도달 0개, 회상 후보(선명도 ≤0.7) **0개** — §5-1이 통째로 굶고 있었다.
→ 감쇠를 `normal[1,7] important[7,21] floor 0.15`로 조정(이전 `[3,14]/[14,45]`·바닥 0.25).
새 값에서 회상 후보 69개. `src/lib/vividness.ts`·`__tests__/vividness.test.ts`·SPEC.md §5·PLAN.md
전부 동기화.

**touch 규칙도 같이 바뀌었다 (유저가 두 번 뒤집음, 최종 확정):**
- 내용·덧붙임 수정 = touch (안 바뀜)
- **프로젝트 지정/해제 = touch** ("지금 이걸 다시 붙잡았다"는 판단 — 유저 최종 확정)
- **tier 변경 = touch 아님** (tier 자체가 감쇠 속도를 바꾸는 신호라 touch까지 하면 중복)
- 상세 화면을 그냥 여는 것 = touch 아님 (원래부터)
- **파편 상세에 `기억하기` 버튼 추가** (흐려진 것에만 노출) — 100% 복귀 + touch_count +1.
  회상 섹션의 `기억하기`와 이름·동작 통일(유저: "아직 이거야는 말 같지도 않은 소리"). AI 칩
  (원탭 진입) 줄과는 구분선으로 분리 — 다른 종류의 행동이라서.

⚠️ **배포 전제 없음 — 순수 클라이언트 로직이라 앱 리로드만 하면 반영된다.** SQL·Edge Function
배포 필요 없음.

### 2. LLM 비용 추적 (`rudy.llm_usage` + 응답별 표시) — "어제 왜 4.93달러 나갔지"

**원인 진단:** `check-brief.mjs`가 찍던 `$0.042`는 Exa 검색비만이었다(코드: `exaCost` 변수).
gpt-5.5 비용은 측정된 적이 없다. 실측: 07-21에 발견 브리핑을 12번 생성(화면 다듬으며 반복 실행),
브리핑 1회가 gpt-5.5를 2번(각도·조립) 태우고 조립 입력은 재료 블록(파편 전체)+검색결과
6건×5개×발췌 900자로 크다. **이게 청구서의 지배적 원인.**

**단가 확인(2026-07-22, OpenAI 공식 페이지 `developers.openai.com/api/docs/pricing`):**
gpt-4o $2.50/$10.00(in/out, per 1M) · gpt-4o-mini $0.15/$0.60 · gpt-5.5 $5.00/$30.00(캐시입력 $0.50).
gpt-4o/mini/임베딩은 현재 페이지에서 빠져 있음(레거시 취급) — 이 단가는 다른 출처와 교차 확인,
유저가 "이것들은 괜찮아"로 사용 지속 확정.

**구현:**
- `supabase/functions/_shared/usage.ts` (신규) — 단가표 + `estimateCost` + `logUsage`(gate_log와
  같은 결, fire-and-forget) + `costTracker`(한 응답 안의 여러 콜 비용을 request_id로 합산).
  **단가 모르는 모델은 0이 아니라 null** — "공짜"와 "단가 모름"을 구분(또 조용히 새는 것 방지).
- `_shared/openai.ts`의 `complete()`·`chatStream()`에 선택적 `onUsage` 콜백 추가 (하위호환,
  기존 호출부는 안 건드려도 동작). `chatStream`엔 `stream_options:{include_usage:true}` 추가.
- 호출부 5곳에 태깅: `chat.rewrite`·`chat.question_judge`·`chat.axis_label`(전부 FAST_MODEL) ·
  `chat.answer`(CHAT_MODEL) · `discovery.angles`·`discovery.assemble`(전부 DISCOVERY_MODEL).
  **임베딩(embed/embedMany)은 추적 범위에서 뺐다** — text-embedding-3-large가 $0.13/1M로
  티끌이고, 곳곳에 흩어진 호출부를 다 건드리는 게 배보다 배꼽. 필요해지면 그때 추가.
- **응답 하나의 총 비용을 DB에 박아둔다** (일회성 UI 트릭이 아니라 durable): `rudy.messages`·
  `rudy.utterances`에 `cost_usd` 컬럼 추가. 채팅은 답변 저장할 때, 브리핑은 원장 insert할 때 같이
  적는다 → **재접속해서 옛 대화·브리핑을 열어도 그때 비용이 그대로 보인다.**
- **모바일 표시**: 채팅 답변 아래·발견 기록 목록 행에 `$0.0031` 같은 캡션 (`src/lib/cost.ts`
  `formatCost`, 채팅·발견 공유). 단가 모르면 "단가 미상"으로 표시(0으로 숨기지 않음).
- **날짜 단위 감사는 별도 트랙 (미착수)** — 유저가 "둘 다" 선택: 응답별 추정치(위 구현)는
  우리 계산이라 틀릴 수 있음 → OpenAI의 **Organization Costs API**(실제 청구액, Admin 키 필요)로
  대조 검증하는 진단 스크립트를 나중에 만들 것. 이번 세션엔 응답별 표시만 구현, Costs API 연동은
  다음 작업.

⚠️ **배포 필요:**
1. `supabase/rudy-usage.sql` (신규) — `rudy.llm_usage` 테이블 + `messages`·`utterances`에
   `cost_usd` 컬럼 추가 + RLS. SQL Editor 붙여넣기, **일회성 배포**.
2. `npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf`
3. `npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf`
4. 크레딧이 지금 바닥나 있어서(유저 확인) 배포해도 실제 호출 전까진 검증 불가 — 유저가 크레딧
   채운 뒤 채팅 1턴 + 발견 1회 생성해서 `rudy.llm_usage`에 행이 쌓이는지, 모바일에 비용이
   뜨는지 확인 필요.

**다음 작업 후보 (유저가 "모델 테스트 말고 나머지"로 물었을 때 답한 것):**
- 위 §10-8 배포 체크리스트 (usage 배포 + 검증)
- OpenAI Organization Costs API 대조 스크립트 (Admin 키 필요, 응답별 추정치 검증용)
- §10-7 `more like this` 칩 — 자리만 있고 미구현(findSimilar 실측 나쁨, 아래 참고). 방향 미정.
- §10-7 나머지 발견 조각(더 있으면 §10-7 표 참고) · §10-8 나머지(넛지·주간편지·월말성적표, 전부 미착수)

**TestFlight 재배포 관련 (2026-07-22 유저 질문):** 백엔드는 클라우드에 있어 지장 없다. 다만
번들 ID를 공유하므로 **TestFlight 빌드를 깔면 dev client가 지워지고**, 새 설치라 알림 권한을
다시 허용해야 토큰이 생긴다. 허용 안 하면 옛 토큰은 죽어 있어서 알림이 아예 안 온다.

---

## ⛔ 아래는 낡은 메모 — 지시로 읽지 마라

**"바로 다음 작업 = more/less"라고 적혀 있었는데 이건 틀렸다.** 유저가 그 뒤에 명시적으로
**"find more/less는 완전 뒤로 미루자"**고 말했다(§10-8 우선). **지금 다음 작업은 맨 위
"🔖 새 세션 시작점" 섹션이다 — EAS dev client 설치 충돌.** more/less는 착수하지 마라.

아래는 그 시점(§10-7 막 끝난 오후)의 나머지 기록만 참고용으로 남긴다:

**최근 세션에서 한 것 (전부 코드 완료, 유저 컨펌·커밋·배포는 미확인):**
- 발견 브리핑 엔진·화면 완성(스트리밍·카드·날짜기록·홈착지·백그라운드생성·던지기 플라이휠·삭제).
- 채팅 + 웹검색(Exa outward 삼단 no/ask/go) + 맥락 주입("그거" 해결) + 역할 앵커 + "외부 찾는 중" 표시.
- 상세는 아래 "채팅+웹검색", "발견 화면 2차 다듬기", "발견 확장 방향" 섹션.

**미배포(사람이 직접):** `chat`·`discovery` Edge Function — 최근 코드 반영하려면 재배포 필요.
```bash
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
npx supabase functions deploy discovery --project-ref ibqyqpmwqujcxlnkyizf
```
`discovery`는 `EXA_API_KEY` 시크릿 필요(로컬 .env엔 있음). `OPENAI_DISCOVERY_MODEL=gpt-5.5` 권장.

**유저 취향 못박기 (뉘앙스 — 새 세션이 놓치기 쉬움):**
- 발견/브리핑: RUDY-DISCOVERY.md의 렌즈가 유일 기준. 음악 검색 금지, 프로젝트 편향 금지(캡 2개).
- 아침 브리핑·액션 제안은 **관찰이지 처방이 아니다.** 투두·달성률 = "상담앱 느낌", 절대 금지(§4-F5 거울).
- 채팅은 **만능 비서가 아니다.** 날씨·시세 등 무관한 조회엔 안 뻗는다. 바깥은 유저 세계와 연결될 때만.
- 산출물은 짧고 자연스럽게. 주절주절·불릿 쪼갬·서론 금지.

## ⚠️ 순서 결정 (2026-07-19) — RUDY-BUILD 착수 순서를 폐기하고 RUDY.md §10을 따른다

RUDY-BUILD.md의 `0 → A → C → B`는 **RUDY.md §10과 충돌한다.** §10-5는 채팅에 "→ 원장 필요"라고
명시하고, RUDY-BUILD §C-2 자발적 연결도 "(Phase B 엔진 재사용)"이라 쓰면서 자기 순서표는 C를
B보다 먼저 둔다 (아직 없는 엔진을 재사용하라는 내부 모순).

그대로 밀면 나오는 물건은 "내 메모를 읽는 GPT"다 — 루디의 능동적 절반(자발적 연결·늦은 의도)이
전부 미구현 계층에 있기 때문. **→ RUDY.md §10 순서로 복귀: 충돌 엔진 → 원장 → 채팅.**

## 진행 상태

| §10 | 내용 | 코드 | 배포/검증 |
|---|---|---|---|
| 0 | 임베딩 파이프라인 (rudy 스키마·embed 웹훅·백필) | ✅ | ✅ 실사용 중 |
| 0+ | 링크 임베딩 품질 (og:description + 유튜브 shortDescription) | ✅ | ✅ |
| 1 | 링크 다이제스트 | ⏸ 보류 (아래 결정 참고) | — |
| 2 | 하이브리드 의미검색 + 검색 UI | ✅ | ✅ "음악 2개" 픽스 확인 완료 |
| 3 | 충돌 회상 (A1) + "왜 지금" | ✅ 완료, 임계값 실측 튜닝(0.42) | ⚠️ 기기 검증 필요 |
| 4 | 원장(rudy.utterances) + 게이트 로그 + 30일 쿨다운 + 응답 캡처 | ✅ | ✅ SQL 실행 완료(2026-07-19) |
| 5 | 채팅 RAG + 자발적 연결 + 원탭 진입 | ✅ | ✅ **v4 배포·100/100 검증 완료** |
| 6 | 클러스터 엔진 → 패턴·경향 (B1·B2) | ✅ 코드 완료 | ⚠️ SQL 실행 + 배포 + 기기 검증 필요 |
| 6+ | 늦은 의도 (F1) + `rudy.evidence` | ✅ 코드 완료 | ⚠️ SQL 실행 + 배포 + 기기 검증 필요 |

## Supabase 배포 상태 (코드 밖 수동 작업)

이미 적용됨: rudy 스키마, `rudy` Exposed schemas 추가, `OPENAI_API_KEY` 시크릿,
`embed`·`embed-query` 함수 배포, fragments Insert/Update 웹훅, `link_description` 컬럼,
임베딩 백필 완료, `rudy-search.sql` (4-arg 타입필터 RPC).

`rudy-collision.sql`·`rudy-ledger.sql` 실행 완료(2026-07-19).

`rudy-chat.sql` 실행 완료. Edge Function `chat` **v4 배포 완료(2026-07-19).**

**배포 커맨드 (CLI가 PATH에 없고 프로젝트가 로컬 링크 안 돼 있다 — 반드시 이 형태로):**
```bash
npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf
```
`supabase functions deploy chat`만 치면 CLI를 못 찾거나 링크가 없어 조용히 실패한다.
배포 확인: `npx supabase functions list --project-ref ibqyqpmwqujcxlnkyizf` 의 `chat` version 증가.
(선택) 시크릿 `OPENAI_CHAT_MODEL`(기본 gpt-4o) · `OPENAI_FAST_MODEL`(기본 gpt-4o-mini).

## 메모리 현황 (RUDY.md §5 대비) — 대부분 미구현

| 설계 | 역할 | 상태 |
|---|---|---|
| `rudy.fragment_embeddings` | 파편 임베딩 | ✅ |
| `rudy.utterances` (원장) | 같은 말 금지(§2-2)의 물리적 실체 | ✅ |
| `rudy.gate_log` | 게이트 판정 사유 (§6-4) — 임계 튜닝의 근거 | ✅ |
| `rudy.evidence` | 유저의 자기 진술만 | ✅ F1이 묻고 받아낸다 |
| `conversations`/`messages` | 대화 이력 | ✅ |
| `link_digests` | B3 캐시 | ❌ (보류) |
| 클러스터 엔진 (§6-2) | ongoing 프로젝트 vs 단발성 | ✅ **휘발성 — 저장하는 게 없는 게 정상이다** |

## 구현 중 내린 결정 (RUDY-BUILD 대비 변경/확정)

- **링크 임베딩 = 제목+og:description** (요약 아님). 유튜브는 og:description이 홍보문구로 오염돼
  페이지의 `ytInitialPlayerResponse.shortDescription`을 직접 파싱 (linkMeta.ts + 스크립트 동일 로직).
- **다이제스트(LLM 요약·의도) 보류.** 비용이 아니라 — (1) 보여줄 표면 없음, (2) 검색품질은
  설명글 임베딩으로 이미 해결, (3) 의도 해석은 lazy가 §2-1 정합. → 채팅 만들 때 lazy 계산.
- **검색 폴백**: 임베딩/RPC 실패 시 키워드 검색으로 자동 폴백(+console.warn).
- **타입 필터 = 서버(RPC)에서** (클라 후필터 아님).
- 스크립트는 `.mjs` (TS 러너 미설치). embedText/해시는 embed 함수와 반드시 동일.
- **충돌: seed 평균 임베딩 폐기 → seed별 개별 비교 후 최댓값** (RUDY-BUILD B-1에서 변경).
  이유 (1) 무관한 걸 여러 개 던진 날 평균은 노이즈 중심이 되어 억지 충돌을 만든다,
  (2) "왜 지금"(B-3)이 어느 씨앗과 부딪혔는지를 요구하는데 평균은 그 정보를 지운다.
- **충돌 게이트와 순위 분리** (RUDY-BUILD B-2에서 변경). B-2의 `sim×faded×weight ≥ 0.35`는
  weight가 위로 열려 있어(1 + touch_count×2 + …) **자주 만진 파편이 무관해도 통과**하고 평범한
  파편은 관련 있어도 못 통과한다 — 억지 충돌 방지 장치가 정반대로 작동. → 게이트는 `sim`만,
  통과한 것 중 순위만 `sim × faded × weight`.
- **선명도는 SQL로 옮기지 않는다.** 감쇠 법칙의 단일 원천은 vividness.ts. RPC는 "아직 흐려졌을
  리 없는 것"(7일 미만)만 보수적으로 걷어낸다. 진단용 `min_age_days=0` 파라미터로 필터를 끌 수 있음.
- **충돌 임계값 0.35 → 0.42** (실측, `check-collision.mjs --all`). 실제 코퍼스 41개 후보로 보니
  0.35는 깨끗한 경계가 아니었음 — teenage engineering 클러스터는 0.52~0.73으로 확실했지만
  0.30~0.34대에 무관한 것과 관련 있는 것이 뒤섞여 있었다(임베딩 이방성). 억지 충돌보다 놓치는
  쪽이 안전하다는 §2-8에 따라 올림. **진짜 튜닝은 원장(§10-4) 게이트 판정 로그로 — 이 값은 잠정.**

## 문서 정합성

- ✅ RUDY.md §8 대화 모델을 Claude → OpenAI로 정정 (2026-07-19).
- ⚠️ **이전 세션의 "완성된 프롬프트 초안"은 실재하지 않았다** (2026-07-19 확인).
  이 파일이 완성됐다고 적었지만 리포 어디에도 저장돼 있지 않았다 — 세션 컨텍스트와 함께 소실.
  → **교훈: 산출물은 "완성"이라 적기 전에 파일로 떨어뜨린다.** 프롬프트는
  `supabase/functions/chat/prompt.ts`에 새로 작성했고, 기록돼 있던 필수 요구사항 3건은 전부 반영:
  세상 지식 예외 / 근거에 created_at 포함 / 시스템에 오늘 날짜 주입.
  목소리는 실사용 관찰로 조였다(아첨·느낌표·군더더기 추측 금지, 평서형 통일).

## 스크립트 인벤토리 (전부 로컬 node 실행, service role 필요)

| 스크립트 | 용도 | 성격 |
|---|---|---|
| backfill-embeddings.mjs | 기존 파편 전체 임베딩 | 일회성(완료) |
| backfill-link-desc.mjs | 기존 링크 og:description 채우기 | 일회성 |
| fix-youtube-descriptions.mjs | 유튜브 홍보문구 오염값 재계산 | 일회성 |
| check-embeddings.mjs | 임베딩 유사도 실측 (`--links` 링크만) | 진단 |
| check-search.mjs | search_fragments RPC 직접 검증 | 진단 |
| check-collision.mjs | collision_candidates 검증 + **임계값 튜닝** (`--all` 감쇠 무시) | 진단 |
| check-ledger.mjs | 원장·게이트 로그 조회 + §6-6 성적표 원형 | 진단 |
| check-chat.mjs | 배포된 chat 함수 E2E — 스트리밍·저장·근거·touch 불변 (`--keep` 대화 보존) | 진단 |
| check-clusters.mjs | 클러스터 임계 스윕 + 축 내용 펼치기 (`0.42` 인자로 확정 임계 확인) | 진단 |
| check-intent.mjs | 늦은 의도 상태 — 예산·후보 풀·질문 이력·받아낸 자기 진술 | 진단 |

## §10-4 원장 — 구현 범위 (의도적으로 좁힘)

**만든 것:** `rudy.utterances`(원장) + `rudy.gate_log` + 30일 되살리기 쿨다운 + acted/dismissed 캡처.

**안 만든 것 — §6-4의 5개 게이트를 추상 프레임워크로 만들지 않았다.** 지금 말하는 주체가
충돌 회상 하나뿐이라 범용 파이프라인은 speculative abstraction(CLAUDE.md §2)이다. 실제로 쓰이는
게이트만 인라인으로 있다: **유사도 게이트**, **쿨다운 게이트**. 화법·확신 게이트는 생성 발화가
없어서 적용 대상 자체가 없고, 예산은 하루 1개 저장(AsyncStorage day key)으로 이미 강제된다.
→ **두 번째 화자(채팅)가 생길 때 공통 모양이 드러나면 그때 추출한다.**

설계 판단 기록:
- `item_ids`에 **씨앗은 넣지 않는다.** "왜 지금"은 렌더 시점 계산·무저장이고 연결 저장은
  Mind SPEC §7 금지. 씨앗은 기기 로컬(AsyncStorage)에 하루치만 남는다.
- **랜덤 픽은 원장에 안 들어간다.** 그건 루디의 발화가 아니라 우연의 자리(§4-F2)다.
  따라서 랜덤으로 뜬 파편에 기억하기/흘려보내기를 해도 루디 성적에 반영되지 않는다 — 의도된 것.
- `ignored`는 저장하지 않는다. "응답 없음 + 24h 경과"로 집계 시점 계산 (노출은 신호가 아니다 §2-3).
- 원장 기록 실패가 회상을 죽이지 않는다 (try/catch + warn).

## §10-5 채팅 — 구현 중 내린 결정

- **자발적 연결의 원장 kind = `resurface`** (surface만 `chat`). 되살리기와 같은 kind를 쓰면
  기존 쿨다운 조회(`resurfacedIdsSince`는 surface를 안 가림)에 자동으로 걸려서, §4-C1이 요구한
  "표면 간 중복 방지"가 **새 코드 없이** 성립한다 — 떠오른 것에서 본 파편을 채팅이 또 꺼내지 않는다.
- **평범한 채팅 답변은 원장에 안 적는다.** 원장은 §2-2 "먼저 거는 말"의 중복 방지 장치인데,
  질문에 답한 걸 전부 넣으면 반복 게이트가 오염된다(§6-4 ⑤도 채팅 응답은 무예산).
  원장에 남는 건 **묻지 않았는데 나간 자발적 연결 하나뿐.**
- **채팅 충돌 임계 = 0.34** (회상은 0.42). 질문↔파편은 파편↔파편과 유사도 분포가 다르다 —
  질문 임베딩은 문서 임베딩과 비대칭이라 같은 관련도에서도 낮게 깔린다. **완전 잠정값이고,
  판정마다 gate_log에 실측을 남긴다** (`surface='chat'`) — 이번엔 §6-4 "감이 아니라 로그로
  튜닝"이 처음부터 가능하다. 며칠 쓰고 `check-ledger.mjs`로 분포 보고 조정할 것.
- **연결 언급 여부는 검증하지 않는다.** 모델에 `<연결>`을 건네고 "억지면 말하지 마라"라고
  지시하므로, 원장의 의미는 "연결을 건넸다"까지가 정확하다. 언급 강제는 화법을 망친다.
- **자발적 연결은 RAG 근거로 이미 인용된 파편을 제외한다** (`exclude_ids`).
  답에 쓴 걸 "이것도 이어지는데?"라고 가리키면 연결이 아니라 자기 답변 반복이다.
- **원탭 진입 질문에 파편 내용을 풀어 넣는다.** "이거 관련 뭐 있었지"만 보내면 임베딩에 주제가
  없어 RAG가 아무거나 물어온다 → `『{제목}』 관련해서 내가 저장해둔 게 또 뭐가 있지?`로 만든다.
- **채팅은 드로어 탭이 아니라 push 화면**(`src/app/chat.tsx`). 드로어는 피드를 거르는 렌즈들의
  자리인데 채팅은 렌즈가 아니라 목적지다. RUDY-BUILD C-3의 `(drawer)/chat.tsx`에서 변경.
- **대화 기록 시트 있음** (유저 요구, 2026-07-19). 목록·이어가기·보이는 지우기 버튼.
  대화 행은 첫 전송 때 지연 생성 — 열 때마다 만들면 빈 대화가 쌓인다.
- **스트리밍은 `expo/fetch`.** supabase-js `functions.invoke`는 응답을 통째로 모아서 주므로
  못 쓴다. 함수 URL을 직접 치고 세션 토큰을 붙인다. 프로토콜은 NDJSON(`d`/`cite`/`link`/`error`).
- **이력 저장은 서버(Edge Function)가 한다.** 스트리밍 도중 앱이 죽어도 대화가 남아야 한다.

### 1차 실사용에서 터진 버그 3건 (2026-07-19, "답이 나오다가 없어짐")

셋 다 원인이 달랐고 증상이 겹쳐 보였다. 스트리밍 UI 만들 때 재발하기 쉬운 것들이라 남긴다.

1. **스트림 끝의 남은 버퍼를 버렸다.** SSE·NDJSON 둘 다 `split('\n')` 후 마지막 조각을
   다음 청크용으로 버퍼에 남기는데, 루프가 끝날 때 그 버퍼를 안 흘렸다. 공급자가 마지막 줄을
   개행 없이 닫으면 **답변 끝이 통째로 잘린다.** → 양쪽 다 루프 뒤에 flush 추가.
2. **저장 확인 없이 화면을 지웠다.** 스트림이 닫히면 무조건 `streaming=null`로 두고 서버에서
   이력을 다시 읽었는데, 아직 안 적힌 답을 읽으면 **다 나온 답이 눈앞에서 증발한다.**
   → 서버가 저장 후 `done` 이벤트를 보내고, 앱은 그걸 받고 이력에서 확인된 뒤에만 지운다.
   못 믿을 땐 흘러나온 텍스트를 그대로 두고 "저장 안 됨"만 표시한다.
3. **ScrollView에 `flex:1`이 없었다.** 세로 flex 컨테이너 안에서 내용 크기로 자라서,
   답변이 길어지면 입력창을 화면 밖으로 밀어냈다.

**교훈: 스트리밍에서 "사라짐"은 대개 세 곳 중 하나다 — 버퍼 flush 누락, 낙관적 화면 정리,
스크롤 컨테이너 flex.** 셋 다 각각 확인할 것.

### 2차 실사용에서 잡은 진범 (2026-07-19, "다음 전송이 안 됨")

**뿌리는 서버 저장 실패였다 — `check-chat.mjs`로 실측해 확정.**
PostgREST 다중 행 insert는 행마다 키가 다르면 **빠진 키를 default가 아니라 null로 채운다.**
user 행에 `cited_ids`가 없어서 null → not null 제약 위반(23502) → 이력 저장 전체 실패 →
클라이언트가 실패 경로 진입 → `streaming` 상태가 안 풀려 **다음 전송 영구 잠김.**
수정 (1) user 행에도 `cited_ids: []` 명시 — 수정 payload를 실 DB에 직접 쏴서 통과 확인,
(2) 상태머신 자체를 재설계 — phase는 idle↔streaming 둘뿐, `finally`가 무조건 idle 복귀.
**교훈: 클라이언트 상태 잠김의 원인이 클라이언트에 없을 수 있다. E2E 진단부터 만들 것 —
`check-chat.mjs`가 첫 실행에서 바로 이걸 잡았다. 그리고 PostgREST 다중 행 insert는
모든 행에 같은 키를 채워라.**

### §10-5 2차 재작업 (2026-07-19) — 사용성 전반

- **스트림 파서를 순수 모듈로 추출** (`src/lib/ndjson.ts`) + 잘림 회귀 테스트.
  버그가 났던 로직을 UI에서 꺼내 jest로 못 박았다.
- **미니 마크다운 렌더러** (`src/lib/markdown.tsx`) — 굵게·목록·코드·링크만. 라이브러리 대신
  직접 쓴 이유: RN 마크다운 렌더러들은 React 19 peer 충돌 위험 + 프롬프트가 문법을 우리가
  정한 부분집합으로 제한하므로 그만큼만 정확히. 파서는 순수 함수, 스트리밍 중 미완성 문법에도 안 죽음.
- **파편·프로젝트 딥링크** — 근거에 id를 넣고 모델이 `[『제목』](mind://fragment/<id>)`,
  `[이름](mind://project/<id>)`로 인용. chat.tsx가 라우팅. 링크 파편은 원본 URL도 근거에 포함.
- **근거 바닥 컷(CITE_FLOOR) 제거** — 실측: 질문 문장 임베딩은 관련 파편도 0.28~0.43에
  깔린다. 어떤 바닥이든 신호를 자른다. 관련성 판정은 근거를 직접 보는 모델의 몫.
- **"홈레코딩 없다" 원인 = 프롬프트** — 검증 결과 "홈레코딩" 리터럴 파편은 실제로 0개,
  의미로 닿는 파편(음성으로 녹음 0.51 등)은 있었다. 채팅도 검색과 같은 RPC를 쓴다.
  프롬프트가 이진 판정("근거에 없으면 없다")이라 근접 매치를 버린 것 →
  "딱 적어둔 건 없는데 닿는 건 이것들" 화법을 명시.
- **중단(■)** — 응답 중 보내기 버튼이 중단으로 바뀜. 서버는 cancel을 감지해 OpenAI 스트림을
  놓고 받은 데까지 저장.
- **대화 지연 생성** — 행은 첫 전송 때 만든다. 열 때마다 만들면 빈 대화가 기록에 쌓인다.
- **기록 시트** — 목록·이어가기·보이는 지우기 버튼(롱프레스는 아무도 발견 못 한다)·현재 대화 표시.
  제목은 첫 질문 그대로(요약 모델 안 부름).
- **빈 화면 예시 칩** — 뭘 물어볼 수 있는지 화면이 직접 보여주고, 탭하면 바로 전송.
- **지연 개선** — 이력 조회를 임베딩·검색과 병렬화, gate_log는 fire-and-forget.
  베이스라인 첫 토큰 3.4s (gpt-4o). 더 줄이려면 `OPENAI_CHAT_MODEL`을 mini 계열로 (env만 변경).
- **검증 도구** — `scripts/check-chat.mjs` (진단용): 스트리밍·done/saved·이력 2줄·근거 인용·
  mind:// 링크·touch 불변까지 앱 없이 확인. 만든 대화는 끝나면 지움(`--keep` 제외).

### 3차 — RAG가 망가져 있던 진짜 이유 (2026-07-19 실측)

**질문 문장을 통째로 임베딩한 것이 원인이었다.** "내가 홈레코딩 관련 저장한 거 있어? 링크도 줘"를
그대로 임베딩하면 "저장/링크" 같은 **메타 표현이 벡터를 지배**해서, 홈레코딩이 아니라
*앱의 링크·저장 기능에 대한 파편*이 상위에 온다:

| 검색어 | 1위 결과 |
|---|---|
| 질문 원문 | 0.535 "링크 던지면 요약해서 왜 저장했는지…" ← 무관 |
| "홈레코딩"만 | 0.511 "음성으로 녹음" ← 정답 |

유저가 검색창엔 "홈레코딩"만 치니까 잘 나왔고, 채팅은 문장을 통째로 던져서 망가졌다.
**같은 RPC를 쓰는데 결과가 달랐던 이유가 이것이다 — 모듈이 다른 게 아니었다.**

→ **질의 재작성(다중 질의 검색)** 도입. 질문에서 주제어 1~3개를 빠른 모델로 뽑고,
주제어별로 검색해 파편별 최고점으로 합친다. **주제어가 나오면 원문은 안 쓴다**(다시 오염됨).
주제어가 없는 질문("요즘 뭐에 꽂혔어?")만 원문으로 검색.
추출 프롬프트에서 동작어(정리/저장/자료)를 명시적으로 배제 — 안 하면 그 단어의 키워드 매치가
1.0점으로 상위를 먹는다. 실측 후 결과: 홈레코딩→음성으로 녹음·기타 코드·vst,
기타→기타 코드 1.000·피아노/기타 연주 1.000. 비용은 재작성 1회(~600ms, gpt-4o-mini).

**교훈: RAG에서 "검색은 되는데 채팅은 안 된다"면 질의 텍스트를 의심하라. 같은 인덱스라도
무엇을 임베딩하느냐가 다르면 다른 시스템이다.**

### 클라이언트 내구성 원칙 (2026-07-19 확정)

**화면에 나온 글자는 서버가 어떻게 되든 사라지지 않는다.** 저장 실패로 텍스트가 증발한
사고 이후 세운 규칙. 로컬 턴 배열(`Turn[]`)에 누적하고, 서버 저장이 확인될 때만 서버 이력으로
승격시킨다. 확인 안 되면 로컬에 남고 "저장되지 않았다"만 덧붙는다.
같은 이유로 **에러를 조용히 삼키지 않는다** — 기록 시트는 조회 실패를 시트 안에 적는다
(예전엔 `.catch(() => {})`라 버튼을 눌러도 아무 일이 안 일어나 "기록이 아예 안 뜬다"가 됐다).

### 4차 검증 결과 (2026-07-19, chat v4 배포 후)

`check-chat.mjs` 10문항 × 10항목 = **100/100 통과.** 검사 항목: HTTP·스트림 에러·스트리밍
토큰 수·done 이벤트·saved:true·messages 2줄·근거 검색·링크 형식·자리표시자 회귀·touch 불변.

추가로 잡은 결함 2건:
- **모델이 프롬프트 예시를 그대로 복사**했다 — `[『내용 요약』](mind://…)`이 답변에 리터럴로
  나왔다. 프롬프트의 자리표시자를 실제 예시(`[유튜브 저장해놓은 기타 코드](mind://fragment/3c0c…)`)로
  바꾸고, 자리표시자 회귀 검사를 스크립트에 추가.
- **"링크 뭐 있었지"에 링크가 없다고 답함** — 질의 재작성에 `type` 추출을 추가해
  `search_fragments`의 타입 필터로 넘긴다(검색 UI의 타입 칩과 같은 동작). 링크/이미지/인용구.

**진단 스크립트의 판정도 두 번 틀렸다.** 세상 지식 답변("재즈 화성이 뭐야?")과 원본 URL 답변에
`mind://` 링크를 요구했다. → 근거 파편의 내용이 실제로 답변에 옮겨졌을 때만 링크를 요구하도록 수정.
**교훈: 실패가 뜨면 코드가 아니라 판정이 틀렸을 가능성부터 확인할 것.**

### 배포 상태 (2026-07-19 기준)

`chat` **v4 배포 완료.** `npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf`
(CLI가 PATH에 없고 프로젝트가 로컬 링크 안 돼 있어 이 형태여야 한다 — 짧은 커맨드는 조용히 실패).

**§10-5 유저 컨펌 완료 (2026-07-20).** 커밋 대기 중 — 커밋 명령은 아직 실행 안 됨(git push까지
사용자가 터미널에서 직접, CLAUDE.md §5 규칙).

## §10-6 클러스터 엔진 — 구현 완료 (2026-07-20), 배포·검증 대기

**RAG 나열과 다른 지점 (착수 전 정의한 것, 결과물은 이 셋을 만족한다):**
1. **질문이 입력이 아니다** — 질문에 가까운 걸 뽑는 게 아니라 90일 코퍼스에서 서로 가까운
   것끼리 묶는다. 검색이 원리상 못 하는 일.
2. **묶고 이름 붙인다** — 파편 N개가 축 1개로. 나열이 아니라 축소.
3. **시간 모양을 안다** — "그 축, 3주째 조용해"를 말할 수 있다. **없는 것을 말할 수 있는 건
   클러스터뿐이다.**

**구조:**
- `rudy.cluster_edges(days, min_sim, max_edges)` — 쌍 유사도를 SQL에서. 3072차원을 Deno로
  끌어오면 파편 500개에 수십 MB 전송이다. **테이블은 안 만든다** (엣지 목록만 반환).
- `_shared/cluster.ts` — 평균연결 병합 + 선명도 가중 + 시간 모양. 순수 함수, 저장 없음.
- `chat/clusters.ts` — findAxes(계산) + axesBlock(프롬프트 블록). 라벨은 LLM 1콜(fast model).
- 채팅 라우팅: **질의 재작성이 `topics: []`를 뱉는 질문이 정확히 "요즘 뭐에 꽂혔어?" 류다** —
  새 분류기 없이 그 자리를 축 경로로 갈아끼웠다. 예전엔 거기서 질문 원문으로 검색했는데
  그게 3차에서 밝혀진 오염된 경로 그 자체였다. 축이 안 서면 기존 검색으로 폴백(채팅에서 침묵은 답이 아니다).

**구현 중 내린 결정:**
- **평균연결(average linkage)이지 연결요소가 아니다.** 단일연결이면 A–B, B–C 두 엣지만으로
  A·C가 한 덩어리가 된다(체이닝) — 무관한 두 주제가 다리 파편 하나로 붙으면 축이 아니라 죽이고,
  거기 이름을 붙이라 하면 LLM은 반드시 그럴듯한 거짓말을 만든다(§2-8).
- **지속 판정에 "서로 다른 저장일 ≥3"을 추가**(span ≥21일과 함께). span만 보면 하루에 몰아
  저장하고 한 달 뒤 1개 더 저장한 게 "3주간 반복된 축"이 된다 — 그건 반복이 아니라 **두 번
  있었던 일**이다. §4-B1이 보려는 건 흩어짐이지 양끝 거리가 아니다.
- **TTL 캐시 안 만들었다.** §6-2는 TTL 캐시를 허용하지만, 트리거되는 질문이 드물고 라벨링이
  1콜(~1.5s)이다. 캐시가 없는 쪽이 §2-1에 더 안전하고 코드도 없다. 지연이 아프면 그때 만든다.
- **축 경로에선 자발적 연결을 안 돌린다.** 답변 전체가 이미 "묻지 않은 것을 꺼내는" 일이라
  거기 연결을 또 얹으면 같은 동작의 반복이다.
- **이름을 못 붙인 묶음은 버린다.** 라벨 프롬프트가 잡동사니면 빈 문자열을 내게 하고, 빈 것은
  축에서 제외 — LLM에게 억지 명명을 시키지 않는 안전장치.
- **판정을 gate_log에 남긴다** (`kind='cluster'`). 충돌 임계를 감으로 정했다가 뒤집은 것과 달리
  이번엔 처음부터 근거가 쌓인다.

**⚠️ 임계 0.42는 5일치 코퍼스로 정한 잠정값이다.** 실측 근거(2026-07-20, `check-clusters.mjs`):

| 임계 | 축 | 묶인 파편 | 최대축 |
|---|---|---|---|
| 0.34 | 8 | 28 | 5 |
| 0.38 | 4 | 15 | 5 |
| **0.42** | **3** | **11** | **5** |
| 0.46 | 2 | 8 | 5 |
| 0.54 | 1 | 3 | 3 |

0.42에서 선 축: teenage engineering(5) / 독서·문학(3) / 앱 UI 메모(3). 앞 둘은 깨끗하고
셋째가 제일 느슨하다. 0.46이면 셋째가 사라지고 커버리지가 절반이 된다.

**⚠️ 셋 다 "단발"로 나왔고 그게 정답이다.** 코퍼스 전체가 2026-07-16~20의 5일치다
(07-17의 54개는 다른 앱에서 옮겨온 일회성 이관 — 이후 이관 없음). 3주에 걸친 축이 안 나오는
게 아니라 **나올 수가 없다**. B1의 핵심 문구("3주에 걸쳐 따로 던진 것들이 사실 한 덩어리")는
데이터가 쌓여야 검증된다. 지속 분기는 스펙대로 짜뒀고 코퍼스가 자라면 자동으로 켜진다.

### 1차 실사용에서 잡은 것 (2026-07-20)

**축 답변 자체는 설계대로 나왔다** — 나열이 아니라 축 4개(믹서/케이스랩/독서/앱UI), 그리고
**"지속적인 관심으로 보이지는 않는다"**고 스스로 말했다. 5일치 코퍼스에서 경향을 지어내지
않은 것 = §2-8이 실제로 작동한 첫 사례.

버그 1건 — **미니 마크다운 파서가 중첩을 못 견뎠다.** 모델이 축을 나열할 때 링크를 굵게
감싼다(`**[제목](mind://…)**`). 인라인 정규식의 `\*\*([^*]+)\*\*`가 링크 마크업을 통째로
삼켜서 **마크업 원문이 화면에 그대로 찍히고 탭도 안 됐다.**
→ 굵게 안을 재귀 파싱하도록 고치고 회귀 테스트 추가(`__tests__/markdown.test.ts`).
**교훈: 프롬프트로 모델의 마크업 습관을 막으려 하지 마라 — 파서가 견뎌야 한다.**
DB 원문을 먼저 확인한 게 진단을 갈랐다 (저장된 URL은 멀쩡했으므로 서버가 아니라 렌더러다).

화법 1건 — 시간 모양이 **계산된 사실**로 넘어가는데 모델이 "생각을 모은 것 같아"처럼
추측 어미를 붙였다. 프롬프트 `## 축`에 금지 문구 추가.

## §10-6+ 늦은 의도 (F1) — 구현 완료 (2026-07-20), 배포·검증 대기

`rudy.evidence`가 **§2-1 경계선의 물리적 실체**다. 루디의 추론은 어디에도 저장 안 하지만
유저가 직접 말한 것은 저장한다. "이 사람은 케이스랩에 빠져 있다"(루디의 결론) ❌ /
"케이스랩 준비하고 있어"(유저의 말) ✅.

**세 갈래를 안 만들었다.** 유저는 "설명 없는 링크 / 고아 파편 / 축의 의도 불명 셋 다 필요"라고
했지만, 실제로 다른 건 **정렬 키뿐**이라 후보 풀 하나 + 우선순위로 짰다:
① 축에 묶였는데 의도 불명(답 하나가 축 전체를 추측→확인으로) → ② 설명 없는 최근 파편
(하나만 설명하지만 최근이라 유저가 실제로 기억한다) → ③ 오래된 고아(물어도 답을 모른다).
트리거 3개·게이트 3개를 만들 뻔한 걸 하나로 줄였다.

**구현 결정:**
- **질문은 채팅 답변 끝에 붙인다.** 새 표면 없음 — §4-F1이 "대화 중 자연스럽게"라 못 박았고
  푸시로 만들면 §2-6 예산·§7-3에 걸린다. 프롬프트가 "흐름에 얹기 어색하면 묻지 마라"까지 지시.
- **쿨다운은 원장 재사용.** `kind='question'` + `item_ids=[파편id]`로 적으면 같은 파편 재질문이
  자동으로 막힌다 — 자발적 연결이 `resurface` 원장을 재사용한 것과 같은 수법. 새 코드 없음.
- **예산 검사를 제일 먼저 한다.** 하루 1개를 이미 물었으면 나머지 조회를 아예 안 돈다 —
  비싼 일이 하루 한 번만 돌게 하는 게 코드 순서로 강제된다.
- **저장되는 문장은 유저의 말 그대로.** 모델은 "이게 답인가"만 판정하고 문장은 안 만진다.
  요약·정규화하는 순간 그건 유저의 진술이 아니라 **루디의 해석**이고 §2-1을 넘는다.
  → `JUDGE_SYS`는 `{"isAnswer": bool}`만 뱉는다. 이 설계를 바꾸지 말 것.
- **소비처까지 같이 붙였다** — 안 그러면 아무도 안 읽는 데이터를 쌓는 거다. 축에 묶인 파편에
  자기 진술이 있으면 `<축>` 블록에 "본인 진술"로 들어가고, 프롬프트가 거기엔 추측 화법을
  금지한다 → §4-B2가 요구한 "확인해주면 추측을 벗는다"가 성립.
- `embedding`은 지금 소비처가 없는데도 채운다. 나중에 §6-4 ② 반복 검사·의미 검색에 필요하고,
  안 채우면 백필을 또 돌려야 한다 (임베딩 백필 이미 두 번 했다).

### F1 1차 실사용에서 잡은 진범 (2026-07-20) — 라우팅 조건이 너무 넓었다

유저가 늦은 의도 질문에 **"그냥 재밌어보여서 그리고 나 음악 했었어 원래"**라고 답했더니
루디가 그걸 질문으로 받아 **축 보고서를 다시 냈다** — 앞 턴과 거의 같은 답(§2-2 위반).
gate_log가 그대로 보여줬다: `question/confidence passed=true`(캡처 성공) 바로 뒤에
`cluster/cluster passed=true`(축 경로 재진입).

**원인: 축 경로 조건을 `topics.length === 0`으로 잡은 것.** 주제어가 안 나오는 건 메타 질문만이
아니다 — 진술·감상·인사·잡담이 전부 빈 배열이다. 3차에서 "질의 텍스트를 의심하라"를 배워놓고
같은 자리에서 프록시 신호를 질문 판정에 재사용했다.

수정 2건:
1. **질의 재작성에 `intent` 필드 추가** (`trend` | `other`). 축으로 가려면 `intent==='trend'` +
   주제어 없음 + **답변 턴이 아님**, 셋 다 만족해야 한다.
2. **`captureAnswer`가 캡처 사실을 돌려주고 `<방금답함>` 블록으로 모델에 전달.** 저장만 하고
   대화에 반영을 안 하니 루디가 자기가 물어놓고 답을 못 들은 것처럼 굴었다. 프롬프트에
   "짧게 받아라, 보고서 금지, 고맙다고 하지 마라, 다시 캐묻지 마라" 명시.

**교훈: "이 메시지가 무슨 종류인가"를 부산물로 판정하지 마라.** 주제어 추출의 빈 배열은
"메타 질문"이 아니라 "구체 명사 없음"일 뿐이다. 판정이 필요하면 판정을 시켜라.

**미해결로 남긴 것:** 축 답변에 반복 게이트(§6-4 ②)가 없다. 같은 질문을 이틀 연속 하면 같은
축 목록이 그대로 다시 나온다. 이번 버그는 라우팅 수정으로 막혔지만 원인이 다른 재발 경로다.

### 시간 질의가 통째로 없었다 (2026-07-20) — "오늘 저장한 거 없다"고 답한 사건

유저: "오늘은 무슨 생각을 했지" → 루디: "아직 오늘 남긴 게 따로 보이진 않네."
**실제로는 그날 6개를 저장했다.** 임베딩 누락 아님(살아있는 파편 119개 전부 임베딩 있음 — 확인함).

**원인 1 — 시간 질의를 검색으로 처리했다.** 파이프라인에 날짜 필터가 어디에도 없다.
"오늘 뭐 저장했지"가 주제어 없음 → 임베딩 유사도 검색으로 갔고, 그날 저장한 게 케이스랩
메모라 질문 문장과 의미가 안 닿아서 안 나왔다. **유사도로 시간을 물으면 영원히 이렇게 된다.**
→ `period` 추출(today|yesterday|week|month) + **유사도를 아예 안 쓰는 날짜 조회 경로**(`<기간>`).
"검색 결과가 아니라 전부다"를 블록에 명시 — 그래야 모델이 "없다"를 단정할 수 있다.

**원인 2 — 타임존.** 서버 UTC / 유저 KST(+9)인데 `systemPrompt`의 오늘 날짜를 UTC로 넣었다.
**KST 00:00~08:59엔 루디가 어제를 오늘로 안다.** 날짜가 "어떤 건 맞고 어떤 건 틀린" 이유.
→ `_shared/time.ts` (KST 상수 고정, 유저 1명). 기간 경계도 KST 자정 기준.
**이 버그는 하루 9시간만 나타나 수동으로는 못 잡는다 → `__tests__/time.test.ts`로 못 박았다.**

**원인 3 — 프롬프트 예시 복사 재발.** 늦은 의도 예시를 `"그러고 보니 어제 던진 『○○』…"`로
썼더니 모델이 17일 파편을 **"어제"**라고 불렀다. 이건 이미 §10-5 4차에서 겪은 실패 모드
(`[『내용 요약』]` 자리표시자 복사)와 같은 것인데 재발시켰다.
→ 예시에서 상대 날짜를 빼고 "블록의 날짜와 오늘을 빼서 말해라, 헷갈리면 숫자로"로 교체.

**원인 2의 나머지 절반 (같은 날 이어서 발견).** 범위만 KST로 고치고 **표시는 UTC로 뒀다.**
그래서 "오늘 7개" 헤더 아래 `날짜: 2026-07-19`인 줄(08:48 KST 저장분)이 섞였고, 모델은
합리적으로 **6개**라고 답했다 — 모델이 틀린 게 아니라 준 데이터가 모순이었다.
→ `kstDate()`를 만들어 파편 날짜를 찍는 **모든 곳**에 적용: `fragBlock`(근거) · `axesBlock`(축) ·
`questionSubject`(늦은 의도) · `shape()`의 저장일 카운트(UTC로 세면 지속 판정이 부풀려진다).
**교훈: 시간대 버그는 한 곳만 고치면 반쪽이 남는다. 경계(자르기)와 표시(찍기)는 다른 코드다.**

**교훈 셋:**
1. **프롬프트 예시에 구체값(날짜·제목·수치)을 쓰면 모델은 그 값을 복사한다.** 이 프로젝트에서
   두 번 터졌다. 예시엔 형식만 쓰고 값은 "블록에서 가져와라"로 지시할 것.
2. **유사도 검색은 시간·개수·존재 여부를 답할 수 없다.** "있냐/없냐", "몇 개", "언제"는
   전부 다른 종류의 질의다. RAG가 만능인 것처럼 라우팅하면 태연한 거짓말이 나온다.
3. 축 답변도 같은 병이 있었다 — 90일 창으로 계산해놓고 "이번 주엔"이라고 말했다.
   → 프롬프트에 "축은 90일 전체에서 계산된 것, 기간을 지어내지 마라" 추가.

## ⚠️ 발견의 렌즈 — 2026-07-20 유저 구술로 확정 (이게 없어서 하루를 날렸다)

**§4-E가 "렌즈 = 내 파편에서 계산된 취향"이라고만 쓰고 실체를 안 정해놨다.** 그래서 구현이
파편을 flat하게 읽고 헛것을 가져왔다. 아래는 유저가 직접 말한 것 — 추론이 아니라 진술이다.

**소스 취향:** Hacker News / Indie Hackers / Product Hunt 결.
**기술 수위:** 적당히 기술적. **너무 기술적이거나 학술적인 건 안 본다** (논문·리서치 링크 X).
**아이디어 확장의 형태:** "비슷한 프로덕트가 있고 **실제로 돈이 된다**" — 실물 + 수익 증거.
개념 설명이 아니라 **누가 만들었고 얼마 버는가**.
**다른 분야:** 예술 자체가 아니라 **새로운 관점**.
**그 외:** 진행 중인 프로젝트에 필요할 기술, 나중에 궁금해할 것, resurface 약간.

**하루 6~8개, 구성 = 「파편 확장형」 몇 개 + 「완전히 새로운 것」 몇 개.**
⚠️ **6~8은 상한이지 목표가 아니다.** 목표로 두면 채우게 되고 5번째부터 쓰레기가 섞인다 —
좋은 게 4개뿐인 날은 4개다 (§2-8 침묵 기본값).

**→ RUDY.md와 충돌한다. 문서를 고쳐야 함:** §4-E2 `주 2~3회 배치, 3~5개`,
§7-3·§4-F4 `브리핑 하루 1회 최대 3항목` → 대략 10배 볼륨이다. 조용히 바꾸지 말 것.

### 3차 브리핑 실패에서 나온 규칙 (2026-07-20) — 실질 수확 1/6

유저 판정: `5번 같은건 쓰레기야` / `앨범은 내 취향이랑 안맞으니까 빼. 내가 알아서 찾아` /
`프로덕트가 2개라는데 어딨어` / `1번은 접근은 괜찮아. 내용이 별로`.

- ⚠️ **"내가 알아서 찾는 영역"엔 들어가지 않는다.** 음악 앨범 추천이 그랬다. 취향 불일치보다
  큰 얘기다 — **유저가 이미 잘 찾는 영역에서는 루디가 무조건 유저보다 못하다.**
  루디의 가치는 **유저가 안 찾아볼 곳**에 있다. → 제외 영역 목록을 렌즈에 유지할 것.
- ⚠️ **와일드카드는 실전에서 쓰레기가 됐다 — RUDY.md §4-E2 수정 사안.** 스펙은
  *"와일드카드 1개는 축을 끈다. 순수 세렌디피티. '이건 네 축이랑 무관해. 그냥.'"* 이라고
  규정하는데, 그대로 실행하니 **연결 없는 잡학**이 나왔다(심해 금색 구체). 무관함 자체는
  가치가 아니다. 폐기하거나 "먼 연결"로 재정의해야 한다.
- **일반론 금지.** 포폴 항목이 `헤딩이 중요하다`, `양보다 질` 같은 검색하면 나오는 조언이었다.
  유저의 실제 파편으로 구체화하지 않으면 접근이 맞아도 내용이 빈다.
- **구성 라벨을 세어보고 쓴다.** "프로덕트 2 · 항목 7"이라고 썼는데 실제로는 프로덕트 1 · 항목 6.
- **"추가"를 "교체"로 읽지 마라.** 유저는 다른 갈래를 *추가*하라고 했는데 프로덕트를 4→1로
  깎았다. 그 결과 잘 되던 쪽까지 같이 사라졌다.

### 오늘의 실패에서 나온 규칙 (2026-07-20)

- **프로젝트 `description`을 먼저 읽어라.** `No phone`을 미니멀폰으로, `Caselab`을 법률
  프로덕트로 읽고 검색을 돌렸다. 둘 다 DB에 설명이 적혀 있었는데 안 읽고 파편에서 추론했다.
  실제: No phone = `Stt tss 음성 및 검수 미팅 어시스턴트`(상담사용 실시간 받아쓰기),
  Caselab = `검수 인텔리전스 보드`(위시켓 IT 컨설팅 검수 추적).
- **관심사를 flat하게 다루지 마라.** 명시적 프로젝트(진행 중인 일) / `💡`(아이디어 수집 단계,
  아직 안 정함) / `글감`(에세이 소재)은 성격이 완전히 다르다. 같은 파이프라인에 넣으면 안 된다.
- **`보이는 거절`은 유저가 명시적으로 좋다고 한 동작이다.** "이건 찾다가 실패했다",
  "이건 리스티클이라 버렸다"를 그대로 말하는 것 — §2-b 그대로. 계속할 것.
- **발견에는 축(클러스터)이 필요 없다.** "3개 이상 뭉쳐야 축"은 §4-B1이 *패턴을 말할 때*
  근거 3개를 요구한 것인데 발견에까지 끌어왔다. 관심사를 알면 되지 3개씩 뭉칠 이유가 없다.

## 브리핑 레시피 — 손으로 1회 뽑아 유저 승인 (2026-07-20)

**코드보다 먼저 손으로 만들어서 검증했다.** 7항목(확장 4 · 새로운 것 2 · resurface 1)으로
뽑았고 유저 승인. 아래는 그때 실제로 통한 것 — 구현 명세로 쓴다.

**기계화되는 부분:**
- 재료를 **성격별로** 읽기: 진행 중 프로젝트(`description` + 파편) / `💡`(미정 아이디어) /
  `글감`(에세이 소재)은 완전히 다른 것이다. flat하게 합치면 안 된다.
- 후보 → 렌즈 적용 → **버린 것 기록**(§2-b 보이는 거절, 유저가 명시적으로 좋아함)
- 원장 기록으로 반복 방지, resurface 후보(감쇠 + 지금 상황과의 관련성)

**기계화가 어려운 부분 — 여기가 진짜 일이다:**
1. **저장된 북마크 × 프로젝트 대조.** 통한 항목 ①은 `Cluely` 북마크가 뭔지 검색으로 알아내고
   `No phone`의 설명(STT 미팅 어시스턴트)과 겹쳐서 **"같은 물건이네"를 알아챈 것**이다.
   파편만으로도, 검색만으로도 안 나온다. **둘을 겹쳐야 나온다.**
2. **검색 실패 시 각도 전환.** 6번 검색 중 **3번이 재시도**였다 (영어권 STT → 한국 벤더 /
   소상공인 시장 → 국내 1인개발 수익 후기). §4-E2가 "장소 탐색이 실제 승부처"라고 한 게 이거다.
   원샷 검색이면 이 항목들은 전부 실패로 남았다.

**항목 하나의 모양:** 제목(주장) → 어느 파편에서 출발했는지 → 실물·숫자 → **함의 한 줄**
(추측이면 "이건 내 추측" 명시) → 출처 링크 2~4개.

## Exa 판정 — 손으로 돌려보고 나온 결론 (2026-07-20)

**막힌 지점 2개, 둘 다 Exa가 푸는 문제가 맞다:**
- 매 검색마다 리스티클이 절반 이상 (`8 Best ...`, `10 Best ...` 어필리에이트)
- **본문을 못 읽어서 제목만 보고 버렸다** — Product Hunt 항목에서 `Context.dev·Pazi는
  이름만 나오고 뭔지 모르겠다`고 쓴 게 그 증상이다. 검색엔진 요약만 보고 판단하고 있었다.

**Exa가 못 고치는 것 = 재시도 판단.** 위 "각도 전환"은 API가 아니라 루프 설계다.
그리고 이날 헛발질 4건(caselab을 법률로, No phone을 미니멀폰으로, 관심사 flat, STM32 우선순위)은
**전부 데이터를 안 읽어서 생긴 것이고 Exa가 고쳤을 건 0개다.**
→ **결론: Exa는 필요하지만 병목이 아니다. 루프부터 짜고 붙인다.**

## ~~E1 아이디어 발견~~ — 폐기 (2026-07-20)

**유저가 쓸모없다고 판정했고 맞는 판정이다.** `ideas.ts`는 유저의 메모끼리 조합해서
아이디어를 만들었는데, §4-E가 절대 구조로 못 박은 문장이 *"내 것에서만 돌면 에코챔버다"* 였다.
그 문장을 코드 주석에 그대로 베껴 적어놓고 정확히 그 에코챔버를 만들었다.

**왜 그걸 골랐나 (기록용):** E2는 검색 API 키 결정이 필요해서 진행이 막히고, E1은 혼자 끝낼 수
있어서다. 그래놓고 "스펙이 최우선으로 지정한 것"이라는 명분을 붙였다 — **구현 편의로 고르고
근거를 나중에 갖다 붙인 것.** 선택지를 줄 때 E1을 "권장"으로 올린 것도 그래서다.

**교훈: 막히면 우회하지 말고 막혔다고 보고할 것.** 우회해서 만든 건 버려진다.

삭제: `supabase/functions/chat/ideas.ts`, `supabase/rudy-ideas.sql`, index.ts의 `intent:'idea'` 경로.
⚠️ `rudy-ideas.sql`을 이미 SQL Editor에서 실행했다면 `utterances.variant` 컬럼과
`rudy.idea_similarity` 함수가 DB에 남아 있다. 무해하고, 브리핑 구현할 때 재사용한다
(variant = 항목 종류, similarity = 반복 게이트).

## 자유 채팅 + 웹검색 — 구현 (2026-07-21)

채팅이 저장소를 넘어 바깥까지 뻗는다(§4-E 정신). **tool-calling 풀 루프 대신** 기존 구조 재사용:
질의 재작성이 `intent`·`period`를 뽑듯 **`outward`(바깥 필요?) 불리언**을 하나 더 뽑아,
true면 Exa로 검색해 `<바깥>` 블록으로 근거에 얹는다(RAG와 병렬, 실패해도 채팅은 삶).
프롬프트: 저장소 근거와 안 섞기, url 인용, 리스티클 거르기, **이 사람 맥락에 어떻게 걸리는지**까지.
= 범용 AI와의 차이(grounding + 바깥). `chat/index.ts`가 `discovery/search.ts`의 `exaSearch` 재사용.
링크 다이제스트(§4-B3)는 **스킵 확정** — 저장 시 의도 추측은 §2-1·§2-7에 반하고, "왜 저장했나"는
이미 만든 늦은 의도(F1)가 추측 대신 묻는다. Exa로 요약만 하는 건 굳이(검색은 설명글 임베딩으로 해결됨).

### 2차 (2026-07-21) — 유저 실사용 지적 반영

- **"그거" 문제 (1순위 버그).** 검색어 추출이 "지금 이 메시지"만 봐서 "오늘 뭐 남겼지 → 그거
  찾아봐"의 '그거'가 헛돌았다. **답변은 history를 받는데 검색/추출 단계는 대화를 못 봤다.**
  → `searchQueries(question, recent)`에 최근 4턴을 넣어 지시어를 실제 소재로 풀게 함.
- **"너무 자유롭다" → outward 삼단(no/ask/go) + 역할 앵커.** 유저 통제:
  - **go**: 명시적 요청 + 이 사람 세계와 연결 → 바로 검색.
  - **ask**: 도움될 순 있지만 명시 안 됨 → 안 뒤지고 답 끝에 "바깥에서 찾아볼까?" 묻게(`<바깥가능>`).
  - **no**: 저장소·지식·잡담, 그리고 **세계와 무관한 사실 조회(날씨·시세)** — 루디의 일 아님.
  - 프롬프트에 역할 앵커: "너는 만능 비서가 아니다. 이 사람 세계를 생각하는 층이다."
- **"외부 찾는 중" 표시.** go일 때 스트림 첫 신호 `{t:'web'}` → 앱이 답 전엔 "바깥에서 찾아보는 중…",
  답 후엔 "· 바깥에서 찾아봤어". (검색이 스트림 직전에 끝나 완벽한 '중'은 아니지만 투명성 확보.)
### 3차 (2026-07-21) — 채팅 표면을 발견처럼 (홈 착지)

**열면 대화 기록 목록부터.** 최근 대화를 자동으로 이어 띄우던 걸 없앴다(`latestConversation` 삭제).
탭하면 이어가기, `새로 채팅`이면 새 대화. 발견 화면의 `mode: home | chat` 패턴을 그대로 재사용.

- **기록 시트(Modal) 폐기 → 홈 모드로 승격.** 시트와 헤더 `기록`·`새로` 버튼이 같이 사라졌다.
  대화 경계는 화면 구조가 말해야지 시트가 말하면 안 보인다.
- **응답 중엔 `‹ 목록`을 잠근다.** 나갔다 다른 대화를 열면 흘러나오던 턴이 남의 대화에 얹힌다
  (로컬 턴은 대화에 매여 있지 않다). 대화 전환·새 대화 시 `turns`·`messages`를 먼저 비운다.
- **원탭 진입(`?q=`)은 목록을 건너뛴다** — 질문을 들고 들어온 것이라 바로 새 대화로.
- **목록으로 나갈 때 갱신한다** — 첫 전송으로 생긴 행과 서버가 붙인 제목이 그때 보인다.
- **시각 표시**(유저 요청): 목록 행에 `날짜 · 시:분`, 내 말 카드 밑에도 시각. 서버 메시지는
  `messages.created_at`(select에 추가), 아직 저장 안 된 로컬 턴은 `Turn.at`(전송 시각)으로 찍는다.
- 조회 실패는 여전히 안 삼킨다(목록 자리에 표시). ⚠️ RN 미리보기 불가 — 기기 검증 필요.

## §10-8 아침 푸시 — 구현 (2026-07-21)

**새 표면을 안 만들었다.** 발견 엔진(`streamBrief`)을 그대로 재사용 — `pg_cron`이 하루 한 번
`morning-briefing` 함수를 호출하고, 그 함수가 (1) 어제 관찰 한 줄 계산 (2) `streamBrief(supabase,
{trigger:'push', prelude})`로 브리핑 생성 (3) 볼 게 있으면만 Expo 푸시 발송. 탭하면 기존 발견
화면으로 간다.

- **`trigger` 컬럼 추가**(`rudy.utterances`, pull/push) — 유저가 명시적으로 요청한 **목록 구분
  표시**용. 판정 로직엔 안 쓴다, 순수 표시 태그. 발견 화면 기록 목록에 "아침" 배지로 뜬다.
- **관찰 한 줄**(§4-F5 거울 정신) — 어제(KST) 프로젝트별 저장 개수만 계산. **"오늘은 X 하세요"
  같은 처방 없음.** 아무 신호도 없으면 관찰 자체를 생략(§2-8, 지어내지 않는다).
  브리핑 앞에 prelude로 얹으면 `parseCards`가 헤딩 이전 줄을 이미 각주로 렌더하므로 **클라 변경 없이**
  조용히 얹힌다(신기하게도 §7-21 "버린 것 각주" 로직이 그대로 이 용도에 맞았다).
- **하루 1회 상한**(§2-6): 함수 자체가 오늘 이미 push 트리거로 보냈는지 확인 후 스킵.
  cron이 중복 호출돼도 안전. **빈 브리핑이면 발송 안 함**(§2-8).
- **Expo 푸시 직접 fetch** (SDK 없이, openai.ts·exaSearch와 같은 관례) — `exp.host/--/api/v2/push/send`.
  `DeviceNotRegistered` 응답 오면 그 토큰 삭제.
- 코드: `supabase/rudy-push.sql`(push_tokens 테이블 + trigger 컬럼) · `supabase/rudy-cron.sql`
  (pg_cron+pg_net 스케줄, **서비스롤 키를 SQL Editor에서만 채우고 커밋 금지**) ·
  `supabase/functions/morning-briefing/index.ts` · `src/lib/push.ts`(등록+탭라우팅, `_layout.tsx`에 배선).

**⚠️ 배포 전제 — 이번엔 코드 배포로 안 끝난다:**
1. `expo-notifications`는 **네이티브 모듈**이다. `npx expo install`로 추가했으니 **기존 dev client로는
   안 돌아간다 — EAS로 새로 빌드해야 한다** (`eas build --profile development`). 이건 사람이 직접.
2. SQL 3개 순서대로 SQL Editor 실행: `rudy-push.sql` → `morning-briefing` 함수 배포 →
   `rudy-cron.sql`(서비스롤 키 채우고 실행, **채운 버전을 git에 올리지 말 것**).
3. Edge Function 배포: `npx supabase functions deploy morning-briefing --project-ref ibqyqpmwqujcxlnkyizf`
4. 새 dev client로 앱 실행 → 알림 권한 허용 → `rudy.push_tokens`에 토큰 들어왔는지 확인.
5. 시뮬레이터/에뮬레이터에선 토큰이 안 나온다(`Device.isDevice`로 조용히 스킵) — **실기기 필수.**

## 발견 확장 방향 — 유저 질문에 대한 의견 (2026-07-21, 일부 착수)

**A. 자유 대화(웹 검색 되는 채팅) — "범용 AI랑 차이 있나?"**
채팅 표면은 §10-5로 이미 있다(파편 RAG + 클러스터 + 늦은 의도). 지금은 **저장한 것만** 검색한다.
차이의 핵심 = **네 코퍼스 grounding + 필요할 때 바깥(Exa)까지.** 범용 AI는 널 모르고, 순수 RAG는
바깥을 못 본다. → **채팅에 Exa를 tool로 붙여** "이런 거 찾아봐/아이디어 줘"에 네 맥락 위에서 신선한
바깥을 물어오게. 그러면 범용 AI와 확실히 다르다. **의견: 할 값어치 있다. "발견의 대화형 표면."**

**B. Exa로 또 뭘 할 수 있나**
- `/contents` — 아는 URL의 본문 정제. → **링크 다이제스트(§4-B3)**: 링크 저장 시 요약·의도추정.
- `findSimilar(url)` — "이 글과 비슷한 것". → 저장한 링크 하나로 **지금 그 근처에 뜬 신선한 것** 찾기(플라이휠).
- `/answer` — 인용 붙은 grounded 답. → 채팅 웹 답변(A).
- 카테고리(research/github/news/company) — 타깃 검색.

**C. 푸시 + 아침 브리핑 — "투두·어제 요약 넣으면 상담앱 느낌? 어떻게 하면 낫지?"**
**핵심 의견: 관찰은 넣되 처방은 빼라. 그게 cheesy를 가르는 선이다.**
- **투두·오늘의 목표·달성률 = 넣지 마라.** Mind/Rudy가 명시적으로 배제한 생산성 문법
  (SPEC 큐·개수·진행률 없음, §4-F5 숫자·통계 금지). 이게 정확히 네가 싫다는 "상담앱 느낌".
- **"어제/오늘의 나"의 올바른 형태는 이미 스펙에 있다 — §4-F5 주간 편지 = 거울.**
  "어제 Caselab 5번 만졌고 No phone은 안 건드렸다"(관찰) ⭕ / "오늘은 No phone 하세요"(처방) ❌.
  사람은 자기 일주일 모양을 스스로 못 본다 — 루디는 본다. 처방 없이 **보여주기만** 한다.
- **액션 제안 "cheesy하지 않게"** = §4-E1 다음단계 아이디어: "피아노 코드가 이어지는데 다음은
  재즈 화성 아닐까?"처럼 **추측("내 추측인데") + 질문형**. 명령문 금지. 근거 파편 인용 필수.
- **결론: 아침 푸시 = 발견 브리핑 + resurface 1 + (있으면) 관찰 한 줄. 투두·코칭 아님.**
  프레임은 사서·거울이지 코치·상담사가 아니다. → §10-8 (푸시=expo-notifications + pg_cron 배치).

## §10-7 발견 엔진 — 조각내서 진행

| 조각 | 내용 | 상태 |
|---|---|---|
| ~~E1~~ | ~~아이디어 발견 (메모 조합)~~ | ❌ **폐기** — 에코챔버였다 (위 참고) |
| 브리핑 엔진 | 재료→각도(gpt-5.5)→Exa검색→조립(gpt-5.5) | ✅ **코드 완료·node 실측 검증.** Deno 배포·기기 검증 대기 |
| 앱 표면 | 전용 '발견' 화면 (`src/app/discovery.tsx`) | ✅ 스트리밍·카드·날짜기록·홈착지·백그라운드생성 |
| E4 던지기 | 발견 카드 → 텍스트 파편 (플라이휠) | ✅ 카드마다 던지기 버튼 |
| ~~more/less~~ | ~~항목 신호 → 다음 각도 가중~~ | ❌ **폐기** — `more like this` 칩으로 퉁친다 (유저, 2026-07-22) |
| more like this | 파편 상세 칩 → 바깥에서 이어지는 것 | 🟡 **칩만 있음(누르면 "아직 준비중")** — 아래 참고 |

### `more like this` — findSimilar 실측 결과가 나빠서 배선을 뺐다 (2026-07-22)

**칩 자리만 잡아뒀다.** `fragment/[id].tsx`의 `rudyQuestions`에서 `question` 없이 라벨만 있는
항목이고, 누르면 라벨이 잠깐 "아직 준비중"으로 바뀐다. 무엇으로 채울지는 **미정 — 다시 논의할 것.**

**Exa `findSimilar`를 실제 파편 URL 4개로 때려본 결과 (엔드포인트는 정상, HTTP 200 · $0.007/콜,
`excludeSourceDomain: true` 적용):**

| 출발 링크 | 물어온 것 |
|---|---|
| Cluely (AI 미팅 어시스턴트) | cluely.unaux.com · cluely.cc · aispaces.net — **Cluely를 긁어간 미러·SEO 사이트** |
| teenage engineering EP-136 | thomann · DJ TechTools · soundium · turntablelab — **같은 제품 파는 쇼핑몰 4개** |
| Product Hunt 제품 페이지 | 중국어 스팸 사이트, bit.ly 단축링크 |
| ZIP-LAB (한국 회사 홈) | position-lab.com/contact |

**원인: findSimilar는 "이 페이지와 같은 주제의 페이지"를 찾는다.** 제품 페이지를 넣으면 그 제품을
파는 가게와 미러가 나온다 — 이미 아는 것을 N번 더 보여주는 것이다. 유저가 원한 "이것과 이어지는
새로운 것"이 아니다. 리스티클 문제(§Exa 판정)와 같은 종류이고, 이건 프롬프트로 못 고친다.

**후보 방향 (미결):** ① 링크 → 모델이 각도 1개 → `exaSearch` (발견 엔진이 이미 증명한 경로),
② findSimilar 결과를 모델이 거르기(6개 중 5개를 버리면 답이 빈다), ③ 둘 다 돌려 합치기($0.014).
**교훈: 외부 API는 이름이 약속하는 것과 실제 동작이 다르다 — 붙이기 전에 실제 데이터로 때려봐라.**

**앱 표면 = 전용 '발견' 화면** (유저 선택, 2026-07-21). 사이드바 `발견`(Rudy 아래) → push 화면.
빈 브리핑이면 "오늘은 볼 게 없다"(§2-8). 브리핑은 바깥 URL(http)만 인용 → 링크는 브라우저로.

### 화면 메이크오버 (2026-07-21) — 유저 피드백 반영

- **스트리밍 전환.** 30~60초를 못 줄이는 대신(각도→조립 순차 필연) NDJSON으로 단계·토큰을 흘린다.
  `brief.ts`를 `streamBrief` 제너레이터로, `index.ts`는 NDJSON, `chatStream`에 model 파라미터 추가,
  클라이언트 `streamBriefing`(expo/fetch + lineFeeder, 채팅과 같은 방식).
- **단계 스테퍼**: 저장한 걸 읽는 중 → 뭘 찾을지 고르는 중 → 바깥 N곳 뒤지는 중 → 브리핑 쓰는 중.
  현재 단계 점이 숨쉬고(pulse), 지난 단계는 흐리게.
- **카드 UI**: 스트리밍 마크다운을 `###`마다 카드로 쪼개 하나씩 페이드+슬라이드로 차오른다("딱딱 나오게").
  index를 key로 써 마지막 카드는 자라기만 하고 새 카드만 애니메이션. 맨 끝 거절 한 줄은 각주로.
- **내용 간결화**(조립 프롬프트): 항목당 2~3문장, 불릿 쪼갬 금지, 서론 금지, 제목만으로 80% 전달.
- ⚠️ RN 미리보기 불가 — 기기 검증 필요.

### 발견 화면 2차 다듬기 (2026-07-21) — 유저 요청

- **홈 착지**: 열면 기록 목록부터. 자동 생성 안 함, `새로 발견하기` 눌러야 생성. 지난 것 탭하면 열람.
- **생성 중 표시·잠금**: 생성 중엔 헤더 `생성 중` 뱃지 + 홈 버튼 "생성 중…" 비활성. `gen` 상태를
  뷰(mode)와 분리해 목록으로 나가도 유지된다.
- **앱-나가도-저장**(index.ts): 클라가 끊겨도 `EdgeRuntime.waitUntil`로 격리를 살려 streamBrief를
  끝까지 돌려 원장에 저장. 화면 이탈뿐 아니라 앱 종료도 커버.
- **중단 대신 삭제** (유저가 의견 물음): 나가도-저장과 진짜 취소는 상충한다(둘 다 연결 끊김). 하루
  한 번·$0.05짜리라 사후 폐기가 낫다 → 기록 행에 "지우기"(deleteBriefing). 진짜 중단 버튼은 안 넣음.
- **던지기 플라이휠**(§4-E4): 카드마다 `↑ 던지기` → 카드 제목(인사이트)을 텍스트 파편으로
  insertFragment. 임베딩돼서 다음 충돌·클러스터·발견에 참여한다. 던지면 "던졌다 ✓".
  ⚠️ 출처(discovery) metadata 컬럼은 없어 생략 — 스키마 변경 없이 v1. 성적표(§6-6) 집계는 나중에.
  ⚠️ per-item acted 추적 없음(브리핑이 원장에 한 덩어리라) — 던지기는 파편 생성까지만.

### 발견 브리핑 엔진 — 완성 (2026-07-21)

**전 과정이 `check-brief.mjs`에서 실측 검증됨.** 실제 브리핑 6항목 + 버린 것 + 못 찾은 것,
$0.042. 손 퀄보다 나았다(Exa가 MosaicVoice·Selectd 같은 실물 경쟁자를 물어옴).

- **판단은 모델빨** (§8-1 실측): gpt-4o는 각도가 얕고 예시를 복사, **gpt-5.5**라야 겹치고 합침.
  → `DISCOVERY_MODEL`(기본 gpt-5.5), 채팅(gpt-4o)과 분리. 조립도 같은 모델.
- **검색 = Exa REST 직접**(`discovery/search.ts`, SDK 안 씀). `x-api-key`, type=auto, highlights.
  본문 발췌가 와서 "제목만 보고 버림" 문제 해결. ~$0.007/검색, 하루 8검색 월 ~$1.7.
- **반복 방지**(§6-4 ②, 유저가 걱정): 최근 30일 브리핑 URL을 원장 text에서 뽑아 조립 프롬프트에
  `<이미 브리핑함>`으로 넣는다. 새 게이트 없이 — 자발적 연결이 원장 재사용한 것과 같은 결.
- **수익 숫자 요구 완화** (유저: "실제 수익 안 나와도 됨") — 웹에 그런 숫자 잘 없다, 억지 금지.
- 코드: `discovery/{material,angles,search,brief,index}.ts` + node 미러 `scripts/_discovery-lib.mjs`
  + 진단 `check-angles.mjs`·`check-brief.mjs`.

**⚠️ 배포 전제 — Supabase 시크릿:** `EXA_API_KEY` 추가 필요(로컬 .env엔 있지만 Edge Function은
시크릿을 쓴다). (선택) `OPENAI_DISCOVERY_MODEL=gpt-5.5`. `OPENAI_API_KEY`는 이미 있음.
**Deno 런타임·시크릿은 로컬 검증 불가 — 배포해야 확인된다** (node 검증은 로직까지만).

### E1 아이디어 발견 — 구현 완료 (2026-07-20)

스펙이 **최우선**으로 지정한 것(§4-E 우선순위: 아이디어 > 영상 > 책 > 긴 글)이고,
외부 API가 없어도 되는 유일한 조각이다 — 아이디어는 검색이 아니라 **내 축에서 생성**하니까.

- **표면 = 채팅, 요청형.** 질의 재작성의 `intent`에 `idea`를 추가했다("아이디어 줘",
  "뭐 만들면 좋을까"). 피드(§7-4)가 아직 없으므로 자발적 발화는 안 넣었다.
- **5종 로테이션** cross·gap·next·revive·counter. `utterances.variant`에 기록하고 다음번엔
  그 다음 타입부터 — "X × Y 어때"만 반복하면 5번째부터 공식으로 읽힌다(§4-E1).
  **재료가 없는 타입은 건너뛴다** (교차는 살아있는 축 2개, 부활은 가라앉은 축 1개+살아있는 축 1개).
- **살아있는 축 / 가라앉은 축은 평균 선명도로 가른다** (합계로 하면 큰 축이 무조건 살아있게 나온다).
  → **부활 조합(④)은 코퍼스가 늙어야 켜진다.** 지금은 5일치라 가라앉은 축이 0개다.
  RUDY.md가 "감쇠 데이터를 가진 Mind만 할 수 있는 유일무이한 무기"라고 쓴 게 이건데,
  데이터가 자라야 작동한다 — 지속 축(§4-B2)과 같은 상황이다.
- **게이트 2개를 실제로 건다.** ① 근거: 본문에서 `mind://fragment/<id>`를 되읽어 **2개 미만이면
  폐기**(§6-4 ① 패턴·예측류 ≥2). ② 반복: 생성한 아이디어를 임베딩해 과거 아이디어와 cos > 0.85면
  폐기(§6-4 ②). 비교는 `rudy.idea_similarity` RPC로 — 3072차원을 앱으로 끌어오지 않는다.
- **아이디어는 본 모델로 만들고 그대로 흘린다.** 빠른 모델로 만들면 뻔해지고, 만든 걸 본 모델에
  다시 태우면 화법이 뭉개지고 근거 링크가 흔들린다 → `preAnswer` 경로로 조각내 스트리밍.
- **아이디어 경로에선 늦은 의도를 묻지 않는다.** 완성된 글을 그대로 흘리므로 `<물어볼것>`이
  모델에 도달하지 않는데 원장엔 "물어봤음"이 적혀 **그 파편이 영영 잠긴다.** 실제로 넣을 뻔했다.
- 생성 실패·게이트 폐기 시 **null → 일반 경로로 흘러간다.** 채팅에서 침묵은 답이 아니다.


---

# 2026-08-02에 RUDY-STATUS에서 잘라낸 것

> 아침 브리핑 3차(집계 전용 Edge Function)와 그 앞 발견 서사·판 A/B 설계표.
> **전부 끝났거나 대체됐다.** 살아 있는 결론은 RUDY-STATUS로 옮겼다.
> 여기 있는 계획을 다음 할 일로 읽지 마라 — 2026-08-02에 새 세션이 정확히 그 사고를 냈다
> ("1순위는 넛지"라고 답했는데 넛지는 이미 8/1에 끝났다).

## 아침 브리핑 3차 (2026-08-01 밤) — 위 4차로 대체됨. **실패 이유만 살아 있다**

RUDY.md §4-F4(아침 브리핑)·§4-A3(넛지)를 **처음으로 실제 구현**했다. 세 번 시도했고 처음 둘은
유저가 명시적으로 거부했다 — 실패 이유를 남기는 게 다음 세션이 같은 삽질을 피하는 유일한 방법이다.

### 실패 1 — 요약형 (즉흥 기준, RUDY-DISCOVERY.md를 안 읽고 짬)
"세 가지를 각각 한두 문장으로 관찰해라"는 프롬프트. 유저 판정: **"뻔한 얘기, 파편 나열, 가독성 0"**.
원인은 모델이 아니라 **필독 문서 두 개(RUDY-DISCOVERY.md·Design.md)를 안 읽고 즉흥으로 짠 것.**
CLAUDE.md가 "발견/브리핑 작업 전 반드시 읽는다"고 못박아둔 걸 건너뛰었다. **전체 되돌림**(파일 삭제,
`git status` 클린 확인).

### 실패 2 — 주장형 (RUDY-DISCOVERY §6 형식을 그대로 옮김, 파편 인용)
문서를 읽고 "제목=주장, 파편을 겹쳐 근거로 삼아라"(§6 항목 모양)를 그대로 썼다. 유저 판정:
**"제일 최악"** — *"파편을 뽑아서 이건 이래 이렇게 설명하지마. 너는 나를 좆도 모르잖아.
이렇게 줄줄이 억지로 관련짓지마."* **RUDY-DISCOVERY §6은 발견(바깥 정보) 형식이지 아침
관찰(안쪽 요약) 형식이 아니다** — 같은 문서라도 용도가 다르면 형식을 그대로 못 옮긴다는 교훈.

### 실패 2.5 — 형태소 토큰화로 "키워드" 시도 (설계 중 자체 기각)
"반복되는 키워드"를 글자 그대로 받아 한국어 조사 제거 토크나이저를 손으로 짰다. 실측 상위가
`https·com·www·아니라·이렇게·있다` — 불용어를 채워도 나오는 건 **단어 카운트지 의미가 아니다**
("옵시디언 3/0"은 통찰이 아니다). 유저가 코드를 보기도 전에 "형태소 분석을... 괜찮아?"로 제지.
**교훈: 의미가 필요하면 의미 있는 도구(임베딩 클러스터)를 써라. 텍스트 프로세싱으로 흉내내지 마라.**

### 확정된 구조 (3차)

**역할 분담:** 코드가 집계한다(`morning/stats.ts`) → UI가 그림으로 그린다(`MorningViz.tsx`) →
모델은 집계를 해석만 한다(`morning/prompt.ts`). **모델에 파편 개별 내용을 안 준다** — 프롬프트로
"엮지 마라"는 안 지켜지므로 구조로 막았다(RUDY-DISCOVERY §7-f의 2단계 분리와 같은 수법).

**관심사 변화는 임베딩 클러스터로 잰다.** `findAxes`(채팅과 공유)가 이미 의미로 파편을 묶고
이름 붙인다 — 거기에 "이 결의 증거가 최근 7일에 몇 개 / 그 앞 21일에 몇 개"만 얹었다.
`새로 생김 / 굵어지는 중 / 잦아드는 중 / 그대로` 네 상태로 코드가 판정하고, UI가 좌우 막대로
그리고, 모델은 그 이름·분포만 갖고 문장을 쓴다. 실측 예: "음성 변경 플러그인: 최근3/이전0(새로 생김)".

**출력은 제목 없는 문단 2~4개** (JSON `{headline, reading[], rejected[]}`). 마크다운 링크·헤딩 금지 —
링크를 걸 파편 id 자체를 안 줬으니 걸 수가 없다(구조적 방지, 프롬프트 규칙 아님).

**축 재료가 채팅과 다르다.** `cluster_edges`(채팅용)는 프로젝트 소속 파편을 뺀다 — "이미 조직된 건
암묵 클러스터 재료가 아니다"(발견 규칙, `rudy-cluster.sql:12-17`). 아침 관찰은 정반대로 **"내가
요즘 어떤가"라 프로젝트 파편이 핵심**이다. 실측(2026-08-01, 살아있는 74개): 제외 있으면 노드 4개
→ **축 0개**(화면이 통째로 빔). 제외를 뺀 형제 RPC `morning_edges`(`rudy-morning.sql`, 신규,
**SQL Editor 붙여넣기 필요**)로 같은 임계 0.42에서 **축 3개** 확인. `findAxes`에 rpc 이름 인자
추가(`chat/clusters.ts`) — 기본값이 `cluster_edges`라 채팅 동작은 안 바뀜.

**넛지(A3) 설계 변경.** 원래 계획(RUDY-STATUS 이전 기록: 떠오르기에 슬롯 1칸 예약)을 실측으로
기각했다 — touch_count 분포가 63:4라 "최저 가중치라 5배 덜 뽑힌다"는 전제가 이 코퍼스에서 거짓,
떠오르기 랜덤 풀(36개)과 넛지 후보(26개)가 **72% 겹쳐서** 이름만 바꿔 같은 걸 두 번 보여주는 꼴이었다.
대신 **아침 브리핑 안의 카드 하나**로 넣었다 — 빈도가 하루 1회로 구조적으로 제한되고, "3주째"
하드코딩 대신 실제 나이를 코드가 계산(`${days}일째 한 번도 안 건드렸어. 버릴까, 진짜 할까?`).
질문 문장은 **모델이 안 쓴다** — 서버 템플릿. "반드시 질문형"·"죄책감 화법 금지"가 코드로 보장됨.

**흘려보내기 지속 버그(2차 판정 후 유저 신고) — 원인·수정.** 브리핑 본문이 원장에 JSON으로
굳어 있어 넛지 응답 후에도 그 JSON은 안 바뀌었다 → 다시 열면 흘려보낸 넛지가 그대로 돌아옴.
판정 원천을 굳은 JSON이 아니라 **넛지 발화 행의 `user_response`**로 바꿈(`morning.ts` `answered()`).

**비용 — $0.128 → $0.026 (-80%, 2차→3차).** 2차는 파편 인용하느라 출력이 길었다(gpt-5.5 출력
$30/1M이 비용의 70%). 파편 목록을 안 주니 입력 6,440→1,462, 출력 2,488→607로 같이 떨어짐.

**F5 개정 반영됨 (RUDY.md).** "숫자·통계·달성률 금지" → 상태 표시(그래프)는 허용, **점수는 여전히
금지**(달성률·연속일수·전주대비·순위) — "숫자를 보여주지 마라"와 "점수를 매기지 마라"는 다른 규칙이라
후자만 남겼다. 이 경계를 지울지는 유저에게 물어본 적 없다 — 필요하면 다시 확인할 것.

**지난 브리핑 4행(구 형식) 삭제 완료.** 발견(`kind=discovery`) 35행은 안 건드림.

### 살아남은 것 / 죽은 것
화면(`MorningViz`·`MorningCard`·`morning.tsx`)과 원장 형식(`kind='pattern'` JSON 한 행)은
4차가 그대로 물려받았다. 죽은 건 **재료 만드는 쪽**뿐이다 — 위 4차 절 「죽은 것」 참고.

---



## 지금 여기 (2026-07-28)

**07-27 세션의 두 문제는 둘 다 코드에 들어가 main에 있다.** 브랜치 `rudy-discovery-fix`는
병합됐다(`68b36f5`). "미구현"이라고 믿고 다시 만들지 마라 — `discovery/idea.ts`가 2단계
분리 구조 그 자체다.

### 문제 ① 조립이 문장 중간에 잘린다 — 배포됨, 표본 1건으로 해소 신호

원인: gpt-5.5 출력 토큰이 함수 실행 시간 한도를 넘겨 죽었다(각도 out 5,111=완주 / 6,539=사망).
수정: 각도 상한 12→8, `why는 한 줄`, 원장 행을 조립 *전에* 만들어 4초마다 갱신, 중간 저장에
`cost_usd`도 기록.

**실측(07-27 13:47~48, `rudy.llm_usage` 완전 실행 1건):**

| call_site | 입력 | 출력 | $ |
|---|---|---|---|
| `discovery.motive` | 6,251 | 731 | 0.053 |
| `discovery.idea` | 564 | 799 | 0.027 |
| `discovery.angles` | 11,793 | 2,410 | 0.131 |
| `discovery.assemble` | 18,003 | 2,947 | 0.178 |

**각도 출력이 5,111~6,539 → 2,410으로 떨어졌다** = 죽던 구간에서 멀어졌다. 회당 총 **$0.389**
(2단계 분리 $0.08이 얹혀 예전 평균 $0.34보다 높다). 표본은 이 1건뿐이다.

### 문제 ② `[아이디어]`가 사실상 확장이다 — 2단계 분리로 배포됨

**→ 상세·근거는 `RUDY-DISCOVERY.md` §7-f. 여기엔 결론만.**

1단계에서 파편→**동기**만 뽑고, 2단계엔 **파편을 안 주고 동기만 준다.** 소재를 안 보여주니
따라갈 수가 없다 — 프롬프트 규칙이 아니라 구조다. 하네스 실측 6/10 성공·소재 누출 0.
`scripts/check-idea-2step.mjs`가 그 하네스다(지우지 마라).

### 07-28 브리핑(22:48 KST) — 유저: "진짜 쓰레기였다, 가치가 없다고 느꼈다"

원장 + `llm_usage` + `gate_log` 대조로 원인 확인:
- **되먹임 재현.** 07-27 [아이디어] "Mindsight 락박스"를 보고 유저가 07-28에 "Times lock box"를
  직접 저장 → 그날 저녁 재료가 돼 브랜드만 바뀐 같은 물건으로 다시 나왔다(§7-f의 되먹임이
  하루 만에 실측). **이건 되먹임 사고가 아니다** — 유저가 발견을 받아 적은 정상 동작이고,
  다음 날 그걸 그대로 되돌려준 게 고장이다.
- **저장한 링크 자기 사이트 재출고.** Pushary/TouchGrass를 Product Hunt에서 저장 →
  각도가 그 이름으로 검색 → pushary.com 3페이지가 "발견"으로 나왔다. `<이미 저장>`은
  URL로만 비교해서 도메인이 다르면(저장 URL=producthunt.com, 결과 URL=pushary.com) 안 걸렸다.
- **중복 게이트가 한국어 제목만 비교해 영어 반복을 못 잡았다.** 어제 항목의 영어 제품명이
  본문엔 있는데 게이트 비교 대상엔 없었다. 언어 차이가 의미 차이를 덮었다 — idea 게이트
  (임베딩)가 죽은 것과 같은 원인.
- **조립이 각도 하나를 항목 둘로 쪼갰다.** 게이트 로그가 `expansion 4`인데 [확장]이 5개
  나왔다. 링크 1개 규칙(`36949c7`, 07-28 21:15 커밋)이 그날 배포가 안 돼 있었다 — 배포됐으면
  이 압력이 억제했을 것.
- 출처 링크 3개 문제도 같은 미배포 때문에 재발.

### 2026-07-29 수정 — discovery v21 배포, 실측 완료

1. **저장한 제품 자기 사이트를 코드로 컷** (`isOwnSite`, `brief.ts`). 도메인이 아니라
   **이름**으로 맞춘다 — Product Hunt 링크 하나 때문에 Product Hunt 소스 결 전체를
   막으면 안 되므로. 07-28 실데이터(저장 링크 47건)로 실측: pushary.com·touchgrass.land만
   정확히 컷, 나머지 통과.
2. **중복 게이트 비교 대상에 영어 링크 제목 추가** (`matchTopics`, 프롬프트 힌트`topics`는
   그대로 둬 입력 비용 안 늚). 락박스 재구성 각도로 재보니 신호는 올랐다(0.374→0.535)
   **but 임계 0.6을 못 넘었다** — 이것만으론 부족.
3. **`REPEAT_SIM` 0.6 → 0.45.** 감이 아니라 실측 — 07-27 항목 6개 중 진짜 반복이 0.535,
   나머지 5개는 전부 0.32 이하였다(2번 적용 후 기준). 그 사이가 비어 있어 0.45로 놓았다.
   ⚠️ 그 각도는 저장이 안 돼 있어 **재구성**이었다(잡음 바닥만 실측). 되돌릴 조건: 멀쩡한
   각도가 잘리기 시작하면(`gate_log.dropped`로 보임) 0.5로 올린다.
4. `ANGLE_SYS`에 "저장한 링크 그 자체를 목적지로 삼지 마라" 추가 (`angles.ts` +
   진단 복제본 `scripts/_discovery-lib.mjs` 동기화).
5. `gate_log.detail`에 **`kept_angles`**(살아남은 각도의 slot/query/from/why) 추가.
   전엔 컷된 것만 남고 통과한 건 안 남아서 "이 항목이 어느 각도에서 나왔나"가 항상 추측이었다.

**배포 직후 실측(07-29 08:13 KST, `threshold=0.45` 로그로 새 코드 확인됨):**
- 저장한 제품 자기 사이트 0개, 출처 링크 전부 1개 — 두 버그 해소.
- [아이디어]가 실측으로 소재를 갈아탔다: `mpk mini 기능들` → 영상편집 다이얼 콘솔(완전히
  다른 물건). `아름다움은 발견될 때 눈부시다` → 포틀랜드 보도블록 모자이크(§4가 원하던
  "아예 다른 것" 갈래, 프로덕트 아님).
- **그런데 반복은 여전히 나왔다** — 원인이 이번엔 정확히 잡힌다. 게이트가 7개 중 5개를
  반복으로 정확히 잡았는데(sim 0.462~0.637, 임계 0.45 이하로 잘 걸림), `MIN_KEEP=4` 안전망이
  "5개 자르면 2개만 남는다"고 판단해 **컷을 통째로 포기**하고 5개 전부 그대로 출고했다.
  락박스 계열이 3일 연속(Mindsight→Habit Control→Promise) 나온 이유, "마케팅 툴들" 파편
  하나가 항목 2개를 먹은 이유가 이거다. **임계 조정은 먹혔다 — 안전망이 죽였다.**
- 비용 $0.319 (0.390 → 0.351 → 0.319, 계속 하락 추세).

**미해결 — 다음이 이어받을 것:** 안전망이 전부-아니면-전무(all-or-nothing)라, 반복이 많을수록
오히려 덜 거른다. **고치는 방향은 확인됨:** "다 자르거나 다 살리거나"가 아니라 **"안 겹치는
순으로 최대 4개는 살린다"**(부분 컷)로 바꾸면 이번 데이터에서 락박스·마케팅중복이 빠지고
포틀랜드 항목(0.462, 아슬아슬하게 반복 판정됐던 것)은 순위로 살아남는다. 대가는 항목 수
7→4. **유저 판단: "일단 지금 상태로 그냥 두자."** 다음에 손댈 때 여기부터 보면 된다.

### 2026-07-29 밤 — 채팅 전량 개편 + 발견 제외 (배포·커밋 완료 `b900d6f`)

**증상:** "나한테 필요한 게 뭐야" 류에 앱 개발 버그 메모가 나왔다(`Year 표시가 없네`, `반응형`,
`모달끄면 저장안되는 문제`). 유저: "무덤에 있는 것도 가져오네."

**진단 (DB 실측):**
- 파편 331개 중 **무덤 238개 = 71.9%**. 무덤은 "나쁜 것"이 아니라 **"일주일 넘은 것"**이다
  (07-16 저장분 43개 = 43개 전부 무덤). **무덤을 빼면 채팅이 지난 5일치만 아는 앱이 된다.**
  → 무덤 필터는 처방이 아니다. 눈에 띈 건 결과지 원인이 아니었다.
- 진짜 원인: 주제어가 안 뽑히는 질문이 **질문 원문 검색**(코드가 스스로 "오염된 경로"라 경고하는
  길)로 떨어져 331개 중 10개를 틀린 벡터로 골랐다.

**고친 것:**
1. **기본 갈래를 유사도 10개 → 전량으로 뒤집었다.** 331개 = 24,567토큰이라 통째로 들어간다.
   검색은 없앤 게 아니라 **역할이 바뀌었다** — 상위 10개의 상세(URL·덧·프로젝트) + 근거 칩.
2. 파편 줄에 **`·무덤` 표시**. 빼는 게 아니라 표시하는 게 처방이다.
3. 전량 블록을 **`system` 바로 뒤**에 뒀다 (캐싱은 앞에서부터 걸린다).
   ⚠️ 기각 목록의 "캐싱 안 걸린다"는 **하루 1회 브리핑** 얘기다 — 채팅은 조건이 반대라 되살렸다.
4. 프로젝트 `description`을 근거에 실었다.
5. 상한 300 → **600**. 실제 파편이 331개라 **오래된 31개가 이미 조용히 잘리고 있었다.**
6. `prompt.ts`에 `## 저장소` 규율 추가.

**모델은 안 올렸다 (`gpt-4o` 그대로).** 재료가 틀리면 모델은 소용없고, 재료가 맞으면 gpt-4o로도
될 수 있다. 나중에 올리면 **차이가 순수하게 모델 몫**으로 읽힌다.

**배포 직후 실측 (3턴):** $0.0788 / $0.0847 / $0.0767 (전 $0.015). description 실린 것 확인됨
("Nophone 맥락 설명" → 실제 설명대로 답함). 개발 버그 메모 안 나옴.
⚠️ ~~**캐시 효과가 안 보인다** — 2번째 턴부터 떨어져야 하는데 3턴 다 같다. 미확인.~~
→ **07-31 해소. 캐시는 걸리고 있었고 단가표에 캐시 입력가가 없어서 안 보였다** (아래 「다음 할 일」).

**발견 제외 (`discover_skip`)** — 프로젝트·파편 두 층. `discover_next`의 대칭이되 **비대칭이 의도**다:
포함은 1회성 지시(브리핑 돌면 꺼짐), 제외는 상시 경계(안 꺼짐). 제외된 프로젝트의 파편은
**통째로 빠진다**(미소속으로도 안 샘). **발견에만 건다** — 채팅·검색은 그대로 다 본다.
배포 확인: 유저가 이미 프로젝트 3개에 켜뒀다.

### 앱 쪽 두 판 — 2026-08-01 **코딩 끝, 실기 검증 대기**

> 기획은 07-31, 코딩은 08-01에 했다. `tsc`·`jest`(46)·`expo lint` 통과했을 뿐 **아무도 실기에서
> 안 돌려봤다** — 유저가 테스트 가능한 환경이 아니어서 두 판을 연속으로 짰다.
> 아래 설계 표는 그대로 두었다(왜 그렇게 짰는지의 근거). 지금 상태만 여기에 적는다.
>
> **배포 안 된 것 (이것부터):**
> 1. **SQL 3개를 SQL Editor에 붙여넣어야 한다** — `rudy-similar.sql`(신규, "이거 관련 뭐 있었지"가
>    이거 없으면 항상 빈 목록) · `rudy-collision.sql`·`rudy-chat.sql`(쿨다운 60→7 반영).
> 2. **chat 함수 재배포** — `npx supabase functions deploy chat --project-ref ibqyqpmwqujcxlnkyizf`
>    (`pinnedId`·`mode=more_like` 받는 자리가 서버에 있다).
>
> **판 A에서 바뀐 것:** `Saved.day`(문자열) → `at`(타임스탬프)+TTL 4시간, `seen` FIFO 24개 신설,
> `todayRecall`→`currentRecall` 개명("하루"를 약속하는 이름이라), `readSaved`/`isFresh`로
> "지금 판인가" 판정 단일화(전엔 `recallUtteranceId`·`recallSeed`가 각자 `dayKey`로 판단),
> `RecallSection`에 `AppState` active 복귀 `load()`. 쿨다운 60→7은 4곳 전부.
> **SPEC 세 줄도 고쳤다** — `:126` 자리엔 새로 생긴 seen 규칙 한 줄이 들어갔다.
>
> **판 B에서 바뀐 것:** 칩이 `이거 관련 뭐 있었지`(인라인, 채팅 안 감) / `채팅하기`(`?fid=`,
> 자동 전송 없음, 입력창 위 "물고 있음 — …/놓기") / `more like this`(`?mode=more_like&q=`)로
> 갈렸고, **`이거 다음 뭐 볼까` 칩은 없앴다**(more like this가 그 자리다).
> 서버는 `mode==='more_like'`면 `EXTRACT_SYS` 대신 `MORE_LIKE_SYS`(종류로 뽑기)를 쓰고
> `outward`를 `go`로 고정한다. **종류를 못 뽑으면 바깥에 안 간다** — 질문 원문 폴백을 막았다
> (원문에 제품명이 들어 있어 그대로 Exa로 가면 findSimilar 실패가 뒷문으로 재현된다).
> `pinnedId`는 `<물고있는파편>` 블록 하나만 더한다(갈래 판정 무수정). `prompt.ts`에 그 절 추가.
>
> **미검증·판단 남은 것:**
> - `similar_fragments`에 **유사도 임계를 안 걸었다.** 검색은 상위 N을 주는 게 일이라는 판단인데,
>   무관한 게 섞여 나오면 그때 임계를 넣는다 (충돌의 0.42는 "루디가 먼저 말할 자격"이라 다른 값이다).
> - `more like this`가 실제로 물건 대신 **종류**를 물어오는지는 돌려봐야 안다. 이게 이 판의 전부다.
> - 판 A는 풀(≈50개)에서 하루 6판×4개=24개를 뽑는다. seen 24개면 하루 안에서 안 겹치는 대신
>   **후보가 마르는 날엔 seen을 통째로 무시하고 채운다** — 겹쳐 보이기 시작하면 그게 이 신호다.

#### 판 B 실사용 첫 반응 — 2026-08-01, 채팅 채용 후 바로 두 개 껐다

`채팅하기`(fid 물기)는 붙자마자 잘 됐다("돼" 확인). 그 대화 안에서 기존 기능 둘이 실제로
망가진 게 보였다 — **로그(`rudy.messages`)로 원문을 직접 읽고** 확인, 문서 서사로 추론 안 함.

- **자발적 연결(§4-C1, `<연결>`) 껐다.** 실측: 임계(0.34) 통과한 연결을 모델이 붙이면서
  `[『』](mind://fragment/…)`처럼 **링크 글자를 비웠고**, 본문에 "이게 지금과 무슨 관련이
  있는지 모르겠어"라고 **스스로 썼다.** 임계를 넘겨도 의미로는 무관한 사례였다는 뜻 —
  튜닝으로 고칠 값이 아니라 기능 자체를 끄라는 신호로 판단. 유저: "관련있는 거 물어보지
  않으면 그냥 굳이 안 띄우는 게 좋겠다." `findLink`·`LINK_THRESHOLD`·`LINK_COOLDOWN_DAYS`·
  `<연결>` 프롬프트 절 삭제(`chat/index.ts`). `collision_by_embedding` RPC는 DB에 그대로 둠
  (되살릴 때 SQL부터 다시 안 붙이려고). **그 자리는 판 B의 `이거 관련 뭐 있었지` 칩이 받는다** —
  사람이 누를 때만, 회상 없이 안 뜨는 게 같은 정신이다.
- **늦은 의도(§4-F1, `<물어볼것>`) 껐다.** `pickQuestion`/`logQuestion` 호출을 뺐다(`intent.ts`엔
  로직 그대로 있음, 되살리려면 다시 부르면 됨). `captureAnswer`(이미 물어놓은 질문에 대한 답
  캡처)는 남겼다 — 끄는 시점에 24시간 안에 답을 기다리는 질문이 있었을 수 있어서.
- 위 둘의 공통 프롬프트 처방 하나 더 넣음: `prompt.ts`에 "## 묻지 않은 것은 꺼내지 마라" 절
  (무관한 파편 끌어오기 금지 / 확인 질문 금지 / 이름 없는 링크 금지). 두 기능을 껐다고 이
  버릇이 다른 경로(예: 축·오늘 갈래)에서 재현 안 한다는 보장은 없어서 넣은 일반 규율이다.
- **떠오르기 카드 → 파편 상세로 연결.** 막아뒀던 이유("열면 touch되어 선명해진다")가
  2026-07-19에 이미 깨져 있었다(파편 상세는 그 뒤로 실질 편집에만 touch). 문서 서사가 아니라
  코드를 다시 읽고 확인한 뒤 풀었다. "왜 지금?"은 카드 탭 자리를 잃어서 액션 줄로 이동.

이 셋 다 **아직 실기 검증 안 됨** — chat 함수 재배포 후 확인할 것.

#### 판 A — 떠오르기 4시간 갱신 (자기완결, 먼저)

| 바꾸는 것 | 어디 | 값 |
|---|---|---|
| 갱신 주기 | `recall.ts` `Saved.day` → 타임스탬프 | **4시간** |
| 한 판 개수 | `RECALL_COUNT` | 4 (유지) |
| 흘려보내기 쿨다운 | **4곳 전부** (아래) | 60일 → **7일** |
| 최근 본 것 제외 | `Saved.seen` 신규, FIFO | 24개(≈6판) |

**쿨다운 60→7은 4곳이다. 단일 원천이 없어서 한 곳만 고치면 조용히 갈라진다:**
`src/lib/supabase.ts:334` `LET_GO_COOLDOWN_DAYS` · `src/lib/fixtures.ts:175` ·
`supabase/rudy-collision.sql:36` · `supabase/rudy-chat.sql:48` (뒤 둘은 **SQL 붙여넣기 필요**).

**SPEC.md 세 줄이 어긋난다 — 유저 승인 받음(2026-07-31), 같이 고친다:**
`:117` "60일간" → 7일 · `:126` "큐·개수·진행률 표시 금지 … 생산성 앱이 되고 죽는다" → **삭제**
(유저: "그 규칙은 이제 성립하지 않아") · `:115` "하루 2개" → 4시간마다 4개
(07-30에 2→4 올릴 때 이미 어긋나 있었다).

**깨지기 쉬운 지점 (전부 실제 경로다):**
- **기존 기기에 옛 `{day,...}` 형태가 남아 있다.** `at`이 없으면 `NaN` 비교로 새 판이 뽑히니
  사고는 안 나지만, `!saved.at → 새 판` / `saved.seen ?? []`로 명시 처리할 것.
- **`recallUtteranceId`·`recallSeed`가 `day !== dayKey(...)`로 "오늘 것인가"를 본다.**
  같이 TTL 기준으로 안 바꾸면 판이 4시간마다 갈리는데 이 둘만 하루 단위로 판단한다.
- **`seen` 제외 후 후보가 4개 미만이면 `seen`을 무시하고 채운다.** 안 그러면 풀(≈50개)이
  마르는 날 떠오르기가 통째로 사라진다.
- **앱을 켜둔 채 4시간이 지나면 안 바뀐다** — `RecallSection`은 `visible` 변화에만 `load()`한다.
  `AppState` active 복귀에서 `load()` 한 줄 추가(새 의존성 0). 이게 없으면 "4시간마다"가 거짓말이 된다.
- 충돌 픽은 원장 30일 쿨다운이 이미 막는다 — `seen`은 **랜덤 뽑기에만** 건다.

#### 판 B — 파편 상세 칩 3개 (판 A 다음)

셋의 역할이 겹치지 않게 갈랐다: **안쪽 / 자유 / 바깥.**

| 칩 | 하는 일 | 새로 만드는 것 | 비용 |
|---|---|---|---|
| **이거 관련 뭐 있었지** | 안쪽 유사 파편, 상세 안에 인라인 목록 | RPC `rudy.similar_fragments` 1개 | **$0** |
| **채팅하기** | 파편 물고 채팅 진입, **자동 전송 없음** | `/chat?fid=` 경로 + 물고 있다는 표시 | 대화한 만큼 |
| **more like this** | 바깥(Exa). 구 "이거 다음 뭐 볼까" 자리 | `searchQueries` 프롬프트 분기 | Exa $0.007 |

- **`similar_fragments`는 `collision_candidates`의 쌍둥이**인데 **회상 필터를 전부 뺀다**
  (`archived`·`let_go_at`·`tier<>'pinned'`·`last_touched_at`). 검색은 "찾으러 온 행위"라
  무덤도 뒤진다(`rudy-search.sql:3`). 한 함수에 두 규칙을 섞지 마라.
  ⚠️ `fetchFragmentsByIds`는 **순서를 보존하지 않는다** — 유사도 순 정렬을 클라이언트에서 다시 건다.
  ⚠️ 임베딩은 저장 후 비동기 생성이라 갓 저장한 파편은 0건이 정상. "아직 없다"를 표시할 것.
- **`fid`는 `q`와 별개 파라미터다.** 기존 원탭 진입(`q`=받자마자 자동 전송, `chat.tsx:200`)을
  건드리지 마라 — 파편 상세의 다른 칩이 그걸 쓰고 있다. `fid`는 전송 안 하고 물고만 있는다.
  서버엔 `pinnedId` optional로 넘겨 `<물고있는파편>` 블록 하나만 더한다. **갈래(intent) 판정은
  그대로 둔다** — 여기 손대면 채팅 전체가 흔들린다.
- **`more like this`의 진짜 함정:** Exa에 가는 검색어는 `outwardBlock(topics)`의 `topics`이고
  (`chat/index.ts:384`), `topics`는 `EXTRACT_SYS`가 **저장소 검색용 주제어**로 뽑은 소재어다.
  파편을 그냥 던지면 소재 이름이 그대로 Exa로 가서 **findSimilar가 실패한 그 결과**(같은 물건
  파는 다른 사이트)가 온다. **질문 문구만 바꾸면 안 막힌다 — 검색어 만드는 자리가 다르다.**
  → 해법: `searchQueries`에 모드 하나. `more_like`면 `EXTRACT_SYS` 대신 "이게 **어떤 종류의**
  것인지 규정하고 그 종류를 찾는 검색어를 내라, 소재의 이름·브랜드·URL은 검색어에 넣지 마라,
  `outward`는 `go`"를 쓴다. **호출 수 증가 0**(이미 도는 호출의 프롬프트만 교체).
  이건 `[아이디어]` 슬롯을 고친 2단계 분리(RUDY-DISCOVERY §7-f)와 같은 약이다.
- 자기 사이트 재출고 컷(`isOwnSite`, `brief.ts`)은 발견에만 있고 채팅엔 없다. **미리 옮기지 마라** —
  실제로 나오면 그때. 볼 지점으로만 안다.

**Exa vs GPT 비교는 판 B가 돈 뒤.** 검색어 만드는 프롬프트는 그대로 두고 엔진만 바꿔 잰다
(3파전 때 "재료·기준은 공유한다, 다르면 비교가 안 된다"와 같은 방식).

#### 나중 (이 두 판에 넣지 마라)

- **넛지(A3)** — 판 B의 칩들이 넛지가 필요로 하던 "그 다음 흐름"이라 순서가 이것이다.
  붙일 때는 `recall.ts`에 슬롯 1칸 예약(충돌 1칸과 같은 수법). ⚠️ 넛지 후보는 `touch_count=0`
  이라 `weight = 1 + touch_count*2`에서 **최저 가중치**라 그냥 풀에 섞으면 5배 덜 뽑힌다.
- **넛지 푸시** — 배관은 전부 살아 있고(`push.ts` 등록·`morning-briefing:32` 발송·탭 라우팅)
  **부르는 사람만 없다**(cron 폐기). 갈 때는 **로컬 알림**(`scheduleNotificationAsync`)이 맞다 —
  넛지 재료가 전부 로컬 계산이라 서버가 낄 이유가 없고, 서버 푸시로 가면 폐기한 cron이 되살아난다.
- **채팅 답변 → 파편으로 저장** — 필요하다고 합의됐으나 설계 미착수. 백로그 「채팅이 저장소를
  고치는 자리로」의 일부라 D1 승인 게이트와 같이 봐야 한다.
- ⚠️ **SPEC `:129-130`**(헤매기에서 판단 버튼을 뺀 이유 — "무한히 뽑을 수 있는 곳에 판단을
  붙이면 솎아내는 노동이 된다")은 **유저가 지우라고 한 `:126`과 다른 규칙이다. 살아 있다.**
  떠오르기가 하루 6판이 되면 이 압력이 옮겨붙는다. 흘려보내기가 노동처럼 느껴지면 여기부터 본다.

### 다음 할 일 (2026-07-31 정리 — 다음 세션 시작점)

> 아래 0/1번(클코 3파전·이관)은 이 세션에서 끝났다 — "3파전 결론" 섹션 참고, 1번 채택·운영
> 방식도 확정됐다. 여기는 그 다음으로 남은 것만 정리.

1. **`discover-websearch` 실사용 관찰 (유저가 직접 돌리며 판단, AI 액션 아님).** 운영 커맨드:
   `node scripts/discover-websearch/run.mjs`. 볼 것은 "2026-07-31 수정" 섹션 맨 아래 "다음" 참고.
2. **채팅 모델(`gpt-4o`) 올릴지 테스트 — 하기로 함(2026-07-31).** 며칠 써보고 판단.
✅ **채팅 캐시 — 2026-07-31에 확인 완료. 오진이었다: 캐시는 처음부터 걸리고 있었다.**
`rudy.llm_usage.cached_tokens` 7일치 실측 — `chat.answer` 13건 중 7건이 캐시 히트, 입력의
40.2%가 캐시(97,408 / 242,302). 대화 안에서 보면 정확히 설계대로다:

| 대화 | 턴 | 입력 | 캐시 |
|---|---|---|---|
| `045cd78d` (07-29) | 1 → 2 | 29,849 → 32,291 | 0 → **27,520** |
| `c2a9ce52` (07-30) | 1 → 2 → 3 | 32,922 → 30,543 → 33,470 | 0 → 2,944 → **29,568** |

**비용이 안 떨어진 진짜 이유: `_shared/usage.ts` 단가표에 4o 계열 `cachedIn`이 없었다.**
`estimateCost`가 `p.cachedIn ?? p.in`으로 폴백해서 캐시 토큰을 **정가로** 계산했다 —
캐시가 아무리 걸려도 `cost_usd`는 절대 안 떨어지는 구조. 07-29의 "3턴 다 같았다"는
캐시 실패가 아니라 **계기판 고장**이었다. 고침: `gpt-4o` `cachedIn: 1.25`,
`gpt-4o-mini` `cachedIn: 0.075` (공식 가격표 재확인 — 4o 계열 50% 할인, gpt-5 계열 90%).
효과: 위 `c2a9ce52` 3턴차 기록 $0.0853 → 실제는 **$0.0483** (-43%). 앞으로 캐시 걸린
턴은 이만큼 싸게 찍힌다(실제 청구는 원래 이 값이었다 — 기록만 틀렸다).

⚠️ **중간의 `cached=2944`(= systemPrompt만)는 버그가 아니다.** `<저장소>` 전량 블록은
**기본 갈래에서만** 붙는다(`index.ts:486`). 기간·열거·축·orient 턴이 끼면 프리픽스가
통째로 바뀌어 다음 턴 한 번이 차가워진다. 갈래가 섞이는 대화의 구조적 대가지 고장이 아니다.
`chat.question_judge`가 캐시 0%인 것도 정상 — 입력이 건당 200토큰이라 최소 1,024에 못 미친다.

✅ **`_discovery-lib.mjs`의 `discover_skip` 미반영 — 07-31에 고침.** 재확인해보니 이 버그는
`check-brief`·`check-angles` 등 **진단 스크립트에만** 영향이 있었다 — `discover-websearch`가
매일 쓰는 재료 로더는 `discover-claude/material.mjs`([discover-websearch/run.mjs:32](../scripts/discover-websearch/run.mjs))
인데 그건 처음부터 `discover_skip`을 반영하고 있었다(실사용 무영향, 코드로 확인함).
`loadMaterial()`에 `discover_skip` 필터·`hidden` 셋을 `discover-claude/material.mjs`와 같은
패턴으로 추가해 진단·실물을 다시 맞췄다.

✅ **`chat` 열거 경로 검증 — 유저가 직접 확인 완료(2026-07-31).**

❌ **조립 비용 모델 비교(gpt-4o 전환 등) — 유저가 뺐다(2026-07-31): "테스트 안해도 될것같아."**
다시 꺼내지 마라 (조립을 gpt-4o로 $0.150→$0.063 레버 자체는 §비용 섹션에 기록만 남겨둠).

❌ **안전망 부분 컷 전환 — 유저가 기각했다(2026-07-31): "다 나와도 상관없어. 적은 건 문제지만
많은 건 문제가 아니야."** 즉 중복 몇 개 섞여도 항목 수 7개가 4개로 주는 것보다 낫다는 판단.
`MIN_KEEP=4` all-or-nothing 안전망은 **지금 그대로 둔다.** 다시 제안하지 마라.

✅ **`angles.ts` 배포 완료 (2026-07-31, 유저 확인).** `discovery` 함수 버전 23, 오늘 배포됨
(`npx supabase functions list`로 재확인). 위 "미정"은 07-30 세션 기준으로 낡은 기록이었다.

### 클코 이관 — 합의된 설계 (2026-07-29, 미착수)

발견을 OpenAI 대신 **맥에서 `claude -p`(헤드리스, v2.1.218 확인)로** 돌린다. 유저 지시:
**"지금 있는 건 그대로 둬. 건드리지 말고 새로 만드는 걸로."** → `scripts/discover-claude/` 신규,
기존 `discovery` Edge Function·`angles.ts`·`brief.ts`·`dedupe.ts`·`discovery.tsx`·
`_discovery-lib.mjs` 전부 무수정. 앱은 `rudy.utterances`를 읽으므로 **UI 변경 0**.

**이득이 비용($12/월→0)보다 큰 게 둘 있다:**
- **원리 D(재시도 루프)가 공짜로 들어온다.** RUDY-DISCOVERY §2-D: "검색 실패하면 각도를 바꿔라.
  원샷 금지. **Exa도 이건 못 고친다 — 루프 설계다**." 지금 파이프라인은 각도→검색 1회→조립으로
  **루프가 없다.** 에이전트는 루프가 기본이다.
- **RUDY-DISCOVERY.md를 원본 그대로 읽힌다.** 지금 §1~§6이 `angles.ts`에 압축돼 박히고
  `_discovery-lib.mjs`에 또 복제돼 있는데, `Read` 툴로 문서를 직접 읽으면 **네 번째 벌이 안 생긴다.**
- 보조 논거: §0에 따르면 **골드 스탠다드 7항목은 원래 Claude Opus가 손으로 만든 것**이다.
  검증 안 된 모델 교체가 아니라 기준을 세운 판단자에게 돌려주는 것.

**2026-07-29 밤 1차 실행 (`briefing-…13-29-14.md`):** 19턴·494초·$1.53, 환각 0(전수 검증됨),
사고 0(개발버그 없음, 링크재출고 없음), 재시도 실증(각주에 "각도를 두 번 바꿔봤다").
유저 지적 3개 → 프롬프트 수정: ① 오늘 파편 쏠림 금지 ② `idea` 최소 2 강제
③ 목표 8개(RUDY-DISCOVERY §1 "상한이지 목표 아니다"를 실사용 튜닝이 뒤집은 걸 반영).

**2차 실행 (`briefing-…23-25-29.md`, 수정 후):** **28턴·933초(15.5분)·$2.30 — 더 오래·더 비쌈.**
정상 종료(is_error false). **아직 안 봤다 — 다음 세션에서 유저가 결과 보고 판단.**
재료 37K를 매 턴 재청구하는 구조라(§ 위 "사용량" 계산) 턴 수가 곧 비용이고, 이번엔 목표를
"8개·idea 2+"로 세게 걸어서 턴이 늘었을 가능성. **다음에 볼 것:** ①②③이 실제로 먹었나
②비용 상승이 감수할 만한가 → 아니면 `--max-turns` 상한 또는 sonnet 전환 검토.

**결정된 값:** `trigger='pull'` · 중복 방지는 **힌트만**(임베딩 게이트는 안 옮긴다 —
"아직 안 일어난 문제에 장치 만들지 마라") · 첫 판은 **수동**, 만족하면 자동.

⚠️ **`trigger`는 `push`로 잡았다가 되돌렸다** (2026-07-29, 유저 지적: "왜 아침으로 떠?
이건 브리핑 아니고 그냥 발견 아니야?"). `push`는 앱에서 **"아침" 배지**가 되는데, 이 스크립트가
만드는 건 **발견**이다. 같은 날 유저가 "아침 브리핑은 발견 빼고 관찰만"으로 정했으므로
발견을 아침이라 라벨링하면 그 결정과 어긋난다. `push`를 밀었던 이유(모닝 버튼 잠금 =
중복 지출 방지)는 부수 효과였고, 아침이 관찰 전용이 되면 그 버튼 자체가 없어질 자리다.
→ **아침 브리핑을 만들 때 그때 새 표면을 쓴다. 발견은 발견으로 남는다.**
부작용: 목록에서 클코 발견과 앱 발견이 **구분이 안 된다**. 비교가 필요하면 `.work/`의
타임스탬프와 `created_at`을 맞춰 본다 (앱을 안 건드리기로 한 결과).

**자동 실행 — 조사 완료. 맥이 유일한 길이다:**

| 방법 | 되나 | 이유 |
|---|---|---|
| 클라우드 예약 에이전트 | ❌ | **로컬 파일·환경변수 접근 불가.** `.env`가 gitignore라 Supabase 키가 없고 옵시디언도 못 읽는다 |
| 세션 크론(CronCreate) | ❌ | 세션 메모리에만 산다. 클로드 끄면 사라진다 |
| 모바일 | ❌ | 모바일 런타임 없음 |
| GitHub Actions | ⚠️ | 돌지만 구독이 아니라 **API 키 과금** — 목적과 반대 |
| **맥 launchd** | ✅ | **전원 연결이면 뚜껑 닫아도 됨** (`pmset repeat wakeorpoweron`). 배터리+닫힘은 안 됨 |

RUDY.md §3-1이 Things 브리지에 대해 내린 판단과 같다: "맥이 꺼져 있으면 그 시점에 멈출 뿐."

### 클코 3파전 — 2026-07-30, 코딩 끝. 다음 세션이 돌리고 판단한다

**시작은 "GPT랑 비교하자, 모델만 바꿔봐"였는데 두 번 헛다리를 짚었다.** 기록해두는 이유는
같은 헛다리를 또 안 짚기 위해서다:
- 1차 시도: `discover-claude`(어제)와 `discover-split`(오늘 초반)을 비교했는데, **둘 다
  내가 그날그날 새로 쓴 프롬프트였다.** `ANGLE_SYS`/`ASSEMBLE_SYS`(프로덕션 상수)를
  **0건** 사용 — grep으로 확인됨. 그래서 "오늘 판이 왜 돈 얘기로 쏠렸나"에 내가
  "품질이 낫다"는 근거 없는 판단까지 얹었다가 유저에게 정면으로 지적당했다.
- 원인은 셋: ① 각도를 12개로 강제(§1 "상한이지 목표 아니다"를 내가 또 위반) ②
  RUDY-DISCOVERY.md §1의 "실제로 돈이 된다/얼마 버는가" 문장 자체가 사업 방향을
  유도 ③ Exa `type=auto`가 인터뷰가 아니라 정리글·업체 홈페이지를 물어옴(옛 스크립트
  `check-idea-engines.mjs` 헤더에 이미 기록돼 있던 현상).
- **재료가 원인이 아니었다** — 실측: 어제·오늘 재료를 diff해서 돈 각도의 출발 파편
  (혁신의숲 리포트 4건·'운영까지 각잡고'·Fedica·해외살기)이 **둘 다에 똑같이 있었음**을
  확인. 어제 판은 안 골랐고 오늘 판이 골랐다 — 재료 탓이 아니라 각도 프롬프트 탓.

**돈 얘기 문장을 지웠다** (하지마 형태 아니라 문장 자체를 뺐다 — 유저가 명시적으로
"제일 싫어하는 얘기"라고 했다):
`RUDY-DISCOVERY.md` §1·§2-D·§3-④·§6 · `supabase/functions/discovery/angles.ts`(배포판) ·
`scripts/_discovery-lib.mjs`(노드 미러, 프로덕션과 반드시 동기화) · `discover-claude/prompt.md` ·
`discover-split/prompt-angles.md`. ✅ **`angles.ts` 2026-07-31에 배포 완료** (버전 23) — 앱
발견 버튼에 반영됨.

**최종 합의: 3파전, 코드로 확정.** 유저 확정 문장: "1. 클코 + 클로드websearch (루프 없이)
2. 클코 + exa". 재료·기준(RUDY-DISCOVERY.md)은 셋이 전부 공유한다 — 다르면 비교가 안 된다.

| | 폴더 | 각도 | 검색 | 조립 | 상태 |
|---|---|---|---|---|---|
| 원본 (안전망) | `discover-claude/` | 한 호출 (WebSearch, 루프 허용) | Claude WebSearch | 같은 호출 | 무수정 |
| **1번** | `discover-websearch/` | 한 호출 (WebSearch, **재시도 금지**) | Claude WebSearch | 같은 호출 | 신규 |
| **2번** | `discover-split/` | 별도 호출 (툴 없음, 인라인 재료) | Exa (Node, 병렬) | 별도 호출 (재료 안 봄) | 신규 |

- **1번**은 `discover-claude/`를 `cp`한 것 — `diff` 결과가 전부다: "재시도 정상, 2~3번까지"
  → "재시도 금지, 각도 10~12개 미리 잡아 방어" + "검색은 병렬로 던져라". 그 외 글자 그대로.
  원본(`discover-claude/`)은 **무수정, 언제든 되돌아갈 안전망.**
- **2번**은 각도 상한을 12→**8~10**으로 되돌렸다(위 원인 ①). 조립 프롬프트에 지정 파편
  보호 규칙 추가.
- **셋 다 기본이 `--save` 없이 저장 안 함** — 원장에 넣으면 다음 판 재료의 <이미 다룬 주제>가
  바뀌어서 비교 조건이 흔들리고, 앱에서 셋을 구분할 방법도 없다(전부 `trigger='pull'`).
- **비교 축이 이제 명확하다:** 1번 vs 원본 = "재시도를 끄면 얼마나 싸지고 얼마나 상하나".
  1번 vs 2번 = "어제 좋았던 게 프롬프트냐 WebSearch냐"(유저 원래 질문). 2번 vs 1번의
  Exa 출처(정리글) vs WebSearch 출처(1차 인터뷰) 차이도 같이 보일 것.

**지정 파편(`discover_next`) 버그 — 이번에 같이 고쳤다.** 원인: `brief.ts:377`이 스스로
인정 — "전부 내린다. 어느 지정이 실제로 각도가 됐는지는 알 수 없다." 검색이 망해 항목이
안 나와도 표시는 사라져서, 유저가 지정해놓고 표시만 잃는 사고가 실제로 났다. 조치:
① 두 프롬프트에 "지정에서 나온 각도는 절대 버리지 마라, 못 찾았으면 못 찾았다고 쓰되
항목은 내라" 추가 ② 소비는 **자동으로 그대로 유지**(유저 확정: "일일이 못 끈다") —
다만 2번은 각도 JSON에 `from_picked`가 있어서 **"각도조차 안 됐으면 그때만 안 내린다"**를
코드로 구현 가능했다(1번·앱은 출력이 마크다운이라 이 확인 자체가 불가능 — `brief.ts:377`의
한계 그대로). ③ `discover-claude/material.mjs`가 전부 `data ?? []`라 **쿼리가 죽어도
조용히 0건**이 되던 것도 고쳤다 — 이제 에러를 던진다(세 판이 공유하는 파일이라 셋 다 적용됨).

### 3파전 결론 — **1번(`discover-websearch`) 채택** (2026-07-31, 유저 확정)

1·2번을 돌려서 나란히 놓고 판단했다. 브리핑 2건은 원장에 저장돼 앱 발견 탭에 있다
(맨 위 `※ 1번/2번 …` 라벨로 구분 — 앱은 셋을 구분 못 하므로 라벨을 본문에 넣었다).

| | 턴 | 시간 | API환산 | 세션 사용량 | 현금 |
|---|---|---|---|---|---|
| 1번 | 15 | 546초 | $1.43 | **+24%p** | 0 |
| 2번 | 2+2 | 629초 | $1.39 | +27%p | + Exa 7건(몇 센트) |

- **"턴 수가 곧 비용"은 깨졌다.** 2번은 턴이 1/4인데 더 먹었다 — `claude -p`를 두 번 띄워
  캐시를 두 번 새로 만들고(118K vs 99K), 각도 호출 하나가 출력 29.7K(사고 포함)를 태웠다.
  **턴을 줄이는 방향으로 최적화할 근거가 사라졌다.**
- 1번이 이긴 축: 갈래 다양성(오프라인 4개 vs 1개) · 출처 1차성 · 보이는 거절(3건 vs 1건).
  2번이 이긴 축: `idea` 슬롯이 전부 프로덕트로 착지 · 각도가 JSON이라 **사후 진단이 가능**
  (1번은 마크다운이라 "어느 각도에서 나왔나"를 영원히 못 짚는다 — `brief.ts:377`과 같은 한계).
- **`discover-split`·`discover-claude`는 안 지운다** — 안전망 + Exa/분할 구조의 유일한 기록.

### 2026-07-31 수정 (1번에만 적용 — 나머지 두 판 프롬프트는 무수정)

1. **`idea` = "누가 이미 만들어 파는 물건"으로 못박음** + **`lens` 1 → 2.** 위 「절대 건드리지
   말 것」 참고. 진단: 소재 절단 규율은 지켜졌는데 검색어에 지명·프로그램 이름이 들어가
   idea 3개가 전부 "가볼 곳"으로 착지했다(감각차단탱크·텃밭분양·오픈마이크). 원인은
   프롬프트 문구가 아니라 **아직 없는 물건은 검색이 안 되고 이미 파는 체험은 검색이 되는 것.**
2. **출처 링크 1개 → 1~3개** (유저: "같은 아이디어여도 제품이 다를 수 있는데 그 다른 게
   또 의미있을 수 있으니까"). 조건 = 뒤 링크가 앞과 다른 걸 보여줄 때만.
3. **무덤 링크가 재료에 현재형으로 들어가던 것** (`material.mjs`, 세 판 공유).
   실측: `=== 이미 저장한 링크 ===` 68개 중 **57개(84%)가 무덤**이었고, 유저가 이틀 전 묻은
   Duedance를 1번이 "저장해둔 Duedance"라고 각주에서 불렀다.
   ⚠️ **빼는 게 처방이 아니다** — 묻어도 유저가 가진 링크라 빼면 07-28 사고(Pushary 자기 사이트
   재출고)가 그대로 재발한다(Pushary·TouchGrass가 지금 무덤이다). **`(묻음)` 표시 + "막는
   용도로만 봐라"**로 갔다. 채팅 무덤 문제를 푼 방식과 같다.

**운영 방식 확정 (2026-07-31):** 자동화 안 한다 — 유저가 매일 손으로 커맨드 치고 앱에서
확인한다. 그래서 `discover-websearch/run.mjs`의 **기본을 저장으로 뒤집었다**
(비교용이라 기본이 저장 안 함이었는데, 채택됐으니 의미가 없어졌다).
**돌리는 커맨드: `node scripts/discover-websearch/run.mjs`** (실험은 `--no-save`).

**다음 (유저가 직접 볼 것):** 돌리면서 ① idea가 물건으로 착지하나 ② 관점 2칸이 오프라인을
받나 ③ 뒤 링크가 앞과 같은 종류인 항목이 3개 이상이면 링크를 1개로 되돌린다.
프로젝트 `discover_skip`·재료 상태는 유저가 알아서 관리 — AI가 먼저 건드리지 마라.
`angles.ts`(앱 발견)는 2026-07-31 배포 완료.

### ⚠️ 발견의 위상 — 유저 온도 (2026-07-28)

유저: **"사실 발견에 크게 의미를 두고 있지 않아. 너무 비싸기도 하고. 무슨 의미인가 싶기도 하고."**

RUDY.md는 발견을 1급으로 놓지만 **지금 유저의 실제 온도는 그렇지 않다.** 발견에 큰 작업을
새로 제안하기 전에 이걸 먼저 떠올려라. 회당 $0.39는 매일 돌리면 월 $12다.
**비용을 줄이는 제일 큰 레버는 모델이 아니라 빈도다**(아래 비용 섹션) — 의미가 확실하지
않은 기능이면 모델 교체(-26%)보다 "덜 자주 돌린다"가 먼저다. 단, 빈도는 유저가 정한다.

---



### 아침/주간 브리핑 — 2026-07-29 재점검 (지금 만들지 않기로)

**모닝 브리핑은 발견과 거의 같은 물건이다.** `morning-briefing/index.ts` 주석이 자인한다:
"발견 화면의 '새로 발견하기'와 **같은 엔진**(streamBrief). `trigger='push'` 태그만 다르다."
차이는 `prelude` 한 줄뿐이고, 그마저 **어제 프로젝트별 저장 개수**이며 모델에 안 가고
`brief.ts:309`에서 문자열로 앞에 붙기만 한다.

**F4가 약속한 재료 4개 중 0개가 있다:** 캘린더·투두 맥락(어댑터 없음) / 맥락 리서페이스 A2
(캘린더·지오펜스 필요) / 넛지 A3(**재료는 다 Mind 안에 있다 — 유일하게 지금 만들 수 있는 것**) /
선제 관찰 D2(캘린더 필요). 지금 들어 있는 발견(E)은 F4 재료 목록에 **없고**, RUDY.md §2-5는
오히려 "발견은 아침 브리핑에 **1개까지**"라고 상한을 건다.

**유저가 원한 것 (2026-07-29 구술):** "어제 혹은 오늘 너는 이랬다. 그리고 패턴, 성향,
최근 관심사, 내가 요즘 어떤지." → 이건 이미 유저가 07-22에 파편으로 적어뒀다:
*"내가 오늘 생각해야할게있나 하면 메모리를 뒤져서 내 경향성을 판단해서 딱 추천해줘야되는데
흐림정도나 이런것도 생각을 하든지."* **그리고 그건 `chat/orient.ts`(축×흐림)가 이미 하는 일인데
채팅에 물어야만 도는 자리에 붙어 있다.**

**결론: 지금 안 만든다.** 코퍼스가 2주치라 축이 잘 안 서고(§6 "코퍼스 자라면 자동으로 켜짐"),
A2·D2는 어댑터가 없다. **투두가 들어오거나 코퍼스가 자란 뒤에 다시 본다.**

**✅ 만들 때의 모양은 정해졌다 (유저 확정 2026-07-29):**
> "아침 브리핑은 **발견은 하지 말고 그냥 관찰 정도만**. 근데 **관찰을 좀 더 디테일하게** 가면 좋겠어."

- **발견을 아예 뺀다.** RUDY.md §2-5의 "1개까지"보다 더 센 결정이다 — 0개.
  아침과 발견은 **다른 표면**이고, 발견은 발견 탭에서 당겨서 본다.
- **관찰이 본체다.** 지금 `observationLine()`은 **개수만** 낸다("어제는 No phone에 3개").
  개수는 관찰이 아니다 — 그게 "뭉뚱그려진" 부분이다. 디테일하게 간다는 건 최소한:
  무엇을 던졌나(제목) · 어느 결이 새로 생겼나 · 뭐가 흐려지고 있나 · 안 건드린 게 뭔가(넛지 A3).
  유저가 원한 것: *"어제 혹은 오늘 너는 이랬다. 그리고 패턴, 성향, 최근 관심사, 내가 요즘 어떤지."*
- ⚠️ `observationLine()`이 낸 줄은 **모델에 안 간다**(`brief.ts:309`에서 문자열 접합).
  관찰이 본체가 되면 이 구조부터 바꿔야 한다 — 관찰을 모델이 **쓰게** 해야 한다.

⚠️ **아침 브리핑은 유저가 사실상 본 적이 없다** — `trigger='push'` 원장이 28건 중 **1건**
(2026-07-22, cron 시절). "별로다"는 출력물 평가가 아니라 **설계 의도와 다르다**는 지적이었다.

**조언("이런 거 해봐라")의 리스크 — 유저가 먼저 우려했다.** prompt.ts §오늘이 이미
"처방하지 마라"를 건다. 정체는 이것: **조언은 틀리면 무시를 학습시킨다**(§2-8). 다만 셋은 다르다 —
처방("오늘은 X 해라") ❌ / 열어둔 제안("케이스랩 3주 조용한데, 접은 거야?") ⭕ /
재료 제시("찾아볼까?") ⭕. **질문형으로만** 내보내면 리스크가 준다. 중간 형태는 이미 있다
(채팅의 `outward='ask'`). 무시가 쌓이는지 `utterances.user_response`로 보고 결정한다.

